import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "coding-agent-evals-harbor-vercel-sandbox",
  title: "Coding Agent Evals With Harbor and Vercel Sandbox",
  description: "Build repeatable coding-agent evaluations with Harbor and Terminal-Bench, run isolated trials on Vercel Sandbox, and interpret results without fooling yourself.",
  date: "2026-09-18",
  readTime: "14 min",
  category: "Engineering",
  tags: ["Coding agents","Harbor","Terminal-Bench","Vercel Sandbox","AI evaluation","Agent benchmarks"],
  faqs: [
      {
          "question": "What is Harbor?",
          "answer": "Harbor is an open-source framework for running and analyzing agent and model evaluations in reproducible task environments. It is the official harness for Terminal-Bench and also supports other benchmark datasets."
      },
      {
          "question": "What does a Harbor task contain?",
          "answer": "A task combines an instruction, an environment definition, resource and timeout settings, and a verifier. The verifier writes a numeric reward so trials can be compared and inspected."
      },
      {
          "question": "Why run Harbor evaluations in Vercel Sandbox?",
          "answer": "Vercel Sandbox gives each trial an isolated Firecracker microVM, supports parallel runs, and can enforce network rules outside the guest. It avoids forcing every evaluation through one local Docker host."
      },
      {
          "question": "Can a single benchmark score identify the best coding agent?",
          "answer": "No. A score depends on the dataset, agent harness, model, prompt, tool versions, environment, attempt count, and verifier. Use repeated runs and task-level analysis before drawing conclusions."
      },
      {
          "question": "How should API credentials be provided to evaluation sandboxes?",
          "answer": "Prefer firewall-level credential brokering or narrowly scoped temporary credentials. Vercel can inject credentials into matching outbound requests so the value does not enter the guest filesystem or environment."
      },
      {
          "question": "Should production teams create their own Harbor dataset?",
          "answer": "Yes, when public benchmarks do not represent their repositories and failure costs. A small dataset of real, sanitized tasks often reveals more about deployment readiness than a larger generic leaderboard."
      }
  ],
  coverImage: "/images/coding-agent-evals-harbor-vercel-sandbox.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/coding-agent-evals-harbor-vercel-sandbox.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/coding-agent-evals-harbor-vercel-sandbox.webp\" alt=\"Coding Agent Evals With Harbor and Vercel Sandbox\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>A coding-agent benchmark is a software system, not a trivia quiz. The score depends on the task instructions, filesystem, installed tools, network access, time limit, verifier, agent harness, model configuration, and number of attempts. Change one of those and you may be measuring a different system.</p>\n<p>Harbor packages those moving parts into repeatable tasks and jobs. Terminal-Bench provides difficult terminal-based tasks. Vercel Sandbox can now run Harbor trials in isolated Firecracker microVMs, making it practical to execute several evaluations in parallel without turning a laptop into a warm pile of fans.</p>\n<p>The setup is useful, but the command is the easy part. A credible evaluation needs controlled inputs, reproducible environments, careful secret handling, and task-level analysis. Otherwise the leaderboard number acquires more confidence than the experiment earned.</p>\n<h2>What Harbor Actually Evaluates</h2>\n<p><a href=\"https://github.com/harbor-framework/harbor\">Harbor</a> is an open-source framework for evaluating agents and models in containerized environments. It can run coding agents such as Codex CLI, Claude Code, OpenHands, Gemini CLI, and others against registered or local datasets.</p>\n<p>A dataset contains tasks. Each task has four important parts:</p>\n<ul>\n<li>An instruction describing the work</li>\n<li>An environment containing the starting files and dependencies</li>\n<li>A verifier that tests the final state</li>\n<li>Configuration for resources, timeouts, and metadata</li>\n</ul>\n<p>Harbor calls one execution of an agent on one task a trial. A job is a collection of trials, possibly spanning several tasks, agents, models, or attempts.</p>\n<p>That vocabulary matters when reading results. \"The model scored 62%\" hides which agent wrapper drove it, how many trials ran, and whether failures came from reasoning, setup, tool use, or the verifier.</p>\n<h2>Why Terminal-Bench Is Useful and Incomplete</h2>\n<p><a href=\"https://www.tbench.ai/news/announcement-2-0\">Terminal-Bench 2.0</a> evaluates whether an agent can complete work in a terminal environment. The tasks are manually reviewed and come with verifiers, giving the benchmark more substance than asking a model to explain code in a chat box.</p>\n<p>Terminal tasks exercise planning, command use, file editing, dependency management, debugging, and persistence across several steps. Those are closer to real engineering work than isolated code completion.</p>\n<p>They are still a sample. Your product may care about a monorepo, a particular deployment stack, database migrations, accessibility checks, or a private framework. A strong Terminal-Bench result does not prove that an agent can safely modify your billing service. It shows performance on the benchmark's task distribution under its published setup.</p>\n<p>Use a public benchmark for a shared baseline. Add a private, sanitized dataset for the work your team actually needs.</p>\n<h2>Why the Execution Environment Changes the Result</h2>\n<p>An agent that spends eight minutes installing dependencies has less time to solve the task. An image with a warm package cache behaves differently from a clean image. A network outage can look like a reasoning failure. A reused filesystem can leak artifacts from an earlier trial.</p>\n<p>Vercel's <a href=\"https://vercel.com/changelog/run-terminal-bench-and-other-harbor-evals-on-vercel-sandbox\">Harbor integration</a> runs each trial in its own Firecracker microVM. Harbor 0.22.0 and later can select the provider with <code>--env vercel</code>. Independent microVMs make parallelism easier while reducing cross-trial contamination.</p>\n<p>Vercel's <a href=\"https://vercel.com/docs/sandbox\">Sandbox documentation</a> describes each sandbox as having its own filesystem and network. The environment supports root access, package installation, snapshots, and management through a CLI or SDK.</p>\n<p>Isolation improves the experiment. It does not make the result automatically reproducible. Pin the task, agent, model, environment image, dependencies, and relevant configuration.</p>\n<h2>Install Harbor and Run a First Trial</h2>\n<p>Use a dedicated environment for the harness. The Vercel integration announcement uses the <code>harbor[vercel]</code> extra.</p>\n<pre><code class=\"language-bash\">uv tool install 'harbor[vercel]'\nharbor --help\nharbor dataset list\n</code></pre>\n<p>Authenticate Vercel in the environment where Harbor runs. Supply model credentials through your chosen provider or use Vercel AI Gateway for a consistent model endpoint.</p>\n<pre><code class=\"language-bash\">export VERCEL_TOKEN=\"&lt;set-locally&gt;\"\nexport AI_GATEWAY_API_KEY=\"&lt;set-locally&gt;\"\n\nharbor run \\\n  -d terminal-bench/terminal-bench-2 \\\n  --agent codex \\\n  --model vercel_ai_gateway/openai/gpt-5.6-sol \\\n  --env vercel \\\n  --n-concurrent 4\n</code></pre>\n<p>Do not commit those values or paste them into a benchmark task. Use placeholders in documentation and load real secrets from the execution environment.</p>\n<p>Start with low concurrency. Four parallel trials are easier to debug than forty. Once one task completes reliably and the outputs land where expected, increase concurrency within API, compute, and budget limits.</p>\n<h2>Understand the Files Harbor Produces</h2>\n<p>Harbor stores a job result and a directory for each trial. The exact layout evolves with the project, but the <a href=\"https://github.com/harbor-framework/harbor/blob/main/docs/content/docs/run-jobs/run-evals.mdx\">evaluation documentation</a> describes job-level configuration and results alongside trial-level records.</p>\n<p>Keep the complete artifact alongside the aggregate reward. A failed trial should retain enough evidence to answer:</p>\n<ul>\n<li>Did the environment start correctly?</li>\n<li>Did the agent understand the instruction?</li>\n<li>Which commands and tools did it use?</li>\n<li>Did it modify the intended files?</li>\n<li>Did the verifier run successfully?</li>\n<li>Did a timeout, provider error, or network rule cause the failure?</li>\n</ul>\n<p>A score without the trace is hard to improve. It also makes infrastructure failures indistinguishable from agent failures.</p>\n<h2>Compare Models Without Changing Everything Else</h2>\n<p>Vercel AI Gateway lets Harbor switch models through one endpoint. That is convenient for controlled comparisons, provided the rest of the job remains fixed.</p>\n<pre><code class=\"language-bash\">harbor run \\\n  -d terminal-bench/terminal-bench-2 \\\n  --agent codex \\\n  --model vercel_ai_gateway/openai/gpt-5.6-sol \\\n  --env vercel \\\n  --n-concurrent 8\n\nharbor run \\\n  -d terminal-bench/terminal-bench-2 \\\n  --agent codex \\\n  --model vercel_ai_gateway/anthropic/claude-fable-5 \\\n  --env vercel \\\n  --n-concurrent 8\n</code></pre>\n<p>The comparison now changes the model while keeping the agent harness and dataset constant. That is narrower than comparing a vendor's complete coding product against another vendor's complete product. Both experiments can be useful, but they answer different questions.</p>\n<p>Record provider and model identifiers exactly. A friendly alias that moves to a newer snapshot can silently invalidate a month-over-month trend.</p>\n<h2>Repeat Trials and Report Uncertainty</h2>\n<p>Agent runs are stochastic. One attempt per task can reward a lucky plan or punish one bad tool call. Run several attempts for important comparisons and report the distribution.</p>\n<p>For binary tasks, useful summaries include pass rate, pass@1, repeated success rate, and confidence intervals. For graded tasks, report the mean and spread, then inspect the tasks near decision boundaries.</p>\n<p>Do not rerun only the failures and combine those results with the original successes. That creates a retry policy, which is a legitimate product behavior, but it is no longer a single-attempt benchmark. Label it honestly.</p>\n<p>A practical report might say:</p>\n<pre><code class=\"language-text\">Dataset: internal-migrations@1.3\nTasks: 24\nAttempts per task: 5\nAgent: codex 0.18.2\nModel: provider/model-snapshot\nEnvironment: vercel-sandbox image abc123\nTimeout: 30 minutes\nSuccess: 91/120 trials\nTask pass@1: 16/24\n</code></pre>\n<p>That is far more useful than \"76% on our benchmark.\"</p>\n<h2>Control Network Access During Trials</h2>\n<p>Coding agents often need package registries, source hosts, or model APIs. An unrestricted sandbox can also reach endpoints unrelated to the task.</p>\n<p>Vercel Sandbox supports network policies that are enforced outside the guest. Its <a href=\"https://vercel.com/kb/guide/running-opencode-securely-with-the-vercel-sandbox\">security guidance</a> recommends restricting egress to required domains and updating the policy before untrusted work runs.</p>\n<p>A safe lifecycle is:</p>\n<ol>\n<li>Start with access needed to install pinned dependencies.</li>\n<li>Prepare the workspace.</li>\n<li>Switch to a narrow allowlist or deny-all policy.</li>\n<li>Run the agent.</li>\n<li>Export results.</li>\n<li>Destroy the sandbox.</li>\n</ol>\n<p>Network rules are part of the benchmark configuration. If one model can browse documentation and another cannot, the score compares tool access as well as model behavior.</p>\n<h2>Keep Credentials Outside the Guest</h2>\n<p>Putting a valuable API key in the sandbox environment lets any process inside the guest read it. That includes the agent, downloaded packages, test scripts, and a malicious task fixture.</p>\n<p>Vercel supports credential brokering at the network firewall. The sandbox makes an outbound request, and the firewall injects a credential only when the request matches the configured destination. The secret does not enter the sandbox filesystem or environment.</p>\n<p>Use a separate, narrowly scoped credential for evaluation even with brokering. Restrict it by provider, project, budget, and lifetime. Assume task content is untrusted, especially when importing a community dataset.</p>\n<p>The safer question is not \"Can the agent print the key?\" It is \"What can any process in this trial cause the credential to do?\"</p>\n<h2>Build a Small Internal Dataset</h2>\n<p>Public benchmarks help with external comparison. Deployment decisions need tasks drawn from your own failure modes.</p>\n<p>A Harbor task uses a <code>task.toml</code>, <code>instruction.md</code>, environment definition, test script, and optional reference solution. The project's <a href=\"https://github.com/harbor-framework/harbor/blob/main/docs/content/docs/datasets/adapters.mdx\">adapter documentation</a> describes the format and expects the verifier to write a numeric reward under <code>/logs/verifier/reward.txt</code>.</p>\n<pre><code class=\"language-toml\">version = \"1.0\"\n\n[task]\nname = \"nandann/fix-canonical-regression\"\n\n[metadata]\ndifficulty = \"medium\"\ncategory = \"web-development\"\ntags = [\"nextjs\", \"seo\", \"testing\"]\n\n[agent]\ntimeout_sec = 1800.0\n\n[verifier]\ntimeout_sec = 120.0\n\n[environment]\ncpus = 2\nmemory_mb = 4096\nstorage_mb = 10240\n</code></pre>\n<p>The instruction should state the goal, constraints, expected output, and files in scope. It should not expose the hidden test or reference solution. The verifier should test outcomes rather than matching one expected patch.</p>\n<p>Good internal tasks come from real incidents and review comments after removing credentials, personal data, and proprietary material that the evaluation does not need. Include straightforward tasks, ambiguous tasks that should trigger clarification, and unsafe requests the agent should refuse or escalate.</p>\n<h2>Design Verifiers That Reward the Outcome</h2>\n<p>A verifier defines success more strongly than the prose description does. If the task asks an agent to fix a security bug but the verifier checks only that one file changed, the benchmark rewards activity instead of correctness.</p>\n<p>Layer checks from cheap to expensive. Start with file and syntax validation, then run focused tests, static analysis, and behavior checks. Use an integration test when the task is about interaction between components. Avoid an LLM judge when a deterministic test can answer the question.</p>\n<p>For a migration task, the verifier might check that the old API is absent, compile the project, run targeted tests, and exercise one representative request. It should also detect forbidden shortcuts, such as deleting the failing test or replacing a function with a hard-coded fixture.</p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\nset -euo pipefail\n\nscore=0\n\nif ! rg -n \"legacyClient\" /workspace/src; then\n  score=$(awk 'BEGIN { print 0.2 }')\nfi\n\nif npm --prefix /workspace run typecheck; then\n  score=$(awk -v s=\"$score\" 'BEGIN { print s + 0.3 }')\nfi\n\nif npm --prefix /workspace test -- --runInBand migration.spec.ts; then\n  score=$(awk -v s=\"$score\" 'BEGIN { print s + 0.5 }')\nfi\n\nprintf '%s\\n' \"$score\" &gt; /logs/verifier/reward.txt\n</code></pre>\n<p>This sketch shows a graded reward, not a universal verifier. In a security-sensitive task, any critical failure may need to force the score to zero. Document the scoring rule and test it against the reference solution, partial solutions, and deliberately deceptive patches.</p>\n<p>Keep the verifier outside the agent's writable workspace where the environment supports that boundary. Do not include hidden test answers in <code>instruction.md</code> or readable setup files.</p>\n<h2>Separate Agent Quality From Harness Quality</h2>\n<p>The agent is more than the model. It includes its system instruction, tool set, context policy, retry behavior, shell integration, patch strategy, and verification loop.</p>\n<p>Run three types of comparison:</p>\n<ol>\n<li>Same harness, different models. This isolates the model choice as much as practical.</li>\n<li>Same model, different harness versions. This measures orchestration and tool changes.</li>\n<li>Complete product against complete product. This answers which packaged system performs better for the selected tasks.</li>\n</ol>\n<p>Do not combine those results into one table without labeling the comparison type. A model may look weak because one harness truncates command output or gives it a shorter timeout. Another may look strong because its harness automatically retries failed tests.</p>\n<p>Version the agent package and record its default prompts. If a hosted product changes those defaults without a visible version, record the evaluation date and any available build identifier.</p>\n<h2>Control Cost Before Increasing Concurrency</h2>\n<p>Parallel microVMs remove a local bottleneck, which makes spending easier to accelerate. Estimate the upper bound before starting a job.</p>\n<pre><code class=\"language-text\">maximum trials = tasks × models × agents × attempts\nmaximum model cost = maximum trials × per-trial token budget\nmaximum compute time = maximum trials × task timeout\n</code></pre>\n<p>A dataset with 100 tasks, three models, two agents, and five attempts produces 3,000 trials. A 30-minute timeout creates a theoretical 1,500 compute-hour ceiling before setup and verifier time.</p>\n<p>Set job-level limits and provider budgets. Start with a representative subset that includes easy, medium, difficult, and historically flaky tasks. Abort the run if environment startup failures or provider errors exceed a threshold. Paying for another hundred trials will not repair a broken image.</p>\n<p>Use concurrency as an experimental parameter. High concurrency can trigger provider rate limits, registry throttling, or shared downstream constraints that do not appear in smaller runs. If results change with concurrency, investigate the infrastructure before attributing the difference to the model.</p>\n<h2>Make Runs Reproducible Months Later</h2>\n<p>Save enough configuration to rerun the job after the default images and model aliases have changed.</p>\n<p>At minimum, preserve:</p>\n<ul>\n<li>Harbor and agent versions</li>\n<li>Dataset name, version, and content digest</li>\n<li>Environment image digest</li>\n<li>Model provider and immutable model identifier where available</li>\n<li>Generation parameters and reasoning setting</li>\n<li>Tool configuration and network policy</li>\n<li>Per-task timeout and resource limits</li>\n<li>Attempt count, random seed where supported, and concurrency</li>\n<li>Verifier version and result artifacts</li>\n</ul>\n<p>Archive dependency lockfiles with private datasets. A floating <code>apt-get install</code> or unpinned npm dependency can turn a stable benchmark into a historical reenactment.</p>\n<p>Snapshotting can reduce setup time, but label the snapshot and describe how it was created. A warm snapshot and a clean build are different environments. Periodically rebuild from the declared source to ensure the snapshot has not become the only surviving specification.</p>\n<h2>Add Evaluation Gates Without Worshipping the Score</h2>\n<p>An evaluation can run on a schedule, before a model change, after a harness upgrade, or before expanding an agent's permissions. Do not require the aggregate score to increase on every change. Sampling noise and task composition make that brittle.</p>\n<p>Use several gates:</p>\n<ul>\n<li>No regression on critical safety tasks</li>\n<li>Aggregate success remains within an agreed tolerance</li>\n<li>Cost and latency stay within task-specific budgets</li>\n<li>Infrastructure failure rate remains below a threshold</li>\n<li>No new destructive or policy-violating behavior</li>\n</ul>\n<p>Keep a small \"must pass\" suite for actions with real consequences. A deployment agent that succeeds on 90% of refactors but occasionally promotes the wrong artifact is not ready for that authority.</p>\n<p>When a gate fails, inspect the changed tasks and traces. A lower aggregate score caused by two new difficult tasks is different from an existing task failing after a tool update.</p>\n<h2>Avoid Common Benchmark Traps</h2>\n<p>Contamination is the obvious concern. If tasks and solutions are public, a model may have seen related material. Fresh internal tasks reduce that risk, although they make cross-company comparison harder.</p>\n<p>Verifier quality is equally important. A test that accepts one hard-coded string can reward a brittle patch. A test suite that depends on an unreliable external service can fail correct work. Run the reference solution and deliberately broken solutions against every verifier.</p>\n<p>Watch for infrastructure bias. Different models may use different command patterns, which can interact with rate limits, package caches, or shell defaults. Track setup failures separately from task failures.</p>\n<p>Finally, avoid choosing tasks after seeing which model wins. Freeze the dataset and evaluation plan first. Otherwise ordinary benchmark iteration turns into an expensive way to confirm a preferred answer.</p>\n<h2>Turn Results Into an Engineering Decision</h2>\n<p>Rankings are less useful than failure clusters. Group unsuccessful trials by cause: misunderstood requirement, incorrect edit, missing verification, destructive command, timeout, dependency failure, or unsafe behavior.</p>\n<p>Then connect the cluster to a product change. A model that solves tasks but skips tests may need a stronger harness contract. Frequent dependency failures may justify a prepared snapshot. Unsafe commands may require tool restrictions and approval gates rather than another prompt paragraph.</p>\n<p>Evaluate cost and latency beside quality. A five-point success improvement may be worthwhile for release remediation and wasteful for routine formatting. Route tasks based on consequences instead of declaring one model the permanent winner.</p>\n<p>Publish an internal evaluation card with the dataset scope, known gaps, environment, date, and intended decision. State what the benchmark does not measure. This prevents a result collected for terminal maintenance tasks from being reused later as evidence for customer-support quality or autonomous production access.</p>\n<p>Revisit the dataset after incidents and difficult human reviews. Add a sanitized task when it represents a failure mode the current suite missed. Retire tasks that no longer match the stack, but keep versioned results so apparent improvement is not caused by quietly removing the difficult work.</p>\n<p>The strongest evaluation program ends with a small number of repeatable decisions: which agent handles which task class, what authority it receives, when a human reviews the result, and which evidence must accompany a claimed success.</p>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
