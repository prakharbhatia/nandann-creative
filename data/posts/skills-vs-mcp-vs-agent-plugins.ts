import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "skills-vs-mcp-vs-agent-plugins",
  title: "Skills vs MCP vs Agent Plugins for Coding Agents",
  description: "Choose between agent skills, MCP servers, and Agent Plugins for coding workflows. Compare context cost, live access, portability, permissions, and maintenance.",
  date: "2026-09-25",
  readTime: "24 min",
  category: "Engineering",
  tags: ["Agent Skills","Model Context Protocol","Agent Plugins","Coding Agents","Developer Tools","AI Engineering"],
  faqs: [
      {
          "question": "What is the difference between an agent skill and an MCP server?",
          "answer": "A skill packages instructions, scripts, templates, and references for a repeatable workflow. An MCP server exposes live tools, resources, and prompts over a protocol. Skills teach the agent how to work; MCP gives it a typed connection to external capabilities and data."
      },
      {
          "question": "What does an Agent Plugin contain?",
          "answer": "Agent Plugins 1.0 can package portable skills and MCP server configuration under one manifest. Clients may also support namespaced, client-specific components such as agents, hooks, commands, or automation templates."
      },
      {
          "question": "Can a skill call an MCP tool?",
          "answer": "Yes. A skill can instruct the agent when and how to use tools supplied by an MCP server. The skill should reference stable capabilities and avoid depending on undocumented client-specific tool names when portability matters."
      },
      {
          "question": "Should every API integration use MCP?",
          "answer": "No. Use MCP when several agents need a discoverable, governed tool or data interface. A small deterministic script or existing CLI may be simpler for a repository-specific task that does not need a persistent external service."
      },
      {
          "question": "Are agent plugins portable across all coding agents?",
          "answer": "The standard makes skills and MCP configuration portable component types, but clients may support different extensions and behaviors. Test the portable core in each target client and treat client-specific namespaces as optional layers."
      },
      {
          "question": "How do teams reduce coding-agent context bloat?",
          "answer": "Keep always-on instructions short, use skill descriptions for discovery and load detailed references only when needed, limit enabled MCP tools, and package related capabilities without activating every component for every task."
      },
      {
          "question": "What should teams review before installing an agent plugin?",
          "answer": "Review the publisher, plugin manifest, skill instructions, scripts, hooks, MCP server commands, requested environment variables, filesystem access, network destinations, update policy, and any client-specific extension directories."
      }
  ],
  coverImage: "/images/skills-vs-mcp-vs-agent-plugins.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/skills-vs-mcp-vs-agent-plugins.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/skills-vs-mcp-vs-agent-plugins.webp\" alt=\"Three engineers organize instruction cards, connector cables, and a modular plugin kit at a worktable\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>Coding agents can now be extended through skills, Model Context Protocol servers, and installable plugins. These terms are often treated as three versions of the same feature. They solve different problems.</p>\n<p>A skill is a task-specific package of instructions and supporting files. MCP is a protocol for discovering and calling live tools, resources, and prompts. An Agent Plugin is a distribution unit that can bundle skills and MCP configuration, with room for client-specific additions.</p>\n<p>The choice affects context usage, permissions, portability, and maintenance. Putting a style guide behind an MCP server adds operational weight. Putting a production database query into a Markdown skill gives instructions without a governed execution path. Packaging unrelated capabilities in one plugin makes installation easy and review difficult.</p>\n<p>This guide gives each layer a clear job, then shows how to combine them without turning the agent into an unreviewable collection of prompts and tools.</p>\n<h2>The Short Decision Rule</h2>\n<p>Start by asking what the agent is missing.</p>\n<h3>Use a skill for repeatable know-how</h3>\n<p>Choose a skill when the missing capability is a workflow, standard, or body of domain guidance. Examples include preparing a release, testing a login flow, applying a repository's migration policy, or producing a document in a house format.</p>\n<p>The skill can include a <code>SKILL.md</code>, scripts, templates, examples, and references. Compatible agents discover it from its name and description, then load the detailed material when the task matches.</p>\n<h3>Use MCP for live systems</h3>\n<p>Choose MCP when the agent must discover or invoke an external capability at runtime. Examples include reading an issue tracker, querying a database, searching current cloud documentation, opening a browser, or changing an infrastructure resource.</p>\n<p>MCP servers expose typed tools and data through a client-server protocol. Authentication, transport, availability, and authorization become part of the design.</p>\n<h3>Use a plugin for distribution</h3>\n<p>Choose an Agent Plugin when several related components should be installed, versioned, and removed as one product. A cloud-development plugin may include onboarding skills plus an MCP server for current documentation. A testing plugin may pair a test workflow with a reporting service.</p>\n<p>A plugin does not replace the components inside it. It packages them.</p>\n<table>\n<thead>\n<tr><th>Need</th><th>Primary choice</th><th>Reason</th></tr>\n</thead>\n<tbody>\n<tr><td>Teach a repeatable repository workflow</td><td>Skill</td><td>Instructions and local resources load when relevant</td></tr>\n<tr><td>Read or change a live external system</td><td>MCP server</td><td>Typed, discoverable runtime interface</td></tr>\n<tr><td>Ship several related capabilities together</td><td>Agent Plugin</td><td>One manifest, version, and installation unit</td></tr>\n<tr><td>Enforce a deterministic lifecycle action</td><td>Client hook or CI</td><td>The action should not depend on model choice</td></tr>\n<tr><td>Apply a short rule to every task</td><td>Repository instructions</td><td>Always-on context is appropriate when it is truly universal</td></tr>\n</tbody>\n</table>\n<h2>What an Agent Skill Is</h2>\n<p>The Agent Skills specification defines a directory centered on <code>SKILL.md</code>. The file begins with YAML frontmatter that includes a name and description, followed by instructions the agent should follow.</p>\n<h3>Discovery stays small</h3>\n<p>The name and description tell the client when a skill may apply. The full body is loaded only after discovery, and linked resources can be read later as the workflow needs them.</p>\n<p>This progressive pattern matters. A team can install many skills without placing every page of guidance into every prompt. Google describes the same motivation in its skills repository: condensed expertise can reduce the context bloat caused by loading broad documentation or tool definitions before they are needed.</p>\n<h3>A skill can contain executable support</h3>\n<p>A useful skill often includes more than prose.</p>\n<pre><code class=\"language-text\">release-validation/\n  SKILL.md\n  scripts/\n    check-version.sh\n    verify-changelog.ts\n  references/\n    release-policy.md\n  templates/\n    release-notes.md\n</code></pre>\n<p>The instructions can tell the agent which script to run, how to interpret its output, and what to report. The script remains deterministic code; the skill provides the judgment about when it applies and how the steps fit together.</p>\n<h3>A skill is not a remote service</h3>\n<p>A skill does not create a standard network connection, credential exchange, or runtime tool discovery mechanism. It may tell the agent to use an existing CLI or MCP tool, but the skill itself is packaged know-how.</p>\n<p>This makes skills easy to keep beside a codebase. It also means maintainers must avoid hiding unsafe shell behavior behind friendly Markdown instructions.</p>\n<h2>What MCP Provides</h2>\n<p>Model Context Protocol defines how an AI application connects to context and actions supplied by servers.</p>\n<h3>The host, client, and server have separate roles</h3>\n<p>The MCP host is the AI application. It creates a client for each configured server. The server exposes capabilities and handles requests.</p>\n<p>Servers can run locally over standard input and output or remotely over Streamable HTTP. The transport affects authentication and operations, while the data layer defines the exchanged methods and structures.</p>\n<h3>Tools, resources, and prompts are distinct primitives</h3>\n<p>The current MCP architecture defines three core server primitives:</p>\n<ul>\n<li>Tools are executable functions for actions such as API calls, file operations, or database queries.</li>\n<li>Resources provide contextual data such as schemas, records, documents, or API responses.</li>\n<li>Prompts are reusable interaction templates.</li>\n</ul>\n<p>Clients discover what a server provides, then retrieve or call the appropriate primitive. Tool inputs use JSON Schema, giving the host a machine-readable contract before execution.</p>\n<h3>MCP introduces an operational boundary</h3>\n<p>A remote server needs authentication, authorization, rate limits, logging, version compatibility, incident handling, and availability targets. A local server may start a process with filesystem and environment access.</p>\n<p>Those costs are justified when several agents need the same live interface or when a system owner wants one governed integration. They are unnecessary for every small workflow.</p>\n<h2>What Agent Plugins 1.0 Packages</h2>\n<p>The Agent Plugins specification version 1.0.0 defines a portable package layout with a root manifest.</p>\n<h3>Portable components have standard locations</h3>\n<p>A conforming package uses <code>plugin.json</code> for metadata. Skills live in <code>skills/</code>, and MCP server configuration lives in <code>mcp.json</code>.</p>\n<pre><code class=\"language-text\">cloud-development/\n  plugin.json\n  skills/\n    project-onboarding/\n      SKILL.md\n    architecture-review/\n      SKILL.md\n  mcp.json\n</code></pre>\n<p>The manifest declares the specification schema, package identity, version, and other metadata. The client discovers skills and MCP configuration from their defined paths.</p>\n<h3>Client-specific components use namespaces</h3>\n<p>Some clients support custom agents, hooks, commands, rules, or automation templates. Agent Plugins allows client-owned data under reverse-domain namespaces.</p>\n<p>VS Code, for example, documents Copilot-specific components under <code>com.github.copilot/</code>. Other clients can ignore that directory and still load the portable skills and MCP definitions.</p>\n<pre><code class=\"language-text\">cloud-development/\n  plugin.json\n  skills/\n  mcp.json\n  com.github.copilot/\n    agents/\n    hooks/\n      hooks.json\n</code></pre>\n<p>This is graceful portability, not identical behavior everywhere. The core travels; optional features depend on the client.</p>\n<h3>Packaging creates a trust decision</h3>\n<p>One installation may add Markdown instructions, executable scripts, hooks, and processes that connect to remote services. Users need to review the whole package, including files absent from its marketplace description.</p>\n<p>The specification requires filesystem-resolved paths supplied by a plugin to remain inside the plugin root. That guards package traversal, but it does not prove that included scripts or remote servers are safe.</p>\n<h2>Compare Context and Tool Costs</h2>\n<p>Agent quality can decline when every capability is always present.</p>\n<h3>Instructions consume attention</h3>\n<p>Always-on instructions compete with the task, conversation, code, and tool output. Long universal rule files encourage contradictions and make it hard to know which sentence changed the result.</p>\n<p>Skills improve the situation through discovery and progressive loading. Keep descriptions precise enough that the agent loads the skill for the right work and ignores it elsewhere.</p>\n<h3>Tool definitions also consume context</h3>\n<p>An MCP client commonly discovers tools and supplies their names, descriptions, and schemas to the model. Connecting several broad servers can produce hundreds of tools before the task begins.</p>\n<p>The current MCP documentation describes progressive tool discovery for clients that federate many servers. Even without that feature, teams can disable irrelevant servers, expose smaller tool groups, or put a gateway in front of approved capabilities.</p>\n<h3>Plugins should not mean activate everything</h3>\n<p>A plugin is a convenient catalog and dependency unit. Installation should not automatically make every skill body and every MCP tool part of each prompt.</p>\n<p>Good clients retain component activation rules. Good plugin authors keep the bundle cohesive so the user can predict what becomes available.</p>\n<h2>Decide with a Capability Matrix</h2>\n<p>The same requirement can look different depending on freshness, risk, and reuse.</p>\n<table>\n<thead>\n<tr><th>Requirement</th><th>Skill</th><th>MCP</th><th>Plugin</th></tr>\n</thead>\n<tbody>\n<tr><td>Repository coding conventions</td><td>Strong fit</td><td>Poor fit</td><td>Package if distributed with related workflows</td></tr>\n<tr><td>Multi-step release procedure</td><td>Strong fit</td><td>Add only for live release systems</td><td>Strong if shipped with tools and templates</td></tr>\n<tr><td>Current issue-tracker data</td><td>Can explain process</td><td>Strong fit</td><td>Package client config with issue workflow</td></tr>\n<tr><td>Production database action</td><td>Can define safeguards</td><td>Strong fit with narrow tools</td><td>Package only after serious review</td></tr>\n<tr><td>Static framework examples</td><td>Strong fit</td><td>Usually unnecessary</td><td>Useful for a maintained vendor kit</td></tr>\n<tr><td>Current vendor documentation</td><td>Skill can route retrieval</td><td>Strong resource/search fit</td><td>Strong combination</td></tr>\n<tr><td>Post-edit formatting command</td><td>Script in skill or hook</td><td>Usually unnecessary</td><td>Client extension may package hook</td></tr>\n<tr><td>Cross-client installation</td><td>Skill spec helps</td><td>MCP config can travel</td><td>Primary purpose of the plugin format</td></tr>\n</tbody>\n</table>\n<h3>Ask whether the information changes</h3>\n<p>Stable procedures and examples fit local resources. Data that changes by the minute belongs behind a live tool or resource.</p>\n<p>Current documentation sits in the middle. A skill can teach the agent how to search and apply it, while an MCP documentation server supplies the latest source material.</p>\n<h3>Ask whether the agent must act</h3>\n<p>Reading a policy needs context. Creating a cloud project needs a tool with identity, permissions, and an audit trail.</p>\n<p>Do not turn an instruction into authority. A sentence saying the agent may deploy does not replace credentials and server-side authorization.</p>\n<h3>Ask how many environments need it</h3>\n<p>A one-repository script may be easiest to maintain locally. A capability used across editors, coding agents, and internal platforms benefits from a protocol or portable package.</p>\n<p>Portability carries testing work. Every target client may interpret activation, approvals, environment variables, and extensions differently.</p>\n<h2>A Layered Architecture That Holds Up</h2>\n<p>Most mature setups use all three layers with narrow responsibilities.</p>\n<h3>Repository instructions define the baseline</h3>\n<p>Keep always-on guidance short: how to build and test, where architecture decisions live, what directories are sensitive, and which actions require approval.</p>\n<p>If a rule applies only to deployments or database migrations, move it into the relevant skill. Universal context should be universal.</p>\n<h3>Skills orchestrate workflows</h3>\n<p>A deployment skill can inspect the repository, select the correct runbook, invoke tests, request approval, call deployment tools, and verify the result. It explains the sequence and stopping conditions.</p>\n<p>The skill should refer to capabilities by purpose where possible. Hard-coding one client's display name for a tool makes reuse brittle.</p>\n<h3>MCP connects live systems</h3>\n<p>Expose narrow operations such as <code>deployment_get</code>, <code>deployment_create_preview</code>, and <code>deployment_rollback</code>. Give each operation a clear schema and enforce authorization on the server.</p>\n<p>Avoid one universal <code>run_shell</code> or <code>call_api</code> tool for a sensitive platform. Broad tools push security decisions into prompts and make audits harder.</p>\n<h3>Plugins distribute the tested combination</h3>\n<p>Once the deployment skill and MCP server work together, package them with version constraints, setup documentation, and optional client integrations.</p>\n<p>The plugin becomes the unit teams install. The skill remains the workflow, and the MCP server remains the live interface.</p>\n<h2>The Google Cloud Plugin as a Concrete Example</h2>\n<p>Google's September 2026 developer plugin demonstrates why the layers are complementary.</p>\n<h3>Skills provide platform procedures</h3>\n<p>The bundle includes guidance for onboarding, authentication, project configuration, architecture, and guarded use of <code>gcloud</code>. These tasks benefit from vendor-maintained instructions and examples.</p>\n<p>Google's skills work also emphasizes progressive loading so every piece of cloud guidance does not occupy the context window at once.</p>\n<h3>MCP supplies current documentation</h3>\n<p>The plugin includes configuration for Google's Developer Knowledge MCP server. The server gives agents a live source of official developer documentation instead of freezing all product details inside the plugin release.</p>\n<p>This separates procedure from freshness. A skill can say how to evaluate an IAM choice, while the documentation service supplies current product facts.</p>\n<h3>The plugin makes the pair installable</h3>\n<p>Users install one named package rather than copying several skill folders and hand-editing an MCP configuration. The Agent Plugins layout aims to keep that package usable across multiple coding-agent clients.</p>\n<p>The example also shows the remaining friction: clients still have different installation commands and authentication setup. A shared package format reduces duplication without erasing client behavior.</p>\n<h2>Avoid the Tool Coupling Trap</h2>\n<p>Coupling appears when a workflow assumes one exact server, tool name, or client implementation.</p>\n<h3>Depend on a capability contract</h3>\n<p>Document what the workflow needs: search official cloud documentation, inspect the active project, or create a preview environment. Map that need to available tools during setup.</p>\n<p>If the skill directly embeds a long list of vendor-specific tool identifiers, a server update can break the workflow even when equivalent capabilities still exist.</p>\n<h3>Keep vendor facts close to the vendor source</h3>\n<p>Do not copy changing quotas, model lists, regions, or pricing into a skill unless the release process updates and tests them. Point the workflow at official documentation through a resource or search tool.</p>\n<p>Keep stable safety rules local. A rule requiring explicit confirmation before deleting a project should not disappear because a documentation service is unavailable.</p>\n<h3>Version the bundle and components</h3>\n<p>Record which skill release was tested with which MCP server behavior. Use semantic versions for plugin changes and describe breaking tool or configuration changes.</p>\n<p>Remote servers may evolve independently from installed files. Capability discovery helps, but end-to-end contract tests are still needed.</p>\n<h2>Security Review by Layer</h2>\n<p>Each layer introduces a different kind of risk.</p>\n<h3>Review skills as code and policy</h3>\n<p>Read the instructions for hidden behavior, unsafe approvals, broad destructive commands, and requests to expose secrets. Inspect every referenced script and template.</p>\n<p>Skills can change agent judgment even when they never execute a program. A malicious instruction can redirect tool use, weaken review, or encourage data disclosure.</p>\n<h3>Review MCP as an application integration</h3>\n<p>Check the server publisher, source, transport, authentication, scopes, data handling, and tool descriptions. For local servers, inspect the command, package source, working directory, and environment variables.</p>\n<p>For remote servers, require TLS, narrow tokens, tenant-aware authorization, request logging, rate limits, and a revocation path. The model must not receive raw credentials.</p>\n<h3>Review plugins as a supply chain</h3>\n<p>A plugin can combine the previous risks and add client-specific hooks. Verify the manifest schema, repository, signed or pinned release where available, contents, update policy, and ownership.</p>\n<p>VS Code warns that plugin hooks and MCP servers may run code on the local machine. Treat installation like adding a developer tool, not like bookmarking documentation.</p>\n<h3>Separate installation from approval</h3>\n<p>Installing a capability should not grant every action automatically. Keep human confirmation or policy checks for high-impact changes such as deletion, payment, production deployment, and access modification.</p>\n<p>The plugin declares what exists. The client and service decide what may run.</p>\n<h2>Build a Skill Without Hiding Too Much</h2>\n<p>A skill should make a workflow clearer to maintainers as well as agents.</p>\n<h3>Write a precise description</h3>\n<p>The description drives discovery. State both the capability and the trigger.</p>\n<pre><code class=\"language-markdown\">---\nname: validate-database-migration\ndescription: Validate a proposed Postgres migration against a disposable branch. Use when a change adds or modifies SQL migration files.\n---\n</code></pre>\n<p>Avoid descriptions such as \"helps with databases.\" They load for unrelated tasks and provide little signal about the expected outcome.</p>\n<h3>Put deterministic checks in scripts</h3>\n<p>Let a script find changed migration files, run a linter, apply them to a disposable database, and report machine-readable results. Let the skill decide when to run the script and how to respond to failure.</p>\n<p>This division reduces prompt length and prevents the model from reimplementing parsing logic differently on every run.</p>\n<h3>Route to live tools explicitly</h3>\n<p>Name the required capability and explain its permission boundary.</p>\n<pre><code class=\"language-markdown\">Use the approved database-branch tool to create an isolated child from the sanitized preview parent. Never use a production connection string. Ask for confirmation before deleting a branch you did not create in this run.\n</code></pre>\n<p>The MCP server must still enforce the rule. Instructions improve agent behavior; authorization provides the guarantee.</p>\n<h2>Design an MCP Server the Skill Can Use</h2>\n<p>The server interface determines how much judgment the model must supply.</p>\n<h3>Prefer task-shaped tools</h3>\n<p><code>create_preview_branch</code> is safer and easier to describe than <code>execute_database_admin_command</code>. Its schema can require a repository and pull-request number while the server selects the approved parent and naming policy.</p>\n<p>Task-shaped tools also produce better audit events because the operation has business meaning.</p>\n<h3>Return structured results</h3>\n<p>Return stable identifiers, state, safe URLs, and explicit error categories. Do not make the skill parse a paragraph to find a branch ID.</p>\n<pre><code class=\"language-json\">{\n  \"branchId\": \"br_preview_412\",\n  \"state\": \"ready\",\n  \"expiresAt\": \"2026-09-27T12:00:00Z\",\n  \"consoleUrl\": \"https://console.example/branches/br_preview_412\"\n}\n</code></pre>\n<p>Keep secrets out of tool output. The server or runtime should attach credentials to downstream requests without exposing them to the model.</p>\n<h3>Limit discovery noise</h3>\n<p>Split very large servers by domain or support filtered discovery. Tool names and descriptions should explain when to use the operation and when not to use it.</p>\n<p>Remove deprecated tools after a migration period. A growing list of aliases gives the model more chances to choose the wrong one.</p>\n<h2>Package and Test a Plugin</h2>\n<p>Distribution adds another quality gate.</p>\n<h3>Keep the bundle cohesive</h3>\n<p>A plugin named <code>postgres-release</code> may reasonably include migration validation, schema review, branch management, and database documentation. Adding social posting and browser design tools to the same package makes permissions and activation hard to reason about.</p>\n<p>Separate plugins can depend on a shared foundational plugin when clients support that model, or document the required companion capability.</p>\n<h3>Test the portable core first</h3>\n<p>Validate <code>plugin.json</code> against the Agent Plugins schema. Confirm that skills are discoverable, referenced files stay inside the package, and <code>mcp.json</code> works in every target client.</p>\n<p>Then test namespaced extensions separately. A plugin can remain useful in a client that ignores its custom hook or agent definition.</p>\n<h3>Test behavior, not installation alone</h3>\n<p>Run representative prompts and verify which skill activates, which tools become visible, what approvals appear, and what files or network destinations are accessed.</p>\n<p>Include negative tests. The agent should not load a deployment skill for a documentation edit, call production tools during preview validation, or continue after a denied approval.</p>\n<h2>Governance for Teams</h2>\n<p>Individual experimentation becomes an organizational dependency quickly.</p>\n<h3>Maintain an approved catalog</h3>\n<p>Record publisher, repository, owner, purpose, version, review date, requested permissions, MCP endpoints, and supported clients. Give teams a supported install path instead of asking every developer to evaluate marketplace packages independently.</p>\n<p>An approved plugin may still contain optional components. Document which ones the organization enables and why.</p>\n<h3>Pin and update deliberately</h3>\n<p>Automatic updates reduce maintenance but can change instructions and executable behavior between two agent runs. Pin production-sensitive plugins or use a staged channel that tests updates before broad rollout.</p>\n<p>Track remote MCP server changes too. An installed plugin version may point to a service whose tool catalog changes without a local package update.</p>\n<h3>Observe outcomes</h3>\n<p>Measure skill activation, tool calls, denials, failures, latency, and repeated human corrections. High activation with low success may mean the description is too broad or the workflow is stale.</p>\n<p>Keep prompts and tool payloads under the same privacy rules as source code and production logs. Observability should not become a second copy of secrets or customer data.</p>\n<h2>A Practical Adoption Sequence</h2>\n<p>Teams do not need to design a marketplace on day one.</p>\n<ol>\n<li>Put concise repository-wide rules in the agent instruction file.</li>\n<li>Identify one repeated workflow with a clear start and finish.</li>\n<li>Package it as a skill with a precise description and deterministic scripts.</li>\n<li>Test activation on tasks that should and should not use it.</li>\n<li>Add MCP only when the workflow needs current external data or governed actions.</li>\n<li>Narrow the MCP tools and enforce permissions on the server.</li>\n<li>Run end-to-end tests for success, denial, timeout, and malformed output.</li>\n<li>Package related, stable components as a plugin when more than one team or client needs them.</li>\n<li>Validate the portable core in each supported client.</li>\n<li>Place client-specific additions in namespaced extensions and make them optional.</li>\n<li>Publish through an approved catalog with owners and version policy.</li>\n<li>Review usage and remove components that add more context or risk than value.</li>\n</ol>\n<p>For implementation details around MCP security, see <a href=\"/blog/implementing-model-context-protocol-for-secure-agent\">Implementing Model Context Protocol for Secure Agents</a>. <a href=\"/blog/secure-tanstack-ai-mcp-oauth-with-vercel-connect\">Secure TanStack AI MCP OAuth</a> covers per-user authorization, and <a href=\"/blog/ai-coding-assistants-cant-read-code\">AI Coding Assistants Can't Read Your Code</a> explains why explicit project context still matters.</p>\n<p>Skills, MCP servers, and plugins work best as layers. Keep knowledge in a skill, live authority behind MCP, and distribution in a plugin. When one layer starts doing all three jobs, context grows, permissions blur, and maintenance gets harder.</p>\n<h2>Official Sources</h2>\n<ul>\n<li><a href=\"https://agentskills.io/specification\">Agent Skills specification</a></li>\n<li><a href=\"https://modelcontextprotocol.io/docs/learn/architecture\">Model Context Protocol architecture</a></li>\n<li><a href=\"https://modelcontextprotocol.io/specification/2026-07-28\">Model Context Protocol specification</a></li>\n<li><a href=\"https://agent-plugins.org/specification\">Agent Plugins 1.0 specification</a></li>\n<li><a href=\"https://cloud.google.com/blog/topics/developers-practitioners/introducing-the-google-cloud-developer-plugin-for-ai-coding-agents\">Google Cloud Developer Plugin for AI coding agents</a></li>\n<li><a href=\"https://cloud.google.com/blog/topics/developers-practitioners/level-up-your-agents-announcing-googles-official-skills-repository\">Google's official Agent Skills repository announcement</a></li>\n<li><a href=\"https://cloud.google.com/blog/topics/developers-practitioners/behind-the-scenes-how-we-build-test-and-scale-google-agent-skills\">How Google builds and tests Agent Skills</a></li>\n<li><a href=\"https://code.visualstudio.com/docs/copilot/concepts/customization\">VS Code agent customization concepts</a></li>\n<li><a href=\"https://code.visualstudio.com/docs/agent-customization/agent-skills\">VS Code Agent Skills</a></li>\n<li><a href=\"https://code.visualstudio.com/docs/agent-customization/agent-plugins\">VS Code Agent Plugins</a></li>\n<li><a href=\"https://code.visualstudio.com/docs/agent-customization/mcp-servers\">VS Code MCP servers</a></li>\n</ul>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
