import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "cloudflare-workers-ai-reject-if-busy",
  title: "Workers AI rejectIfBusy: Fail Fast or Wait?",
  description: "Learn how Workers AI rejectIfBusy changes overload behavior, how to handle error 3040, and when fail-fast inference is safer than queueing.",
  date: "2026-09-18",
  readTime: "15 min",
  category: "Engineering",
  tags: ["Cloudflare Workers AI","rejectIfBusy","Backpressure","AI Infrastructure","TypeScript","Reliability"],
  faqs: [
      {
          "question": "What does rejectIfBusy do in Cloudflare Workers AI?",
          "answer": "When rejectIfBusy is true, Workers AI rejects a synchronous inference request instead of leaving it in the capacity queue when inference capacity is unavailable. The rejection uses HTTP 429 with internal error code 3040."
      },
      {
          "question": "Where does rejectIfBusy go in a Workers binding call?",
          "answer": "For env.AI.run(), rejectIfBusy belongs in the third argument, not in the model input object. For the native REST and OpenAI-compatible APIs, it goes in an options object in the request body."
      },
      {
          "question": "Should every Workers AI request use rejectIfBusy?",
          "answer": "No. Use it when a queued answer will miss a latency deadline, when the caller has a fallback, or when accepting more work would amplify overload. Keep queueing for background work and user flows where waiting is preferable to failure."
      },
      {
          "question": "How should clients handle Workers AI error 3040?",
          "answer": "Classify it separately from quota exhaustion and invalid requests. Apply bounded retries with jitter only when enough deadline remains, use a fallback or degraded path when available, and return an honest retryable response instead of an endless loading state."
      },
      {
          "question": "Does rejectIfBusy reduce Workers AI cost?",
          "answer": "It can prevent unwanted inference from starting after a request is no longer useful, but it is primarily a latency and backpressure control. Teams should measure accepted requests, capacity rejections, fallbacks, and usage rather than assuming a fixed cost reduction."
      }
  ],
  coverImage: "/images/cloudflare-workers-ai-reject-if-busy.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/cloudflare-workers-ai-reject-if-busy.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/cloudflare-workers-ai-reject-if-busy.webp\" alt=\"Workers AI rejectIfBusy: Fail Fast or Wait?\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>Cloudflare Workers AI now lets an application decline to wait when inference capacity is busy. Set <code>rejectIfBusy: true</code>, and a synchronous request is rejected when capacity is unavailable instead of remaining in the capacity queue.</p>\n<p>That sounds like a small request option. Operationally, it decides who owns the waiting time. Without it, the platform can queue the request. With it, your application receives a capacity signal and can retry, use another model, fall back to a non-AI path, or tell the user to try again.</p>\n<p>The right choice depends on the deadline and the value of a late answer. An autocomplete suggestion that arrives after the user has typed the next sentence is waste. A background classification job may be perfectly useful thirty seconds later. Queueing treats those two requests alike unless the application supplies a policy.</p>\n<p>This guide shows how <code>rejectIfBusy</code> works, how to handle its error correctly, and how to turn the option into a measured overload strategy rather than another boolean added to every request.</p>\n<h2>The short answer</h2>\n<p>Use <code>rejectIfBusy</code> when a queued inference is likely to become useless before capacity returns, the caller has a fallback, or accepting more waiting work would make an overloaded system harder to recover.</p>\n<p>Leave it off when the request can wait, the user prefers a delayed result to no result, or your application has no safe way to handle a capacity rejection.</p>\n<p>Cloudflare's <a href=\"https://developers.cloudflare.com/workers-ai/features/reject-if-busy/\">official documentation</a> says a rejected request returns HTTP <code>429</code>, internal error code <code>3040</code>, and the message <code>Capacity temporarily exceeded, please try again.</code> This is a capacity signal. It is not the same as an invalid model request or the daily free-allocation limit.</p>\n<h2>What changes when you fail fast</h2>\n<p>Assume an interactive request has a four-second product deadline. The model service becomes busy at the moment the request arrives.</p>\n<p>If the request waits in a capacity queue, several outcomes are possible. Capacity may return quickly and the answer may still be useful. It may return at 3.8 seconds, leaving almost no time for inference and network delivery. It may return after the browser has disconnected. The model could then perform work that the user never sees.</p>\n<p>With fail-fast behavior, the application receives a rejection close to admission time. It can spend the remaining deadline on a smaller model, cached answer, search result, deterministic rule, or clear error state.</p>\n<p>This is admission control. It does not increase GPU capacity or make inference faster. It prevents a caller from pretending that waiting has no cost.</p>\n<p>Queueing remains useful. It smooths short bursts, improves utilization, and can absorb temporary contention. Problems appear when the queue is invisible to the product deadline. At that point, waiting converts a capacity shortage into tail latency, timeouts, abandoned requests, and retry storms.</p>\n<h2>How to set <code>rejectIfBusy</code></h2>\n<p>The parameter location differs by API surface. That detail is easy to miss.</p>\n<p>For a Workers AI binding, pass the option as the third argument to <code>env.AI.run()</code>:</p>\n<pre><code class=\"language-ts\">const result = await env.AI.run(\n  \"@cf/google/gemma-4-26b-a4b-it\",\n  {\n    messages: [\n      { role: \"user\", content: \"Summarize this incident report.\" },\n    ],\n  },\n  { rejectIfBusy: true },\n)\n</code></pre>\n<p>Do not put the option inside the model input object. Cloudflare states that the binding reads it from the third argument.</p>\n<p>For the native REST API, put it in the request body's <code>options</code> object:</p>\n<pre><code class=\"language-json\">{\n  \"messages\": [\n    { \"role\": \"user\", \"content\": \"Summarize this incident report.\" }\n  ],\n  \"options\": {\n    \"rejectIfBusy\": true\n  }\n}\n</code></pre>\n<p>The OpenAI-compatible Chat Completions endpoint also accepts a top-level <code>options</code> object. Cloudflare warns that some OpenAI clients strip unknown fields. Inspect the actual HTTP request or run a controlled capacity test before assuming the option reaches the service.</p>\n<h2>Recognize the right 429</h2>\n<p>An HTTP status alone is not enough to choose a retry policy. Cloudflare's <a href=\"https://developers.cloudflare.com/workers-ai/platform/errors/\">Workers AI error reference</a> lists several errors, including more than one that uses <code>429</code>.</p>\n<p>Code <code>3040</code> means the service is out of capacity. The same code is returned when <code>rejectIfBusy</code> triggers. Code <code>3036</code> indicates that an account has used its daily free allocation. Retrying a quota-exhausted request every second will not make it succeed. Retrying a temporary capacity rejection may succeed, but only if the request still has time and the retry does not add to an overload loop.</p>\n<p>Normalize provider errors at the boundary:</p>\n<pre><code class=\"language-ts\">type InferenceFailure =\n  | { kind: \"capacity\"; retryable: true }\n  | { kind: \"quota\"; retryable: false }\n  | { kind: \"invalid_request\"; retryable: false }\n  | { kind: \"upstream\"; retryable: true }\n\nfunction classifyWorkersAIError(error: unknown): InferenceFailure {\n  const value = error as {\n    status?: number\n    code?: number\n    message?: string\n  }\n\n  if (value.status === 429 &amp;&amp; value.code === 3040) {\n    return { kind: \"capacity\", retryable: true }\n  }\n\n  if (value.status === 429 &amp;&amp; value.code === 3036) {\n    return { kind: \"quota\", retryable: false }\n  }\n\n  if (value.status &amp;&amp; value.status &gt;= 500) {\n    return { kind: \"upstream\", retryable: true }\n  }\n\n  return { kind: \"invalid_request\", retryable: false }\n}\n</code></pre>\n<p>Adapt the parsing to the error shape produced by your binding or client. Log the provider status and internal code, but avoid logging prompts, credentials, or sensitive model output by default.</p>\n<h2>Decide from the request's deadline</h2>\n<p>The application should know how much time remains before it sends an inference request. A fixed retry count ignores work already spent on authentication, database queries, retrieval, prompt construction, and network hops.</p>\n<p>Carry a deadline through the request:</p>\n<pre><code class=\"language-ts\">const startedAt = Date.now()\nconst deadlineAt = startedAt + 4_000\n\nfunction remainingMs() {\n  return Math.max(0, deadlineAt - Date.now())\n}\n</code></pre>\n<p>Before retrying, reserve time for the model and the response path. If 600 milliseconds remain and the normal inference takes two seconds, retrying only creates a later failure.</p>\n<p>A useful policy is:</p>\n<ol>\n<li>Attempt the preferred model with <code>rejectIfBusy</code>.</li>\n<li>On code <code>3040</code>, check the remaining deadline.</li>\n<li>Retry once with jitter only when enough time remains.</li>\n<li>Otherwise use a cheaper or more available model, a deterministic path, or a retryable response.</li>\n<li>Stop when the caller disconnects or the deadline expires.</li>\n</ol>\n<p>The exact numbers should come from observed latency, not a universal template.</p>\n<h2>Retry without creating a storm</h2>\n<p>Immediate retries synchronize clients. A burst that encounters no capacity returns together, retries together, and produces another burst.</p>\n<p>Use bounded exponential backoff with jitter:</p>\n<pre><code class=\"language-ts\">function retryDelay(attempt: number) {\n  const base = Math.min(100 * 2 ** attempt, 800)\n  return Math.floor(Math.random() * base)\n}\n</code></pre>\n<p>For a four-second interactive deadline, one retry may be enough. For background work, a durable queue can retry over a longer period. Keep client retries and server retries coordinated. If the browser retries twice, the API retries twice, and an AI gateway retries twice, one user action can become many provider calls.</p>\n<p>Attach an idempotency key or request identifier when downstream actions can be repeated. Text generation itself may produce different output on each attempt. Tool calls triggered by that output need their own deduplication and authorization controls.</p>\n<h2>Build a fallback ladder</h2>\n<p>A fallback should preserve the product's intent at lower quality or reduced scope. It should not silently change the meaning of the operation.</p>\n<p>For customer-support drafting, the ladder might be:</p>\n<ol>\n<li>preferred instruction model;</li>\n<li>smaller approved model with a shorter prompt;</li>\n<li>retrieval results presented without generated prose;</li>\n<li>saved draft and a message that generation is temporarily unavailable.</li>\n</ol>\n<p>For content moderation, a weaker fallback may be unsafe. The application might quarantine the content for later review instead of allowing it. For code generation, a cached generic answer could be more harmful than an honest failure.</p>\n<p>Cloudflare AI Gateway offers controls such as retries and model fallback. If you combine gateway behavior with application behavior, document which layer makes each decision. The article on <a href=\"https://www.nandann.com/blog/identity-based-ai-model-routing\">identity-based AI model routing</a> explains why model access and fallback should also depend on the caller's authority and data boundary.</p>\n<h2>Match the policy to the workload</h2>\n<p>Interactive autocomplete has a short useful life. Enable fail-fast behavior, avoid multiple retries, and degrade to no suggestion.</p>\n<p>A conversational assistant can tolerate more delay, but users need progress and a cancel path. A single bounded retry or model fallback may work. Do not leave the interface showing an indefinite typing indicator after the original request has timed out.</p>\n<p>Document processing is usually background work. A durable queue is better than repeatedly asking the browser to retry. Leaving <code>rejectIfBusy</code> off may be appropriate if the platform queue fits the job's service objective. If job ownership, visibility, or scheduling matters, manage the queue in your application and use fail-fast admission at the provider boundary.</p>\n<p>Safety checks and authorization decisions should fail closed. If the AI call is only advisory, use a deterministic safety baseline. If the model is required, return a pending or unavailable state rather than bypassing the control.</p>\n<p>Batch enrichment can wait and is cost sensitive. Spread work, respect rate limits, and use backpressure before sending requests. <code>rejectIfBusy</code> is a final capacity signal, not the batch scheduler.</p>\n<h2>Protect the service with local concurrency limits</h2>\n<p>Fail-fast provider behavior does not stop your Worker from accepting unlimited upstream work. Apply concurrency limits before inference. A simple semaphore can cap active requests per isolate or process, but distributed traffic needs a shared strategy or platform-level control.</p>\n<p>Limit by workload and tenant. One bulk customer should not consume the entire interactive pool. Separate queues for latency-sensitive and background work. Set per-tenant budgets and maximum prompt sizes before a request reaches the model.</p>\n<p>Rate limits and capacity are different. Cloudflare publishes <a href=\"https://developers.cloudflare.com/workers-ai/platform/limits/\">Workers AI limits</a> by task and model. Staying below an account rate limit does not guarantee immediate GPU capacity. A capacity rejection does not necessarily mean the account exceeded a documented requests-per-minute limit.</p>\n<h2>Keep tenant policy intact during fallback</h2>\n<p>Capacity pressure is a bad time to loosen data controls. A fallback model or endpoint may use a different provider, region, retention policy, context window, safety profile, or commercial agreement. Route only to destinations already approved for that tenant and data class.</p>\n<p>Attach policy information before model selection. The router should know whether the prompt contains personal data, source code, health information, payment details, or customer-confidential documents. It should also know whether the tenant permits cross-provider fallback. A model that is technically available can still be an invalid destination.</p>\n<pre><code class=\"language-ts\">type InferencePolicy = {\n  tenantId: string\n  dataClass: \"public\" | \"internal\" | \"restricted\"\n  allowedModels: string[]\n  allowProviderFallback: boolean\n  deadlineAt: number\n}\n</code></pre>\n<p>When the preferred model returns <code>3040</code>, filter candidates through policy before considering latency or price. If no approved fallback remains, fail honestly. Do not send the prompt to an unapproved model just to improve the apparent success rate.</p>\n<p>Tool permissions need the same treatment. A smaller fallback model may have different tool-calling behavior or weaker instruction following. Re-run server-side authorization for every tool call and keep high-impact tools behind approval. Never inherit a model's earlier authorization decision from a failed attempt.</p>\n<p>Cache use also needs boundaries. A cached answer can be an excellent overload response for public documentation. It is unsafe when the cache key omits tenant, permission, locale, source version, or other context that affects the answer. Record whether the user received a generated, cached, retrieved, or deterministic response so incidents and evaluations can reconstruct the path.</p>\n<p>Finally, protect the error channel. Internal capacity codes, model identifiers, account limits, and routing policy are useful in logs but may reveal infrastructure details to an anonymous caller. Return a stable product error and keep the diagnostic fields in protected telemetry associated with the request ID.</p>\n<h2>Measure whether fail-fast helps</h2>\n<p>Track at least these events:</p>\n<ul>\n<li>inference attempts by model and workload;</li>\n<li>accepted requests;</li>\n<li>capacity rejections with code <code>3040</code>;</li>\n<li>quota and rate-limit rejections;</li>\n<li>queue or gateway wait time when observable;</li>\n<li>retry attempts and eventual outcome;</li>\n<li>fallback selection and success;</li>\n<li>end-to-end latency, including p95 and p99;</li>\n<li>client cancellations;</li>\n<li>generated work discarded after disconnect;</li>\n<li>cost or neuron usage by outcome.</li>\n</ul>\n<p>Do not report a lower latency percentile without reporting rejected requests. A service can look fast by failing every busy call immediately. Pair latency with success rate, fallback rate, and user-visible completion.</p>\n<p>Create separate dashboards for interactive and background traffic. Their healthy shapes differ. Interactive workloads care about deadline success. Background workloads care about completion age, backlog, throughput, and cost.</p>\n<p>A useful capacity test increases offered load gradually while recording completed, rejected, timed-out, and cancelled requests. Compare queueing with fail-fast behavior at the same traffic levels. The goal is to find where useful completions stop increasing and tail latency starts consuming the deadline.</p>\n<h2>Return an honest response to the user</h2>\n<p>Map provider errors to product language. Users do not need Cloudflare's internal code, but support and telemetry do.</p>\n<p>For an interactive endpoint, a structured response can preserve both:</p>\n<pre><code class=\"language-json\">{\n  \"error\": \"AI_CAPACITY_TEMPORARY\",\n  \"message\": \"Generation is temporarily busy. Try again in a moment.\",\n  \"retryable\": true,\n  \"requestId\": \"req_01...\"\n}\n</code></pre>\n<p>Use an appropriate HTTP status for your contract. Avoid claiming that a retry will succeed at a specific time unless you have a reliable signal. Do not turn a capacity response into a generic <code>500</code> that triggers broad incident alarms and hides the overload pattern.</p>\n<p>If a fallback produced the answer, consider exposing that in telemetry and, when it changes quality materially, in the interface. Silent fallback can complicate debugging and evaluation.</p>\n<h2>Test overload before production finds it</h2>\n<p>A normal functional test will rarely exercise <code>rejectIfBusy</code>. The provider has capacity, the request succeeds, and the team learns nothing about its rejection path. Build a test seam around the inference client so code <code>3040</code> can be injected deterministically.</p>\n<p>Test at least these cases:</p>\n<ul>\n<li>the first attempt receives <code>3040</code> and a retry succeeds within the deadline;</li>\n<li>the first attempt receives <code>3040</code> with too little time left to retry;</li>\n<li>the preferred model is busy and the fallback succeeds;</li>\n<li>both preferred and fallback models reject capacity;</li>\n<li>the caller disconnects before the retry timer fires;</li>\n<li>the account returns quota code <code>3036</code>, which must not enter the capacity retry loop;</li>\n<li>a client library drops the custom <code>options</code> field;</li>\n<li>two application replicas receive the same idempotent job.</li>\n</ul>\n<p>Then run a load test in a controlled environment. Increase concurrency rather than only total request count. Record the point where capacity rejections begin, how the fallback behaves, and whether local queues continue growing after useful throughput flattens. Stop the test if it threatens production quotas or shared tenants.</p>\n<p>Fault injection at the client boundary is more repeatable than trying to exhaust a shared model. It also lets CI verify the response contract on every change. A staging load test is still valuable for timing and concurrency behavior, but it should not be the only way to reach the error branch.</p>\n<h2>Cancellation is part of capacity management</h2>\n<p>When a browser closes, an upstream request times out, or the user presses Stop, propagate cancellation as far as the client and platform allow. A retry scheduled after cancellation should never start. Retrieval, prompt construction, tool calls, and fallback selection should all check the same abort signal.</p>\n<pre><code class=\"language-ts\">export async function generateWithDeadline(\n  input: ModelInput,\n  signal: AbortSignal,\n) {\n  if (signal.aborted) throw signal.reason\n\n  const result = await env.AI.run(\n    MODEL,\n    input,\n    { rejectIfBusy: true },\n  )\n\n  if (signal.aborted) throw signal.reason\n  return result\n}\n</code></pre>\n<p>The example prevents work before the call and discards a late result. Whether an in-flight inference can receive the signal depends on the exact client and interface in use, so verify cancellation support before promising it. The application should always stop retries and avoid launching downstream tools for an abandoned turn.</p>\n<p>Cancellation metrics reveal hidden waste. Count provider completions that arrive after the client has gone, fallback work started after an upstream timeout, and tool calls whose result had no consumer. <code>rejectIfBusy</code> protects admission during capacity pressure. Cancellation protects capacity after the request has lost its value.</p>\n<h2>A production policy example</h2>\n<p>Write the policy before shipping the flag:</p>\n<pre><code class=\"language-text\">Workload: editor autocomplete\nDeadline: 2,500 ms end to end\nAdmission: rejectIfBusy enabled\nRetries: one, only when at least 1,800 ms remain\nFallback: smaller approved model\nFinal degradation: return no suggestion\nClient retry: none automatically\nMetrics: capacity rejection, fallback, deadline success, cancellation\n</code></pre>\n<p>For background summarization:</p>\n<pre><code class=\"language-text\">Workload: uploaded-document summary\nDeadline: 15 minutes\nAdmission: application queue controls dispatch\nProvider behavior: rejectIfBusy enabled at dispatch boundary\nRetries: durable exponential backoff, capped attempts\nFallback: none unless document policy permits another model\nFinal degradation: job remains failed with a retry action\nMetrics: queue age, attempts, completion age, capacity rejection, cost\n</code></pre>\n<p>Both examples use <code>rejectIfBusy</code>, but for different reasons. The interactive path protects a product deadline. The background path keeps queue ownership and retry visibility inside the application.</p>\n<h2>What to ship</h2>\n<p>Add <code>rejectIfBusy</code> only after defining the error parser, deadline, retry ceiling, fallback, and metrics. Verify that the client library preserves the option. Test code <code>3040</code> separately from quota exhaustion. Cancel work when the caller leaves, and keep retry behavior in one clearly owned layer.</p>\n<p>Cloudflare's option gives the application a useful capacity signal. The engineering work is deciding what the signal should mean for each request. A short-lived suggestion should fail quickly. A durable job should wait somewhere you can observe and control it.</p>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
