import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "wordpress-7-2-dataform-editor-inspector",
  title: "WordPress 7.2 DataForm Inspector: Plugin Test Guide",
  description: "WordPress is rebuilding the editor inspector with DataForm for 7.2. Test plugin panels, custom post types, roles, media filters, and view config now.",
  date: "2026-09-18",
  readTime: "15 min",
  category: "WordPress",
  tags: ["WordPress 7.2","DataForm","Gutenberg","Plugin Development","Block Editor","Quick Edit"],
  faqs: [
      {
          "question": "What is the WordPress 7.2 DataForm editor inspector?",
          "answer": "It is an experimental rebuild of the Post or Page settings tab on top of DataForm, the same form system used by Quick Edit in the Site Editor. The project aims to stabilize the work for WordPress 7.2."
      },
      {
          "question": "How can plugin developers test the new inspector?",
          "answer": "Install Gutenberg 24.0 or newer on a test site, enable Editor Inspector: Use DataForm under Settings > Gutenberg, and compare plugin behavior with the experiment on and off. Gutenberg trunk is the most current test target."
      },
      {
          "question": "Which plugin extension points should continue to work?",
          "answer": "The official testing call says PluginDocumentSettingPanel, PluginPrePublishPanel, PluginPostPublishPanel, PluginSidebar, PluginPostStatusInfo fills, and removeEditorPanel should continue working. Each plugin still needs to test its own behavior."
      },
      {
          "question": "Which inspector integrations are at risk?",
          "answer": "PluginPostExcerpt is not ported, editor.PostFeaturedImage does not work yet, the featured-image picker does not yet pass through editor.MediaUpload, and integrations that depend on classic panel CSS selectors or DOM markup should be expected to break."
      },
      {
          "question": "Does the DataForm experiment affect taxonomy panels and meta boxes?",
          "answer": "The September 17 testing call lists block editing, taxonomy panels, and meta boxes as out of scope. Its immediate focus is compatibility for extensions that add to or modify the editor inspector."
      }
  ],
  coverImage: "/images/wordpress-7-2-dataform-editor-inspector.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/wordpress-7-2-dataform-editor-inspector.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/wordpress-7-2-dataform-editor-inspector.webp\" alt=\"WordPress 7.2 DataForm Inspector: Plugin Test Guide\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>WordPress is rebuilding the Post and Page settings tab with DataForm, the same form system used by Quick Edit in the Site Editor. The experiment is available in Gutenberg 24.0 and is being aimed at WordPress 7.2.</p>\n<p>For users, the goal is consistent settings in two places that currently drift apart. For plugin developers, the change is more consequential. Code that adds a supported plugin panel should keep working. Code that reaches into the old inspector's markup, CSS classes, deprecated slots, or featured-image implementation may not.</p>\n<p>The WordPress project published a <a href=\"https://make.wordpress.org/core/2026/09/17/call-for-testing-dataform-editor-inspector/\">call for testing on September 17</a>. This is the useful window to find compatibility problems while the design and extension paths are still being discussed.</p>\n<p>This guide explains what is changing, what WordPress expects to remain compatible, and how to test a real plugin across posts, pages, custom post types, roles, and media integrations.</p>\n<h2>What the DataForm inspector changes</h2>\n<p>With the <code>Editor Inspector: Use DataForm</code> experiment enabled, the familiar summary panels in the editor's Post or Page tab are replaced by a DataForm. The affected settings include featured image, excerpt, status, date, author, template, and other fields supported by the current post type.</p>\n<p>The same form definition also builds Quick Edit in the Site Editor. One definition can therefore control which fields appear and how they are arranged in both editing surfaces.</p>\n<p>This is the main architectural shift:</p>\n<pre><code class=\"language-text\">Before\nPost editor inspector -&gt; its own panels and markup\nSite Editor Quick Edit -&gt; DataForm configuration\n\nTarget direction\nPost editor inspector -&gt; shared DataForm configuration\nSite Editor Quick Edit -&gt; shared DataForm configuration\n</code></pre>\n<p>The <a href=\"https://github.com/WordPress/gutenberg/issues/76076\">tracking issue</a> describes the intent as a visually and behaviorally consistent experience with developer hooks that work across screens. It also makes clear that extensibility should move toward entity fields and actions rather than old slots tied to one interface.</p>\n<p>The experiment is not a finished WordPress 7.2 contract. It is a compatibility target under active testing. Plugin developers should report concrete breakage rather than coding against every private detail they see in the current Gutenberg build.</p>\n<h2>Why WordPress is consolidating the two forms</h2>\n<p>Quick Edit and the full editor can modify many of the same properties. Maintaining separate UI implementations creates predictable problems: labels differ, one screen receives a new field before the other, permissions are interpreted differently, and plugins need separate integration code.</p>\n<p>DataForm describes the data, editable fields, layout, validation, and changes through a shared component model. The <a href=\"https://developer.wordpress.org/block-editor/reference-guides/packages/packages-dataviews/\">DataViews package documentation</a> defines <code>DataForm</code> as the component for editing one item. A field describes how a value is read, rendered, edited, validated, and written. The form configuration decides which fields appear and how they are laid out.</p>\n<p>For a plugin, the intended gain is not a different-looking sidebar. It is one extension model that can reach the editor inspector and Quick Edit. That could reduce duplicated controls and mismatched behavior, provided the Fields API and configuration filters cover the real extension needs found during testing.</p>\n<h2>The compatibility map today</h2>\n<p>The official testing call gives a specific snapshot of what should work and what does not yet work.</p>\n<table>\n<thead>\n<tr><th>Integration</th><th>Current expectation</th></tr>\n</thead>\n<tbody>\n<tr><td><code>PluginDocumentSettingPanel</code></td><td>Should keep working</td></tr>\n<tr><td><code>PluginPrePublishPanel</code></td><td>Should keep working</td></tr>\n<tr><td><code>PluginPostPublishPanel</code></td><td>Should keep working</td></tr>\n<tr><td><code>PluginSidebar</code></td><td>Should keep working</td></tr>\n<tr><td><code>PluginPostStatusInfo</code></td><td>Should keep working</td></tr>\n<tr><td><code>removeEditorPanel</code></td><td>Should keep working</td></tr>\n<tr><td><code>PluginPostExcerpt</code></td><td>Not ported; deprecated</td></tr>\n<tr><td><code>editor.PostFeaturedImage</code></td><td>Does not work yet</td></tr>\n<tr><td><code>editor.MediaUpload</code> in featured image</td><td>Not used by the picker yet; exploratory work exists</td></tr>\n<tr><td>Selectors tied to classic inspector markup</td><td>Expected to be unreliable</td></tr>\n<tr><td>CSS tied to classic panel class names</td><td>Expected to be unreliable</td></tr>\n</tbody>\n</table>\n<p>\"Should keep working\" still calls for a test. A slot may render but have spacing problems, save the wrong value, appear for the wrong role, or behave differently between the full editor and Quick Edit.</p>\n<p>The recommendation for content previously rendered through <code>PluginPostExcerpt</code> is to move it to <code>PluginDocumentSettingPanel</code>. The former is deprecated and may not be added to the new implementation.</p>\n<h2>Set up a useful test environment</h2>\n<p>Do not enable the experiment first on a production site. Use a disposable local, staging, or Playground environment with representative content and plugin settings.</p>\n<p>The WordPress call gives two supported starting points:</p>\n<ul>\n<li>Gutenberg 24.0 or newer, which also works with WordPress 6.9;</li>\n<li>Gutenberg trunk, which requires WordPress 7.0 or newer and contains the latest fixes.</li>\n</ul>\n<p>Install the selected Gutenberg version, then open Settings > Gutenberg and enable <code>Editor Inspector: Use DataForm</code>. Test the same content with the experiment on and off.</p>\n<p>Record the versions in every report:</p>\n<pre><code class=\"language-text\">WordPress: 7.1.1 RC1 or trunk revision\nGutenberg: 24.0.0 or commit SHA\nPlugin: name and exact version\nTheme: name and exact version\nBrowser: name and version\nExperiment: on or off\nPost type: post, page, or custom slug\nUser role: administrator, editor, author, contributor\n</code></pre>\n<p>WordPress also provides linked Playground instances in the testing call. Playground is useful for a quick visual and functional check. Upload your plugin ZIP through the Plugins screen, reproduce the behavior, and save enough information to recreate the same environment locally if debugging needs source maps or automated tests.</p>\n<h2>Build a before-and-after inventory</h2>\n<p>Before looking for bugs, list everything the plugin adds to the Post or Page tab. Search the codebase for editor slots, filters, panel removal, and CSS selectors.</p>\n<p>Useful search terms include:</p>\n<pre><code class=\"language-sh\">rg \"PluginDocumentSettingPanel|PluginPostStatusInfo|PluginPostExcerpt\" .\nrg \"PluginPrePublishPanel|PluginPostPublishPanel|PluginSidebar\" .\nrg \"removeEditorPanel|editor.PostFeaturedImage|editor.MediaUpload\" .\nrg \"edit-post|post-summary|document-settings|components-panel\" src build\n</code></pre>\n<p>The last search is intentionally broad. It finds style and DOM assumptions that may never call a public extension API. Review each match rather than replacing class names with new experimental ones.</p>\n<p>For every integration, record:</p>\n<ul>\n<li>where it appears with the experiment off;</li>\n<li>where it appears with the experiment on;</li>\n<li>whether it appears in Site Editor Quick Edit;</li>\n<li>the value it reads;</li>\n<li>the value it writes;</li>\n<li>the REST field or meta key involved;</li>\n<li>which post types and roles should see it;</li>\n<li>the public API it relies on;</li>\n<li>any private DOM or CSS dependency.</li>\n</ul>\n<p>Screenshots help with layout comparisons. A small automated test is better for persistence and permissions.</p>\n<h2>Test supported plugin panels</h2>\n<p>Start with <code>PluginDocumentSettingPanel</code>, because WordPress identifies it as the temporary home for inspector content until the Fields API lands.</p>\n<p>A panel test should cover more than rendering:</p>\n<ol>\n<li>Create a post and set the plugin value.</li>\n<li>Save, reload, and confirm the value persists.</li>\n<li>Change the value and verify dirty-state handling.</li>\n<li>Switch between the Post and Block tabs.</li>\n<li>Close and reopen the settings sidebar.</li>\n<li>Test autosave and manual save.</li>\n<li>Test a failed REST update.</li>\n<li>Confirm the panel respects post-type support and capabilities.</li>\n<li>Check keyboard navigation, focus order, labels, help text, and errors.</li>\n<li>Compare the same workflow with the experiment off.</li>\n</ol>\n<p>If the plugin uses <code>removeEditorPanel</code>, verify that it removes only the intended setting and does not hide unrelated DataForm fields. A removal call that worked by matching an old panel identifier may behave differently when the setting is represented as a field.</p>\n<p>Pre-publish and post-publish panels are outside the main summary form but should still be exercised. Publish, schedule, update, and fail a save. Check that the plugin does not rely on state formerly exposed by the classic inspector's component tree.</p>\n<h2>Test custom post types from their declared support</h2>\n<p>The DataForm inspector should show settings supported by the current post type. A custom type without <code>excerpt</code> support should not gain an Excerpt field. A type without thumbnails should not show a featured-image control. Hierarchical and template-aware types may have different fields from ordinary posts.</p>\n<p>Create a matrix from the plugin's registered post types:</p>\n<table>\n<thead>\n<tr><th>Post type</th><th>Block editor</th><th>Supports</th><th>Expected fields</th><th>Plugin fields</th></tr>\n</thead>\n<tbody>\n<tr><td><code>book</code></td><td>Yes</td><td>title, editor, excerpt, thumbnail</td><td>status, date, author, excerpt, featured image</td><td>ISBN, edition</td></tr>\n<tr><td><code>event</code></td><td>Yes</td><td>title, editor, custom-fields</td><td>status, date, author</td><td>venue, start time</td></tr>\n<tr><td><code>legacy_item</code></td><td>No</td><td>title</td><td>Not part of this inspector test</td><td>Classic metaboxes</td></tr>\n</tbody>\n</table>\n<p>Open new and existing records for each type. Test missing values and values created before the plugin's current version. Confirm that REST schema, <code>show_in_rest</code>, authentication callbacks, and meta registration still permit the editor to read and write the fields.</p>\n<p>Do not report classic meta-box behavior as a DataForm regression without checking scope. The call for testing lists meta boxes and taxonomy panels as separate concerns.</p>\n<h2>Test roles and capabilities</h2>\n<p>An administrator-only test misses one of the highest-risk regressions. The official call asks developers to test editors, authors, and contributors. It gives a concrete example: an author should not receive the Author setting.</p>\n<p>For every role the plugin supports, test:</p>\n<ul>\n<li>which core fields are visible;</li>\n<li>which plugin fields are visible;</li>\n<li>whether a disabled field is clearly disabled rather than silently failing;</li>\n<li>whether saving strips or preserves values the role cannot edit;</li>\n<li>whether REST errors are displayed;</li>\n<li>whether private, draft, pending, scheduled, and published states behave correctly;</li>\n<li>whether switching author, template, status, or date respects capabilities.</li>\n</ul>\n<p>Check the server response as well as the interface. Hiding a field is not authorization. The REST endpoint, meta registration, and save callback must reject unauthorized changes independently.</p>\n<p>Automate at least one lower-privilege path if the plugin changes editorial permissions. User-facing controls are likely to move again while the experiment develops, but capability rules should remain testable through outcomes.</p>\n<h2>Audit featured-image customizations carefully</h2>\n<p>Featured image has two named compatibility gaps in the current experiment.</p>\n<p>The <code>editor.PostFeaturedImage</code> filter does not work yet. Plugins that modify the classic featured-image component through that filter need to report their use case and test current Gutenberg work. CSS or scripts that target the old featured-image markup should be treated as fragile.</p>\n<p>The new picker's media flow also does not yet pass through <code>editor.MediaUpload</code>. An exploratory pull request exists, but the testing call says it is on hold. If your plugin replaces the media picker, test the linked PR or Playground build from the official call in both the post editor and Site Editor Quick Edit.</p>\n<p>Document why the plugin replaces the picker. Is it enforcing an asset library, adding licensing metadata, restricting sources, applying an image transform, or changing permissions? A clear use case helps the WordPress team evaluate whether an old filter should be ported or a new API should represent the requirement.</p>\n<p>Avoid shipping a DOM patch for the experiment. It may work for one Gutenberg build and fail before WordPress 7.2 reaches beta.</p>\n<h2>Use view configuration for supported layout changes</h2>\n<p>WordPress already exposes server-side view configuration filters. For posts, the official call names <code>get_entity_view_config_posttype_post</code>. Other post types use the corresponding dynamic filter name.</p>\n<p>The <a href=\"https://developer.wordpress.org/block-editor/reference-guides/view-config-reference/\">view configuration reference</a> documents a <code>form</code> structure containing a layout and ordered field list. A configuration can choose field order, labels, layout types, and nested children. Only fields registered for the entity are rendered.</p>\n<p>A simplified shape looks like this:</p>\n<pre><code class=\"language-php\">$form = array(\n    'layout' =&gt; array( 'type' =&gt; 'panel' ),\n    'fields' =&gt; array(\n        array(\n            'id'     =&gt; 'status',\n            'label'  =&gt; __( 'Status', 'my-plugin' ),\n            'children' =&gt; array( 'status', 'password', 'sticky' ),\n        ),\n        'date',\n        'author',\n    ),\n);\n</code></pre>\n<p>Use the official schema and filter documentation for the build you test. Preserve unknown entries when modifying shared configuration so another plugin or a later WordPress version is not accidentally removed. Treat field IDs as identities, not as display labels.</p>\n<p>The Fields API intended to add custom fields across the inspector and Quick Edit is aimed at 7.2 but is not yet a stable contract. Follow the tracking issues rather than copying private package exports into a production plugin.</p>\n<h2>Separate API compatibility from visual compatibility</h2>\n<p>A plugin can pass its save tests and still produce a poor sidebar. DataForm may render fields with different spacing, labels, panel triggers, focus behavior, or responsive layout.</p>\n<p>Run a visual and accessibility pass at common viewport sizes. Zoom to 200 percent. Navigate with the keyboard. Check screen-reader labels and error associations. Use long translated labels and right-to-left text. Test empty, loading, error, and disabled states.</p>\n<p>Remove selectors that style generic WordPress internals. A rule targeting a reusable panel class can affect core fields and other plugins. Scope plugin styles to a wrapper you own. Prefer component props and public layout configuration over DOM traversal.</p>\n<p>Do not assume a visually similar control has the same semantics. A date field can represent local time, site time, a scheduled publication, or an immutable event time. Verify the saved REST value and timezone conversion.</p>\n<h2>Report findings so maintainers can use them</h2>\n<p>The WordPress call says the best reporting location for extension compatibility is the linked GitHub issue. Reports about the inspector's design belong on the tracking issue.</p>\n<p>A useful report contains:</p>\n<pre><code class=\"language-text\">Plugin and version:\nWordPress and Gutenberg versions:\nExperiment state:\nPost type and role:\nExtension API used:\nExpected behavior:\nObserved behavior:\nSteps to reproduce:\nScreenshots or short recording:\nBrowser console and REST errors:\nMinimal plugin or repository:\nCan the plugin adapt with a public API?:\n</code></pre>\n<p>Report successful tests too. The project explicitly says that \"checked X, nothing changed\" is useful. Compatibility evidence helps distinguish widely used working paths from paths nobody exercised.</p>\n<p>Keep the reproduction small. A plugin ZIP with one panel and one failing save is easier to diagnose than a production plugin with unrelated build tooling. Remove secrets and customer content before attaching logs or exports.</p>\n<h2>Add regression coverage before the interface stabilizes</h2>\n<p>Visual details may change during the experiment, but plugin outcomes can be automated now. Build tests around public behavior: the field is available to the right role, the value loads, an edit becomes dirty, saving persists it, and reopening the post shows the same value.</p>\n<p>Use end-to-end selectors based on accessible roles, names, and plugin-owned identifiers. Avoid a long selector copied from the current DataForm DOM. If the field label changes through translation, give the plugin's wrapper a stable identifier that you control while still asserting the visible accessible label.</p>\n<p>Keep experiment-on and experiment-off cases temporarily if the plugin supports both the current editor and the developing 7.2 path. Reuse the same outcome assertions through a small page-object adapter. That isolates expected UI differences without duplicating the whole test suite.</p>\n<p>Server tests should cover the authorization boundary separately. Call the REST update as each supported role, confirm unauthorized fields are rejected, and verify that a failed update does not erase an existing value. A browser test that cannot see a field proves nothing about whether the endpoint will accept a forged request.</p>\n<p>Add custom post-type fixtures with different <code>supports</code> combinations. Include a post type with excerpt and thumbnail support, one without them, and one unavailable in the block editor. This catches assumptions that every entity has the same DataForm definition.</p>\n<p>When Gutenberg changes, run the suite against the pinned version used by customers and a scheduled job against Gutenberg trunk. A trunk failure is an early compatibility signal, not automatically a production emergency. Record the first failing commit or package version so a report can identify the regression window.</p>\n<p>Screenshot tests can help with plugin-owned panels, but keep the capture area narrow. Full-editor screenshots change for many unrelated reasons and produce noisy diffs. Pair visual coverage with functional assertions so a pixel match never substitutes for a successful save.</p>\n<h2>What site owners should test</h2>\n<p>Site owners who use SEO, social, workflow, or custom-field plugins can contribute without writing code. Use a staging site or WordPress Playground, enable the experiment, and compare the Post and Page tabs before and after.</p>\n<p>Check whether plugin panels are present, values can be edited and saved, featured-image flows still work, and authors see the right controls. If something disappears, record the plugin name and version and capture matching screenshots with the experiment on and off.</p>\n<p>Do not use a production editorial workflow as the first test. The experiment may contain known issues, and active Gutenberg builds change quickly.</p>\n<h2>Prepare without coding against an experiment</h2>\n<p>Plugin teams can do useful work now without committing to unfinished APIs:</p>\n<ul>\n<li>inventory inspector extensions and private selectors;</li>\n<li>migrate away from deprecated <code>PluginPostExcerpt</code> where practical;</li>\n<li>move inspector content into supported <code>PluginDocumentSettingPanel</code> surfaces;</li>\n<li>add tests for custom post types and lower-privilege roles;</li>\n<li>isolate featured-image customization behind one adapter;</li>\n<li>separate server authorization from field visibility;</li>\n<li>follow the Fields API and inspector tracking issues;</li>\n<li>test each Gutenberg milestone before WordPress 7.2 beta.</li>\n</ul>\n<p>WordPress 7.2 has other developer work in progress, including the proposed Secrets API covered in <a href=\"https://www.nandann.com/blog/wordpress-72-secrets-api\">our WordPress 7.2 Secrets API analysis</a>. Treat both as release targets under development, not promises that every proposal will ship unchanged.</p>\n<p>The DataForm inspector is a chance to replace two drifting interfaces with a shared model. Whether that works for the plugin ecosystem depends on evidence gathered now. Test the extension points your plugin actually uses, report the gaps with small reproductions, and avoid trading an old private selector for a new one.</p>\n<hr />\n<div style=\"background: rgba(99,102,241,0.08); border: 1px solid rgba(99,102,241,0.25); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #818cf8; margin-top: 0;\">Need help with your WordPress site?</h3>\n  <p>Whether you're dealing with security vulnerabilities, plugin conflicts, slow load times, or Core Web Vitals failures — we handle the technical side so you can focus on your business. Custom plugin development, performance hardening, Wasm integrations, and ongoing maintenance retainers.</p>\n  <p><a href=\"/contact?ref=wordpress-blog\" style=\"display: inline-block; background: linear-gradient(135deg, #6366f1, #4f46e5); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Talk to Us About Your WordPress Site &rarr;</a></p>\n</div>\n",
};

export default post;
