import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "cloudflare-worker-previews-coding-agents",
  title: "Cloudflare Worker Previews for Safer Coding Agents",
  description: "A practical guide to Cloudflare Worker Previews for coding agents, including CI setup, data isolation, access controls, testing, observability, and cleanup.",
  date: "2026-09-23",
  readTime: "20 min",
  category: "Engineering",
  tags: ["Cloudflare Workers","Coding Agents","CI/CD","DevOps","Security"],
  faqs: [
      {
          "question": "What is a Cloudflare Worker Preview?",
          "answer": "A Worker Preview is a named, branch-sized deployment environment under one Worker. It has its own URL, configuration, observability, and automatically isolated Durable Objects and Containers."
      },
      {
          "question": "Are Worker Previews safe for coding agents?",
          "answer": "They are a strong execution boundary when paired with scoped credentials, Cloudflare Access, separate stateful resources, explicit cleanup, and tests that prove no preview can write to production data."
      },
      {
          "question": "Do Worker Previews copy production settings?",
          "answer": "No. Preview deployments do not inherit production settings automatically. Define preview bindings and variables explicitly in the previews configuration block."
      },
      {
          "question": "Which bindings are automatically isolated?",
          "answer": "Durable Objects and Containers are automatically isolated per preview. KV, D1, R2, Vectorize, Hyperdrive, Analytics Engine, Pipelines, and several other bindings require separate preview resources if you need data isolation."
      },
      {
          "question": "Can a preview call another production Worker?",
          "answer": "Yes. A service binding from a preview currently calls the bound Worker's production deployment, so teams must treat that boundary as a deliberate production dependency."
      },
      {
          "question": "How many previews can one Worker have?",
          "answer": "Cloudflare documents a limit of 100 previews per Worker on the Free plan and 500 on paid plans, with up to 100 deployments retained per preview."
      },
      {
          "question": "Should preview URLs be public?",
          "answer": "Preview URLs are public by default. Protect sensitive applications with Cloudflare Access and avoid placing secrets or customer data in an unprotected preview."
      }
  ],
  coverImage: "/images/cloudflare-worker-previews-coding-agents.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/cloudflare-worker-previews-coding-agents.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/cloudflare-worker-previews-coding-agents.webp\" alt=\"Cloudflare Worker Previews for Safer Coding Agents\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>A coding agent can open a pull request in minutes. The harder question is where its code should run before a person approves it. A shared staging Worker gives every branch the same URL, bindings, logs, and mutable state. Two agents working at once can overwrite one another. A test that succeeds may have exercised the wrong commit. A cleanup step can remove data another review still needs.</p>\n<p><a href=\"https://developers.cloudflare.com/workers/previews/\">Cloudflare Worker Previews</a> give each branch or pull request a named environment under the same Worker. The environment gets a stable preview URL, immutable deployment URLs, separate configuration, and its own observability stream. Durable Objects and Containers are isolated automatically. Other stateful bindings need deliberate separation.</p>\n<p>That combination makes previews useful for agent-generated changes, but a preview is not a security policy by itself. The safe design has five parts: a unique name, least-privilege deployment credentials, explicit non-production bindings, access control on the URL, and reliable cleanup.</p>\n<h2>What Worker Previews change</h2>\n<p>Cloudflare introduced Worker Previews on September 22, 2026. The feature is designed for branch and pull-request testing in the same runtime that serves the production Worker. A team can create hundreds of named previews without creating a separately managed Worker for every short-lived change.</p>\n<p>Each named preview has two useful addresses:</p>\n<ul>\n<li>A stable preview URL that follows the latest deployment to that preview name.</li>\n<li>An immutable deployment URL that points to one exact deployment.</li>\n</ul>\n<p>The stable URL is convenient for a pull request comment, a human review, or a browser-based test suite. The immutable URL is better evidence. If an agent reports that deployment <code>abc123</code> passed, reviewers can open that exact build instead of whatever was most recently pushed to the branch.</p>\n<p>Cloudflare currently documents 100 previews per Worker on the Free plan and 500 on paid plans. Each preview retains up to 100 deployments, with older deployments removed as the limit is reached. These are generous limits for ordinary pull-request traffic, but repositories with automated dependency updates or large agent swarms still need cleanup.</p>\n<h2>Why a shared staging Worker breaks under agent concurrency</h2>\n<p>Traditional staging often assumes a small number of human developers who coordinate releases. Coding agents change that operating model. Several tasks can start from the same commit, make unrelated changes, and deploy within the same few minutes.</p>\n<p>On a shared staging Worker, the last deployment wins. The first agent may run an API test after the second agent has replaced the code. Logs from both tasks are interleaved. A database migration from one branch can change the assumptions of another. Review links become time-sensitive because they no longer identify a commit.</p>\n<p>A named preview removes the code collision. It also gives the team a natural unit for authorization, observability, and deletion. The branch <code>agent/rate-limit-fix</code> can map to a sanitized preview name such as <code>pr-1842</code>. Everything produced for that review can be labeled with the same identifier.</p>\n<p>This fits the same principle we use in <a href=\"/blog/agentic-ci-pipelines-autonomous-code-review-testing\">agentic CI pipelines</a>: an agent should produce inspectable evidence in a bounded environment, not merely assert that its patch works.</p>\n<h2>A practical lifecycle for an agent-created preview</h2>\n<p>A robust lifecycle is short and deterministic:</p>\n<ol>\n<li>CI receives a pull-request event from a trusted repository context.</li>\n<li>The workflow creates a preview name from the pull-request number, not raw branch text.</li>\n<li>It deploys with <code>wrangler preview</code> and captures the JSON result.</li>\n<li>Tests run against the immutable deployment URL.</li>\n<li>The stable preview URL is posted to the pull request for review.</li>\n<li>Logs, test results, and the deployment identifier are attached to the check.</li>\n<li>The preview is deleted when the pull request closes.</li>\n</ol>\n<p>The pull-request number is a better identifier than a branch name because it is compact, stable, and easy to validate. It also avoids punctuation that may be unsuitable in resource names.</p>\n<p>Cloudflare requires Wrangler 4.135.0 or later for Worker Previews. A minimal local deployment looks like this:</p>\n<pre><code class=\"language-bash\">npx wrangler preview --name pr-1842 --json\n</code></pre>\n<p>The JSON output is important. Parse it and pass the URLs to later steps. Do not scrape terminal prose or guess a hostname.</p>\n<h2>Configure preview values explicitly</h2>\n<p>Preview deployments do not automatically inherit production settings. That is a useful safety property because a branch cannot silently acquire every production binding, route, and secret. It also means a preview that is not configured carefully may fail only after deployment.</p>\n<p>Keep shared runtime settings at the top level and preview-specific values under <code>previews</code>:</p>\n<pre><code class=\"language-jsonc\">{\n  \"$schema\": \"node_modules/wrangler/config-schema.json\",\n  \"name\": \"orders-api\",\n  \"main\": \"src/index.ts\",\n  \"compatibility_date\": \"2026-09-23\",\n  \"vars\": {\n    \"APP_ENV\": \"production\"\n  },\n  \"previews\": {\n    \"vars\": {\n      \"APP_ENV\": \"preview\",\n      \"ALLOW_TEST_USERS\": \"true\"\n    }\n  }\n}\n</code></pre>\n<p>Cloudflare keeps items such as <code>compatibility_date</code> and static asset configuration at the top level. Production routes, Cron Triggers, and queue consumers are not placed in the preview block. Treat configuration review as part of the pull request, especially when an agent modifies bindings or compatibility flags.</p>\n<p>Secrets deserve a separate path. Use preview secrets for credentials that are valid only in preview infrastructure. A production API token copied into preview configuration defeats the main containment benefit.</p>\n<h2>Understand the resource isolation matrix</h2>\n<p>Worker code isolation and data isolation are different things. According to Cloudflare's <a href=\"https://developers.cloudflare.com/workers/previews/resources/\">preview resource documentation</a>, Durable Objects and Containers are automatically isolated for each preview. Many other bindings are not.</p>\n<table>\n<thead>\n<tr><th>Resource</th><th>Default preview behavior</th><th>Safer agent setup</th></tr>\n</thead>\n<tbody>\n<tr><td>Durable Objects</td><td>Automatically isolated</td><td>Verify namespace behavior in integration tests</td></tr>\n<tr><td>Containers</td><td>Automatically isolated</td><td>Apply preview-specific resource limits</td></tr>\n<tr><td>KV</td><td>Uses the configured namespace</td><td>Bind a dedicated test namespace</td></tr>\n<tr><td>D1</td><td>Uses the configured database</td><td>Bind a disposable or seeded preview database</td></tr>\n<tr><td>R2</td><td>Uses the configured bucket</td><td>Use a test bucket and object prefix</td></tr>\n<tr><td>Hyperdrive</td><td>Uses the configured binding</td><td>Point to a non-production database</td></tr>\n<tr><td>Vectorize</td><td>Uses the configured index</td><td>Create a test index or use read-only fixtures</td></tr>\n<tr><td>Service bindings</td><td>Calls the bound production Worker</td><td>Replace with a test service or enforce read-only calls</td></tr>\n<tr><td>Queues</td><td>Producers are available, consumers are not</td><td>Send only to a test queue</td></tr>\n<tr><td>Workflows</td><td>Existing deployed workflows</td><td>Avoid irreversible production actions</td></tr>\n</tbody>\n</table>\n<p>The service-binding behavior is the easiest trap to miss. A preview can be isolated at its public URL while an internal call still reaches a production Worker. Map the complete call graph before allowing an agent to run end-to-end tests.</p>\n<h2>Give the agent a narrow deployment credential</h2>\n<p>The workflow needs a Cloudflare API token and account identifier. It does not need a broad account token. Cloudflare's <a href=\"https://developers.cloudflare.com/workers/ci-cd/external-cicd/github-actions/\">GitHub Actions guidance</a> recommends scoped API tokens, and that advice matters more when untrusted or agent-produced code enters the workflow.</p>\n<p>Store the token in the repository's secret store. Expose it only to the deployment job. Do not make it available to pull requests from forks, arbitrary shell steps, or the Worker at runtime.</p>\n<p>A useful separation is:</p>\n<ul>\n<li>The coding agent proposes source and configuration changes.</li>\n<li>A fixed, reviewed CI workflow performs the deployment.</li>\n<li>A separate job runs tests against the returned URL.</li>\n<li>The close event invokes a fixed cleanup command.</li>\n</ul>\n<p>This keeps the agent from rewriting the mechanism that grants it access. Changes to the deployment workflow should require code-owner review.</p>\n<h3>Threat-model the pull-request boundary</h3>\n<p>The most important distinction is whether the pull request comes from a trusted branch in the repository or from a fork. Workflows triggered by forked code should not receive Cloudflare deployment credentials. Even when the contributor is trusted, the code under review can read environment variables, make outbound requests, or change test scripts if the workflow exposes secrets to the same process.</p>\n<p>Separate build and deploy responsibilities. A build job can run linters and unit tests with no cloud credential. A protected deploy job can consume the checked-out commit through a fixed command after repository rules approve the event context. Keep the deployment token out of package lifecycle scripts because an agent can modify <code>package.json</code> and turn <code>npm install</code> into a secret-exfiltration step.</p>\n<p>Review these attack paths before enabling previews:</p>\n<ul>\n<li>A modified test script prints environment variables or uploads them to an external host.</li>\n<li>A Worker deployed for review reads a shared KV, D1, or R2 binding and returns production data.</li>\n<li>A service binding reaches a privileged production Worker that trusts internal traffic.</li>\n<li>A preview sends email, messages, payments, or webhooks with live credentials.</li>\n<li>An attacker guesses a public preview URL and reaches an unreleased admin route.</li>\n<li>A close-event workflow accepts an unvalidated name and deletes another preview.</li>\n</ul>\n<p>Controls should be enforceable outside the proposed code. Repository environment protection, scoped tokens, fixed CI actions, isolated bindings, and network-side access policies are stronger than asking generated code to behave.</p>\n<h3>Decide what the agent may observe</h3>\n<p>Coding agents often need logs to diagnose a failing preview. Give them the smallest useful view. A sanitized test log with request identifiers, stack traces, and synthetic fixtures is usually enough. Account-wide logs or production traces can contain customer identifiers, bearer tokens, and internal URLs that are unrelated to the task.</p>\n<p>If an agent can query observability data directly, scope access to the named preview and a short time range. Redact authorization headers, cookies, query-string secrets, and request bodies by default. Record when an automated system reads logs and which pull request authorized the access.</p>\n<p>There is also a prompt-injection boundary. Data returned by the application, logs, issue text, and pull-request comments are untrusted inputs to an agent. They should not be able to alter the fixed deployment workflow or convince it to reveal a secret. Treat natural-language instructions found in runtime data as data, not as workflow authority.</p>\n<h2>Build the GitHub Actions workflow</h2>\n<p>Cloudflare publishes an <a href=\"https://developers.cloudflare.com/workers/previews/automation-examples/\">automation example</a> for pull requests. The following shape adds a few controls that matter for coding agents:</p>\n<pre><code class=\"language-yaml\">name: worker-preview\n\non:\n  pull_request:\n    types: [opened, synchronize, reopened, closed]\n\npermissions:\n  contents: read\n  pull-requests: write\n\nconcurrency:\n  group: worker-preview-${{ github.event.pull_request.number }}\n  cancel-in-progress: true\n\njobs:\n  deploy:\n    if: github.event.action != 'closed' &amp;&amp; github.event.pull_request.head.repo.full_name == github.repository\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n      - run: npm ci\n      - run: npm test\n      - name: Deploy preview\n        run: npx wrangler preview --name pr-${{ github.event.pull_request.number }} --json\n        env:\n          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}\n          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}\n</code></pre>\n<p>Production deployments should remain a different workflow with different permissions. A preview check passing is evidence for review, not permission to promote itself.</p>\n<h2>Test the immutable deployment, review the stable URL</h2>\n<p>Use the two URL types for different jobs. The test runner should receive the immutable deployment URL from the deployment result. The pull-request comment should present both the stable URL and the tested deployment identifier.</p>\n<p>Start with a small set of tests that establish the environment boundary:</p>\n<pre><code class=\"language-bash\">curl --fail --silent --show-error \"$DEPLOYMENT_URL/health\"\ncurl --fail --silent --show-error \\\n  -H \"X-Test-Run: $GITHUB_RUN_ID\" \\\n  \"$DEPLOYMENT_URL/api/version\"\n</code></pre>\n<p>Then run contract and browser tests. Include assertions that expose configuration mistakes, such as the environment name, non-sensitive database marker, and build commit. Never return secrets or full binding details from a debug endpoint.</p>\n<p>For agent-generated applications, add adversarial checks. Try a production account identifier. Attempt a write outside the test tenant. Confirm that outbound webhooks resolve to a test receiver. These tests prove containment instead of assuming it.</p>\n<h2>Protect preview URLs with Cloudflare Access</h2>\n<p>Preview URLs are public by default. That may be acceptable for a public documentation change, but it is a poor default for admin screens, unreleased product behavior, internal APIs, or data-backed applications.</p>\n<p>Cloudflare supports custom preview domains and recommends using a dedicated hostname. A wildcard DNS record and certificate can provide addresses such as <code>pr-1842.preview.example.com</code>. Put Cloudflare Access in front of that hostname and authorize the people or service identities that need to review it.</p>\n<p>Access control does not make production data safe. It limits who can reach the preview. You still need separate data bindings, test credentials, and outbound-action controls.</p>\n<p>If an automated browser suite must pass Access, give it a narrowly scoped service token. Keep that token in the test job and rotate it independently of the deployment token.</p>\n<h2>Use preview-specific observability</h2>\n<p>Cloudflare gives previews separate logs, traces, metrics, and Tail Worker behavior. This is valuable when an agent changes error handling or performance-sensitive code. Reviewers can inspect only the traffic for one branch rather than filter a shared staging stream.</p>\n<p>Cloudflare's <a href=\"https://developers.cloudflare.com/workers/previews/test-and-debug/\">testing and debugging guide</a> notes that previews can use full sampling. That makes a short-lived environment a good place to collect a complete trace set, provided the logs do not include credentials or personal data.</p>\n<p>Useful evidence for a pull request includes:</p>\n<ul>\n<li>The exact deployment identifier and commit SHA.</li>\n<li>Test request identifiers that can be found in logs.</li>\n<li>Error count and sampled trace links.</li>\n<li>CPU time and subrequest changes for representative calls.</li>\n<li>A list of bindings used by the preview.</li>\n</ul>\n<p>Avoid turning observability into an unbounded data dump. Attach the evidence needed to review the change, then rely on retention policies and preview deletion.</p>\n<h2>Handle migrations and mutable state separately</h2>\n<p>Schema changes are where disposable environments become genuinely useful. A preview should not run a migration against the production database. For D1, bind a dedicated database or create a disposable one as part of the workflow. For an external database behind Hyperdrive, use a preview database, schema, or tenant that the preview credential cannot escape.</p>\n<p>Seed only the records needed by tests. Synthetic fixtures are safer than a copy of customer data and make failures reproducible. If production-like distribution matters, generate it rather than exporting identities, tokens, or free-form user content.</p>\n<p>Make migrations idempotent where practical. A pull request may deploy several times, and CI retries happen. The preview name is stable while deployments beneath it are not.</p>\n<p>If the application cannot support isolated state, be honest about the limit. Run read-only smoke tests and reserve mutation tests for a controlled environment. A preview URL cannot compensate for an unsafe database topology.</p>\n<h3>Make test data disposable and recognizable</h3>\n<p>Add a preview identifier to every synthetic tenant, object key, and webhook event. This makes accidental cross-environment writes visible and simplifies cleanup. For example, an R2 object path can begin with <code>previews/pr-1842/</code>, while a database tenant can store <code>preview_id = 'pr-1842'</code> under a credential restricted to that tenant.</p>\n<p>Do not depend only on naming. Enforce the boundary with database permissions, separate resource identifiers, or both. An application bug can omit a prefix. A credential that cannot write outside the preview schema turns that bug into a failed test instead of a production incident.</p>\n<p>Fixtures should be small enough to recreate on every deployment when needed. Version the fixture schema with the application and store no real access tokens, email addresses, or free-form customer text. If a bug requires a production-shaped case, reduce and anonymize it before adding it to the test corpus.</p>\n<h2>Clean up closed and abandoned previews</h2>\n<p>Deletion should be triggered by the pull-request close event, but events can fail. Add a scheduled reconciliation job that lists active previews, compares them with open pull requests, and flags or removes stale entries after a grace period.</p>\n<p>Cleanup needs to cover resources the preview workflow created outside Worker Previews. That can include a D1 database, R2 prefix, external test tenant, webhook receiver, or Access policy. Label each resource with the pull-request number and creation time.</p>\n<p>Use a two-stage policy for anything with diagnostic value:</p>\n<ol>\n<li>Disable access and mark the resource expired when the pull request closes.</li>\n<li>Delete it after a short retention window unless an incident or review hold exists.</li>\n</ol>\n<p>This gives engineers time to inspect a failure without leaving old environments reachable indefinitely.</p>\n<h2>Know where previews differ from other Cloudflare workflows</h2>\n<p>Worker Previews are one of three related tools. They solve different problems.</p>\n<table>\n<thead>\n<tr><th>Tool</th><th>Best fit</th><th>Resource behavior</th></tr>\n</thead>\n<tbody>\n<tr><td>Worker Previews</td><td>Branch and pull-request testing</td><td>Named isolation under one Worker, with resource-specific rules</td></tr>\n<tr><td>Version preview URLs</td><td>Inspecting an uploaded production version</td><td>Uses production resources</td></tr>\n<tr><td>Wrangler environments</td><td>Persistent staging, regional, or customer environments</td><td>Separate named Workers with independent configuration</td></tr>\n</tbody>\n</table>\n<p>Use Worker Previews for short-lived review. Use a Wrangler environment when the environment must persist, has a distinct operational owner, or needs fully independent configuration. Use a version preview URL when you are validating the exact version already moving through production deployment controls.</p>\n<p>Cloudflare states that long-lived staging support for previews is planned. Until the documented model changes, do not stretch a pull-request primitive into a permanent environment without understanding its limits.</p>\n<h2>A review checklist for coding-agent previews</h2>\n<p>Before enabling automatic preview deployment, verify these controls:</p>\n<ul>\n<li>Wrangler is 4.135.0 or newer and the version is pinned in the lockfile.</li>\n<li>Preview names come from validated pull-request numbers.</li>\n<li>Forked pull requests cannot access deployment secrets.</li>\n<li>The deployment token cannot modify unrelated account resources.</li>\n<li>Preview variables and secrets are explicit.</li>\n<li>D1, KV, R2, Hyperdrive, Vectorize, queues, and workflows have reviewed boundaries.</li>\n<li>Service bindings do not create an unexpected path to production.</li>\n<li>The test suite targets the immutable deployment URL.</li>\n<li>Human review uses a stable URL protected by Access when needed.</li>\n<li>Logs and traces identify the commit without exposing sensitive values.</li>\n<li>Close-event cleanup and scheduled reconciliation both exist.</li>\n<li>Production promotion remains a separate, human-governed action.</li>\n</ul>\n<p>This design gives coding agents fast feedback while keeping the evidence legible to humans. The same boundary also helps ordinary engineering teams: parallel branches stop fighting over staging, review links remain useful, and the path from commit to observed behavior becomes easier to audit.</p>\n<h2>Sources and freshness</h2>\n<p>This guide was verified on September 23, 2026 against Cloudflare's <a href=\"https://blog.cloudflare.com/worker-previews/\">Worker Previews announcement</a>, <a href=\"https://developers.cloudflare.com/workers/previews/\">preview overview</a>, <a href=\"https://developers.cloudflare.com/workers/previews/configuration/\">configuration reference</a>, <a href=\"https://developers.cloudflare.com/workers/previews/resources/\">resource isolation table</a>, <a href=\"https://developers.cloudflare.com/workers/previews/test-and-debug/\">testing guide</a>, <a href=\"https://developers.cloudflare.com/workers/previews/custom-domains/\">custom-domain guide</a>, <a href=\"https://developers.cloudflare.com/workers/previews/automation-examples/\">automation examples</a>, and <a href=\"https://developers.cloudflare.com/workers/previews/compare-workflows/\">workflow comparison</a>. Limits and binding behavior can change, so check the linked references before standardizing the workflow across a large account.</p>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
