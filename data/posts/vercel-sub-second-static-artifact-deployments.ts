import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "vercel-sub-second-static-artifact-deployments",
  title: "Sub-Second Vercel Deployments for Static Artifacts",
  description: "Use Vercel CLI's sub-second artifact deployment path for small HTML and Markdown outputs, with practical limits, security controls, and lifecycle guidance.",
  date: "2026-09-18",
  readTime: "13 min",
  category: "Hosting & DevOps",
  tags: ["Vercel","Vercel CLI","Static deployment","Coding agents","CI/CD","Developer tools"],
  faqs: [
      {
          "question": "What is a sub-second artifact deployment on Vercel?",
          "answer": "Vercel CLI can detect a small eligible directory of HTML or Markdown artifacts, skip the build step, upload it, and return a live deployment URL in under a second under suitable network conditions."
      },
      {
          "question": "What are the limits for instant artifact deployment?",
          "answer": "Vercel's September 2026 announcement supports directories containing up to 10 HTML or Markdown files with a total size of 5 MB or less. The CLI must be version 59.16.0 or later."
      },
      {
          "question": "Is this the same as vercel deploy --prebuilt?",
          "answer": "No. The prebuilt flow uploads a .vercel/output directory produced by vercel build or another Build Output API process. Instant artifact deployment detects a small static directory and skips a conventional build."
      },
      {
          "question": "Can coding agents use this for preview links?",
          "answer": "Yes. An agent can write a report or prototype to a clean directory, run Vercel CLI, capture the URL from stdout, and attach that URL to its task result. The deployment should still use scoped credentials and an appropriate retention policy."
      },
      {
          "question": "Are generated Vercel deployment URLs private?",
          "answer": "Not necessarily. Generated URLs can be publicly accessible unless Deployment Protection is enabled. Do not deploy sensitive reports until access controls and data handling have been reviewed."
      },
      {
          "question": "When should I use a normal framework deployment instead?",
          "answer": "Use the normal build path when the artifact needs server code, a large asset set, a framework compilation step, environment-dependent generation, complex routing, or more than the instant deployment limits allow."
      }
  ],
  coverImage: "/images/vercel-sub-second-static-artifact-deployments.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/vercel-sub-second-static-artifact-deployments.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/vercel-sub-second-static-artifact-deployments.webp\" alt=\"Sub-Second Vercel Deployments for Static Artifacts\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>Most preview deployments spend time doing work a static report does not need. The platform installs dependencies, detects a framework, runs a build, packages output, and creates a deployment even when the input is one HTML file and a small stylesheet.</p>\n<p>Vercel CLI now has a shorter path. Version 59.16.0 and later can recognize an eligible directory of HTML or Markdown artifacts, skip the build step, and return a live URL. Vercel's launch example completed in 802 milliseconds.</p>\n<p>This is useful for coding-agent reports, disposable prototypes, benchmark results, design reviews, and small documentation previews. It is deliberately constrained: up to 10 supported files and 5 MB total. The constraints are a feature because they keep the fast path predictable.</p>\n<p>The result is still a deployment. It has a URL, access policy, retention behavior, storage footprint, and possible production alias. Fast publishing should not turn temporary output into unmanaged infrastructure.</p>\n<h2>What Vercel Added</h2>\n<p>Vercel announced <a href=\"https://vercel.com/changelog/sub-second-artifact-deployments-are-now-supported-in-vercel-cli\">sub-second artifact deployments</a> on September 17, 2026. Run <code>vercel deploy</code> against a small directory, and the CLI automatically determines whether it can use the instant path.</p>\n<p>Eligible directories currently have:</p>\n<ul>\n<li>No more than 10 files</li>\n<li>A combined size of 5 MB or less</li>\n<li>HTML, HTM, or Markdown files</li>\n<li>Vercel CLI 59.16.0 or later</li>\n</ul>\n<p>Vercel creates a project when needed, uploads the valid artifacts, and returns a deployment URL. Passing <code>--prod</code> can create a production deployment and assign the configured production domain.</p>\n<pre><code class=\"language-bash\">npx vercel@latest deploy ./report\n</code></pre>\n<p>The general <a href=\"https://vercel.com/docs/cli/deploy\">Vercel CLI deployment documentation</a> notes that standard output is always the deployment URL. That makes the command easy to compose into scripts and agent workflows.</p>\n<pre><code class=\"language-bash\">deployment_url=\"$(npx vercel@latest deploy ./report --yes)\"\nprintf '%s\\n' \"$deployment_url\"\n</code></pre>\n<p>Keep diagnostic output on standard error and treat standard output as data. A script that scrapes the prettiest line from interactive logs will break eventually.</p>\n<h2>Instant Artifacts Are Not Prebuilt Deployments</h2>\n<p>Vercel already supports <code>vercel build</code> followed by <code>vercel deploy --prebuilt</code>. That flow produces <code>.vercel/output</code> using the Build Output API, then uploads the prepared result.</p>\n<pre><code class=\"language-bash\">vercel build\nvercel deploy --prebuilt\n</code></pre>\n<p>Prebuilt deployment is useful when you want to build locally, inspect the output, avoid sharing source, or use a custom build pipeline. It still involves a build somewhere.</p>\n<p>The new artifact path is smaller. You point the CLI at a directory that is already the thing you want to publish. There is no framework build to transfer or execute. For a one-page report, that can remove most of the latency and most of the configuration.</p>\n<p>Use <code>--prebuilt</code> when your deployment has the full Vercel Build Output structure. Use the instant artifact path when the deliverable is a tiny set of static HTML or Markdown files.</p>\n<h2>A Minimal Static Report</h2>\n<p>Create a clean output directory. Do not point the deploy command at the repository root, where credentials, source maps, fixtures, and unrelated files may be present.</p>\n<pre><code class=\"language-text\">report/\n├── index.html\n└── details.html\n</code></pre>\n<p>The HTML should be self-contained or reference only assets that will remain available. A simple report can embed a small stylesheet and avoid JavaScript entirely.</p>\n<pre><code class=\"language-html\">&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n  &lt;head&gt;\n    &lt;meta charset=\"utf-8\" /&gt;\n    &lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" /&gt;\n    &lt;meta name=\"robots\" content=\"noindex,nofollow\" /&gt;\n    &lt;title&gt;Accessibility review&lt;/title&gt;\n    &lt;style&gt;\n      body { max-width: 760px; margin: 3rem auto; padding: 0 1rem; font: 16px/1.6 system-ui; }\n      .pass { color: #176b35; }\n      .fail { color: #a51d2d; }\n      code { background: #f3f4f6; padding: .1rem .3rem; }\n    &lt;/style&gt;\n  &lt;/head&gt;\n  &lt;body&gt;\n    &lt;main&gt;\n      &lt;h1&gt;Accessibility review&lt;/h1&gt;\n      &lt;p&gt;Generated from commit &lt;code&gt;4f89c2a&lt;/code&gt;.&lt;/p&gt;\n      &lt;h2&gt;Summary&lt;/h2&gt;\n      &lt;p class=\"pass\"&gt;31 checks passed.&lt;/p&gt;\n      &lt;p class=\"fail\"&gt;2 checks need review.&lt;/p&gt;\n    &lt;/main&gt;\n  &lt;/body&gt;\n&lt;/html&gt;\n</code></pre>\n<p><code>noindex</code> is useful for temporary reports, but it is not access control. Anyone who can reach an unprotected URL may still read the page.</p>\n<h2>A Safe Coding-Agent Publishing Pattern</h2>\n<p>A coding agent can produce an artifact and publish it as one step in a larger task. Keep generation, validation, and deployment separate.</p>\n<pre><code class=\"language-text\">task request\n  -&gt; agent writes files to ./artifact\n  -&gt; validator checks type, count, size, and forbidden content\n  -&gt; human or policy approves publication\n  -&gt; Vercel CLI deploys ./artifact\n  -&gt; workflow records URL and deployment ID\n</code></pre>\n<p>The validator should reject symlinks, hidden files, unexpected extensions, oversized files, embedded credentials, and references to local-only resources. It should confirm that <code>index.html</code> exists when the output is meant to open as a site.</p>\n<pre><code class=\"language-bash\">artifact_dir=\"./artifact\"\n\ntest -d \"$artifact_dir\"\ntest -f \"$artifact_dir/index.html\"\n\nfile_count=\"$(find \"$artifact_dir\" -type f | wc -l | tr -d ' ')\"\ntest \"$file_count\" -le 10\n\nsize_bytes=\"$(du -sk \"$artifact_dir\" | awk '{print $1 * 1024}')\"\ntest \"$size_bytes\" -le 5242880\n\nfind \"$artifact_dir\" -type l -print -quit | grep -q . &amp;&amp; exit 1\n</code></pre>\n<p>Use a more precise validator in production, especially on systems where <code>du</code> and <code>find</code> behave differently. The point is to validate the exact publication directory instead of trusting the agent's statement that it only wrote a report.</p>\n<h2>Build a CI Workflow Around the Artifact Directory</h2>\n<p>Keep the deployment job narrow enough that its review is boring. The job should receive a known artifact, validate it again, and deploy only that directory. It should not check out unrelated branches, install arbitrary project dependencies, or give the report generator a production token.</p>\n<pre><code class=\"language-yaml\">name: Publish static review artifact\n\non:\n  workflow_dispatch:\n    inputs:\n      source_run_id:\n        description: \"Approved workflow run containing the artifact\"\n        required: true\n\npermissions:\n  contents: read\n  actions: read\n\njobs:\n  deploy:\n    runs-on: ubuntu-26.04\n    environment: artifact-previews\n    steps:\n      - name: Download approved artifact\n        run: |\n          # Download only from the expected workflow and repository.\n          # Extract into ./artifact without executing its contents.\n          true\n\n      - name: Validate files\n        run: ./scripts/validate-static-artifact.sh ./artifact\n\n      - name: Deploy preview\n        env:\n          VERCEL_TOKEN: ${{ secrets.VERCEL_ARTIFACT_TOKEN }}\n        run: |\n          npx vercel@59.16.0 deploy ./artifact \\\n            --yes \\\n            --token \"$VERCEL_TOKEN\" &gt; deployment-url.txt\n\n      - uses: actions/upload-artifact@v5\n        with:\n          name: deployment-record\n          path: deployment-url.txt\n</code></pre>\n<p>The comments stand in for repository-specific artifact retrieval. Do not copy an untrusted archive into a working directory and rely on <code>unzip</code> defaults. Reject absolute paths and <code>..</code> traversal during extraction.</p>\n<p>Use an environment approval if publishing is consequential. A preview project token should not be able to change the main website's production domain.</p>\n<h2>Handle Failure as a First-Class Result</h2>\n<p>Fast deployment can fail before or after upload: CLI authentication can expire, file eligibility can change, network access can fail, the project can hit a limit, or protection settings can make the returned URL inaccessible to the intended reviewer.</p>\n<p>Return a structured failure instead of asking the agent to repeat the command indefinitely.</p>\n<pre><code class=\"language-json\">{\n  \"status\": \"failed\",\n  \"stage\": \"deploy\",\n  \"retryable\": false,\n  \"reason\": \"artifact exceeds 5 MB instant-deployment limit\",\n  \"artifactBytes\": 6812400,\n  \"suggestedPath\": \"normal-build\"\n}\n</code></pre>\n<p>Classify limits as non-retryable until the artifact or deployment path changes. Classify a transient network response as retryable with a small attempt ceiling and backoff. Store stderr privately, since command errors can expose project names or paths that do not belong in a public report.</p>\n<p>After receiving a URL, verify it with an authenticated request if protection is enabled. Confirm the expected title or provenance marker instead of treating any HTTP 200 page as success.</p>\n<h2>Capture Provenance With the URL</h2>\n<p>A live URL is not enough to reproduce a report. Store the inputs and versions that created it.</p>\n<p>Record:</p>\n<ul>\n<li>Source repository and commit</li>\n<li>Task or pull-request identifier</li>\n<li>Agent and model configuration if AI generated the artifact</li>\n<li>Generator version</li>\n<li>Validation result</li>\n<li>Deployment URL and ID</li>\n<li>Creation and expiry date</li>\n</ul>\n<p>Embed a small provenance block in the page when appropriate. Do not expose private prompt content or credentials. A commit SHA and timestamp are often enough for a reviewer to connect the page to its source.</p>\n<p>Vercel's <a href=\"https://vercel.com/docs/deployments/generated-urls\">generated URL documentation</a> explains that each deployment receives a unique URL and that CLI deployments also receive project and author-related aliases. Unique URLs are convenient for immutable review links, while an alias can point to the newest version.</p>\n<p>Choose deliberately. A pull-request comment should usually link to the immutable deployment. A team dashboard may use a stable alias.</p>\n<h2>Preview and Production Mean Different Things</h2>\n<p>Running <code>vercel deploy</code> creates a preview deployment by default. Running with <code>--prod</code> creates a production deployment and may move production domains.</p>\n<p>Do not add <code>--prod</code> to an automated agent simply because the output is static. Production aliasing is an external side effect. It should require explicit policy or approval.</p>\n<p>Vercel supports a staged production flow:</p>\n<pre><code class=\"language-bash\">vercel deploy --prod --skip-domain\nvercel promote &lt;deployment-id-or-url&gt;\n</code></pre>\n<p>The first command creates a production-targeted deployment without immediately assigning the production domain. The second promotes it after review. Vercel documents this flow in its <a href=\"https://vercel.com/docs/cli/deploying-from-cli\">CLI deployment guide</a>.</p>\n<p>For disposable reports, stay with preview deployments unless a permanent public URL is part of the requirement.</p>\n<h2>Protect Reports That Contain Internal Information</h2>\n<p>Generated deployment URLs should be treated as public unless protection is configured. A hard-to-guess URL is not a permission system.</p>\n<p>Vercel's <a href=\"https://vercel.com/docs/deployment-protection\">Deployment Protection documentation</a> describes Vercel Authentication, password protection on supported plans, and trusted IP controls for Enterprise. Standard Protection can restrict generated deployment URLs while leaving a production custom domain available.</p>\n<p>Review the protection policy before the first automated deployment. Do not wait until an agent has published an incident report with customer identifiers.</p>\n<p>Sensitive material may not belong on this path at all. Authentication reduces casual access, but deployment storage, logs, backups, integrations, and recovery behavior still need to satisfy the organization's data policy.</p>\n<h2>Keep Deployment Tokens Narrow</h2>\n<p>An agent that can run <code>vercel deploy --prod</code> with a team-wide token may be able to affect more than one temporary report. Use a dedicated project and a credential scoped as narrowly as the platform allows.</p>\n<p>Separate projects by trust level. Public demonstrations, internal reports, and customer-specific previews should not share one project merely because the files are small.</p>\n<p>Set a budget and rate limit around automated publishing. A broken loop can create hundreds of deployments in minutes. The upload is fast enough that the failure becomes an inventory problem before anyone notices the command is repeating.</p>\n<p>Use an idempotency key in the surrounding workflow, even if the CLI itself creates a new deployment per call. A source commit, task ID, and artifact digest can identify an existing successful publication. If the same request arrives again, return the stored URL or make an explicit new-version decision.</p>\n<p>Do not use a broad personal token for unattended deployment. The automation should remain valid when one employee leaves and should have no authority beyond the artifact project and required environment.</p>\n<h2>Plan Retention Before the Queue Grows</h2>\n<p>Every deployment consumes deployment storage and remains reachable for as long as the retention policy and protection exceptions keep it.</p>\n<p>Vercel's <a href=\"https://vercel.com/docs/deployment-retention\">deployment retention documentation</a> lets projects define retention periods for canceled, errored, preview, and production deployments. Some deployments remain protected by exceptions, such as current production or aliased deployments.</p>\n<p>Hobby accounts receive 10 GB of deployment storage. Vercel changed Hobby retention in September 2026 so older deployments outside the protected set are deleted sooner when the account exceeds its allowance. A stable branch or custom alias can still keep a deployment active.</p>\n<p>For automated artifacts, define an expiry policy that matches their value. A pull-request report rarely needs to survive for years. A compliance artifact may require a different storage system with explicit retention guarantees.</p>\n<p>Avoid creating one Git branch per generated report. That can generate branch aliases and preview deployments that stay protected. Use CLI deployments to a dedicated artifact project and retain the source artifact in the system of record.</p>\n<h2>Know When the Fast Path Is the Wrong Path</h2>\n<p>The 10-file and 5 MB limits rule out many applications. Use a normal project build when you need:</p>\n<ul>\n<li>Framework compilation or server rendering</li>\n<li>Functions, middleware, or API routes</li>\n<li>Large images, fonts, or JavaScript bundles</li>\n<li>Environment-dependent generation</li>\n<li>Complex rewrites, redirects, or headers</li>\n<li>More than a small collection of documents</li>\n</ul>\n<p>Do not split a real application into ten strange files to keep an impressive deployment time. The fast path is for artifacts that are naturally small.</p>\n<p>Markdown support is convenient for plain reports, but rendering expectations should be tested before relying on it for a client-facing page. HTML gives you direct control over semantics, styling, metadata, and accessibility.</p>\n<h2>Keep the Artifact Accessible and Self-Contained</h2>\n<p>A report that deploys quickly can still be unpleasant to review. Use semantic headings, a single main landmark, sufficient color contrast, keyboard-accessible controls, and useful link text. Include a viewport declaration and test the page at a narrow width.</p>\n<p>Avoid loading a large framework from a CDN for a static table. It adds another availability and security dependency while working against the reason the artifact qualified for the fast path.</p>\n<p>When external assets are necessary, pin or control them. A report that references an image in a temporary CI URL will become incomplete after the job expires. A stylesheet fetched from a mutable third-party URL can change the meaning or appearance of an archived review.</p>\n<p>For diagrams, inline a modest SVG only after sanitizing it. SVG can contain scripts, links, and external references. Treat agent-generated markup as untrusted until a sanitizer and a content security policy say otherwise.</p>\n<p>A small Content Security Policy can reduce risk for reports that need no scripts:</p>\n<pre><code class=\"language-html\">&lt;meta\n  http-equiv=\"Content-Security-Policy\"\n  content=\"default-src 'none'; style-src 'unsafe-inline'; img-src 'self' data:; base-uri 'none'; form-action 'none'\"\n/&gt;\n</code></pre>\n<p>Test the policy against the report. If a feature needs broader access, add the narrow source it requires instead of replacing the policy with <code>default-src *</code>.</p>\n<h2>Use Stable Aliases Sparingly</h2>\n<p>An immutable deployment URL is useful evidence because it continues to identify one artifact. A stable alias is useful for \"latest report\" workflows, but it changes what the same URL means over time.</p>\n<p>Keep both when the use case needs them. The review record can point to the immutable URL, while a team bookmark points to the alias. Record which deployment currently owns the alias.</p>\n<p>Aliases also affect cleanup. Vercel retention policies can protect aliased deployments from deletion. An automation that assigns a new custom alias for every task may accidentally convert disposable reports into retained storage.</p>\n<p>Prefer one rotating alias per report class, such as <code>latest-accessibility-review</code>, and immutable deployment URLs for individual runs. Remove obsolete custom aliases when the workflow ends.</p>\n<h2>Do Not Confuse Speed With Cacheability</h2>\n<p>Skipping a build shortens creation time. It does not determine how browsers or the CDN cache the content. Test response headers and update behavior if the artifact is consumed by automation.</p>\n<p>An immutable URL can use long-lived caching because its contents should not change. A stable alias that moves between deployments needs cache behavior that lets clients observe the new target. Avoid adding client-side cache workarounds until you have inspected the actual headers Vercel returns for the deployment and alias.</p>\n<p>If the report includes private data, browser and intermediary caching belong in the threat model. Deployment authentication controls access to a request; it does not automatically erase content already cached by an authorized client.</p>\n<h2>Useful Automation Examples</h2>\n<p>Pull requests can receive a static quality report after tests finish. A benchmark job can publish its task summary and link to raw artifacts stored elsewhere. A design agent can publish a small HTML prototype for review. A migration script can create a before-and-after route report.</p>\n<p>The common trait is bounded output. The deployment is the final presentation layer, not the build system or source of truth.</p>\n<p>A reliable workflow returns a structured result:</p>\n<pre><code class=\"language-json\">{\n  \"deploymentUrl\": \"https://example.vercel.app\",\n  \"sourceCommit\": \"4f89c2a\",\n  \"artifactFiles\": 2,\n  \"artifactBytes\": 18432,\n  \"visibility\": \"protected-preview\",\n  \"expiresAfterDays\": 14\n}\n</code></pre>\n<p>That record can be audited and deleted. A URL pasted into a chat message cannot.</p>\n<h2>A Small Deployment Still Needs Ownership</h2>\n<p>Sub-second deployment removes ceremony from sharing a small artifact. It does not remove the need to decide what was published, who can read it, how long it stays, and whether the URL represents preview or production.</p>\n<p>Keep the pipeline narrow: generate into a clean directory, validate the files, require approval for production, protect non-public content, record provenance, and expire old deployments. With those controls in place, a coding agent can turn a result into a reviewable page almost as quickly as it can write the final sentence.</p>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
