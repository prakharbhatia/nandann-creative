import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "fastapi-static-files-on-the-cdn-deployment-architecture",
  title: "FastAPI Static Files on the CDN: The Deployment Architecture Behind Faster Python Frontends",
  description: "Architectural guide to serving FastAPI static assets via a CDN with fingerprinting, caching, and deployment workflows.",
  date: "2026-09-16",
  readTime: "16 min",
  category: "Engineering",
  tags: ["FastAPI","CDN","StaticFiles","Asset Fingerprinting","Caching","Deployment","Web Performance"],
  faqs: [
      {
          "question": "Why should you serve static assets from a CDN when using FastAPI?",
          "answer": "Static assets dominate the 1st-party surface area of a web app. A CDN reduces latency by delivering assets from edge locations closer to users, enables parallel downloads, and shares TLS termination across requests. The API server remains focused on dynamic content while the CDN handles the static assets."
      },
      {
          "question": "What is asset fingerprinting and how does it help with caching?",
          "answer": "Fingerprinting adds a content-based hash to asset filenames (for example, app.[hash].js), which enables long-lived caches on the CDN. A manifest maps logical asset names to fingerprinted filenames and templates reference the latest versions, ensuring cache busting only when content changes."
      },
      {
          "question": "How should assets and manifests be organized for CDN deployment?",
          "answer": "Emit fingerprinted assets to a CDN origin (such as S3) and reference the fingerprinted URLs from templates or bootstrapping code. Use a manifest to map logical asset names to hashed filenames so the HTML always loads the correct files."
      },
      {
          "question": "What does a deployment workflow look like for CDN-backed assets?",
          "answer": "Build frontend assets, run fingerprinting to produce hashed filenames, create or update a manifest, and update templates to reference the new files. Purge old assets as needed to ensure users fetch the latest assets and keep the CDN in sync with deployments."
      },
      {
          "question": "What caching headers should fingerprinted assets use?",
          "answer": "Fingerprint assets can be cached aggressively with Cache-Control: public, max-age=31536000, immutable. HTML shells or non-fingerprinted assets should use shorter TTLs or revalidate headers to reflect freshness."
      },
      {
          "question": "How should purges be handled when assets change?",
          "answer": "Purge should be an integral part of deployment: purge only the changed fingerprinted URLs, automate purges in the deployment pipeline, and use a manifest-based approach to invalidate those specific assets. If granular purging isn’t available, plan a controlled purge window or consider a blue/green origin swap to minimize user impact."
      },
      {
          "question": "What cross-origin and TLS considerations are important?",
          "answer": "Asset requests should come from the CDN domain while API requests go to the origin; configure CORS appropriately and ensure TLS termination is correctly configured at the CDN or origin. Maintain consistent TLS coverage and apply security headers like CSP and HSTS to protect users and assets."
      }
  ],
  coverImage: "/images/fastapi-static-files-on-the-cdn-deployment-architecture.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/fastapi-static-files-on-the-cdn-deployment-architecture.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/fastapi-static-files-on-the-cdn-deployment-architecture.webp\" alt=\"FastAPI Static Files on the CDN: The Deployment Architecture Behind Faster Python Frontends\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<h2>Overview and Goals</h2>\n<h3>Why serving static assets via a CDN improves frontend performance</h3>\n<ul>\n<li>Static assets (JavaScript, CSS, images) dominate the 1st-party surface area of a web app. If these assets are served from the origin, you’re competing with API latency, TLS handshakes, and potentially suboptimal network paths.</li>\n<li>A CDN materially reduces latency by serving assets from edge locations closer to users, enabling parallel downloads, and amortizing TLS termination across many requests.</li>\n<li>The API server (FastAPI) remains focused on dynamic content and API endpoints, while the CDN handles the heavy lifting for static assets.</li>\n</ul>\n<h3>Typical deployment architecture</h3>\n<ul>\n<li>Origin: FastAPI application running on an ASGI server (e.g., Uvicorn, Hypercorn) behind a load balancer or reverse proxy.</li>\n<li>CDN: A public caching layer that hosts static assets (JS/CSS/Images) and serves them from a dedicated asset domain.</li>\n<li>Optional components: Reverse proxy or API gateway at the edge; TLS termination at the CDN or origin; and a storage service as the CDN origin (for example, object storage with a CDN front).</li>\n<li>Data flow pattern: Client requests go to the CDN edge for static assets, which may fetch from the CDN origin as needed, while API traffic goes to the origin FastAPI API. In practice, asset requests hit the CDN edge; API calls hit the origin or an API gateway.</li>\n</ul>\n<h3>What this guide covers (scope and boundaries)</h3>\n<ul>\n<li>How to structure assets and references so the CDN can serve them efficiently.</li>\n<li>Asset fingerprinting and versioning for reliable cache busting.</li>\n<li>Deployment workflows that keep the API code clean and the assets consistently reachable from the CDN.</li>\n<li>Caching strategies, security headers, and observability relevant to a FastAPI + CDN deployment.</li>\n<li>Concrete patterns and example structures you can adapt.</li>\n</ul>\n<hr />\n<h2>Architecture Components</h2>\n<h3>API server (FastAPI) details</h3>\n<ul>\n<li>FastAPI serves API endpoints on an origin domain (e.g., https://api.example.com). The API itself should be agnostic to where static assets come from, as long as the HTML templates reference the CDN-hosted assets.</li>\n<li>Development vs production: in development you can rely on FastAPI's StaticFiles to serve local assets. In production, you emit assets to a CDN origin and reference the CDN URLs from your templates or client-side code.</li>\n</ul>\n<h3>CDN details</h3>\n<ul>\n<li>The CDN hosts static assets under a versioned/hashed path (e.g., https://cdn.example.com/static/app.abc123.js).</li>\n<li>Cache behavior: long TTLs for fingerprinted assets, with a mechanism to purge or invalidate upon asset updates.</li>\n<li>Cache keys: ensure the CDN uses a stable path that includes the fingerprint to avoid cache misses on every deploy.</li>\n</ul>\n<h3>Reverse proxy / load balancer</h3>\n<ul>\n<li>A common pattern is: Client -> CDN -> Origin Load Balancer -> FastAPI instances.</li>\n<li>TLS termination can happen at the CDN or at the origin. If TLS termination happens at the CDN, ensure the origin connection is secure (e.g., TLS to FastAPI).</li>\n</ul>\n<h3>Data flow and cross-origin considerations</h3>\n<ul>\n<li>Asset requests should be served from the CDN domain. API requests should go to the origin domain.</li>\n<li>If the CDN domain and API domain differ, configure appropriate CORS policies and TLS settings to avoid mixed content and cross-origin issues.</li>\n<li>Consider a single TLS certificate for both domains via SANs or a certificate per domain with proper redirection rules.</li>\n</ul>\n<hr />\n<h2>Static Assets Handling</h2>\n<h3>Development vs Production</h3>\n<ul>\n<li>Development: Use FastAPI's StaticFiles to serve local assets for rapid iteration.</li>\n<li>Production: Emit assets to a CDN origin, then reference CDN URLs from HTML templates or frontend code.</li>\n</ul>\n<h3>Asset fingerprinting and versioning</h3>\n<ul>\n<li>Fingerprinting: include a content-based hash in the asset filename (for example, app.[hash].js). This enables long-lived caches on the CDN because content changes only when the file contents change.</li>\n<li>Versioned URLs: when you deploy a new frontend build, generate a manifest that maps logical asset names to fingerprinted filenames, then update templates or a manifest-driven loader to reference the correct files.</li>\n<li>Reference management: templates or frontend bootstrapping code should be able to resolve the latest fingerprinted asset filenames from the manifest automatically.</li>\n</ul>\n<h3>Asset upload and origin storage</h3>\n<ul>\n<li>Common patterns:</li>\n</ul>\n  - Build step outputs fingerprinted assets to a local directory.\n  - An upload step pushes the assets to a CDN origin, often backed by object storage (e.g., S3) or a dedicated asset bucket.\n  - The deployment process updates references in HTML templates or a manifest so the next user load uses the latest assets.\n<h3>Example directory layout (production-oriented)</h3>\n<ul>\n<li>/frontend/</li>\n</ul>\n  - /dist/\n    - app.abc123.js\n    - styles.efg456.css\n  - /static/      (optional during dev)\n<ul>\n<li>/templates/</li>\n</ul>\n  - index.html (references https://cdn.example.com/static/app.abc123.js)\n<ul>\n<li>/app/</li>\n</ul>\n  - main.py (FastAPI app)\n<ul>\n<li>/deploy/</li>\n</ul>\n  - scripts/\n    - upload_assets.sh\n    - update_manifest.sh\n<ul>\n<li>/assets/</li>\n</ul>\n  - manifest.json (maps logical names to fingerprinted files)\n<h2>Caching Strategy</h2>\n<h3>Cache headers and TTLs</h3>\nFingerprinted assets deserve aggressive caching on the CDN. The filename change itself acts as a natural cache-buster, so you can safely push a one-year TTL for these files. When the content changes, the fingerprint changes, and the new URL replaces the old one without backfilling the old asset.\n<p>For HTML and non-fingerprinted assets, keep shorter TTLs or revalidate headers. If you serve them from origin or from a CDN with dynamic behavior, you want a policy that refreshes frequently enough to reflect updates but not so aggressively that you negate the benefits of a cache.</p>\n<p>Concrete guidance: <ul> <li>Fingerprinted assets (JS, CSS, images with content hashes): Cache-Control: public, max-age=31536000, immutable</li> <li>HTML shells and non-fingerprinted assets: Cache-Control: no-cache, must-revalidate or a short max-age (seconds to a few minutes)</li> <li>Busting headers: when you deploy a new fingerprint, the asset URL changes and a fresh fetch occurs automatically.</li> </ul></p>\n<pre>\n// Nginx-style example (conceptual)\nmap $uri $is_fp {\n  ~* \\.[0-9a-f]{8,}\\.[^./]+$ 1;\n  default 0;\n}\nserver {\n  location / {\n    if ($is_fp) {\n      add_header Cache-Control \"public, max-age=31536000, immutable\";\n    } else {\n      add_header Cache-Control \"no-cache, must-revalidate\";\n    }\n  }\n}\n</pre>\n<p>In practice you’ll want to reflect these headers in your origin responses or via the CDN’s policy engine. A small automation rule that applies to all fingerprinted assets and a separate rule for index.html or non-fingerprinted assets makes the policy easy to audit and reason about.</p>\n<h3>Purge strategies</h3>\nTreat purge as an integral part of deployment, not a reaction to broken caches. The goal is to invalidate only what changes while preserving the rest of the cache.\n<p>Key ideas: <ul> <li>Purge on asset updates: whenever the manifest maps a logical asset to a new fingerprint, purge the old fingerprinted URL. Purges should target specific URLs rather than broad, all-assets purges.</li> <li>Automate purges in the deployment pipeline: a step should read the manifest, derive the list of changed URLs, and issue CDN purge requests.</li> <li>Use a manifest-based purge: invalidate only the changed fingerprinted assets, not every asset on the domain. This keeps latency low for users fetching unchanged content.</li> </ul></p>\n<p>Vendor-friendly pointers (illustrative only; adapt to your stack): <ul> <li>CloudFront: create invalidation paths for each changed fingerprinted asset (or a small, minimal set if possible).</li> <li>Cloudflare: purge by URL for each changed asset, or use selective purge where supported.</li> <li>Fastly: purge by surrogate key or by URL, depending on your setup and how you tag objects.</li> </ul></p>\n<p>Automation example (bash sketch): <pre> // Pseudo-script outline: purge only fingerprinted assets that changed MANIFEST=manifest.json CHANGED=$(jq -r 'to_entries[] | select(.value | test(\"\\\\.[0-9a-f]{8,}\\\\.\")) | .key' $MANIFEST)</p>\n<p>for logical in $CHANGED; do   hashed=$(jq -r --arg key \"$logical\" '.[$key]' \"$MANIFEST\")   url=\"/static/$hashed\"   # Call your CDN purge API here with the URL   echo \"Purging $url\"   # curl -X POST \"https://cdn.example.com/purge\" -d \"{\\\"files\\\":[\\\"$url\\\"]}\" done </pre></p>\n<p>If you cannot purge granularity for a given CDN, plan a controlled purge window around deploys or use a blue/green strategy that swaps the CDN origin. The trade-off is a brief period where some users may still fetch the old fingerprinted asset until the purge takes effect, so align purge timing with your monitoring and health checks.</p>\n<h3>Purge vs TTL decisions</h3>\n<ul>\n<li>Fingerprinted assets: rely on the fingerprint for cache busting; you can lean on long TTLs, and purges become a safety net for edge cases.</li>\n<li>Non-fingerprinted assets: if the CDN lacks granular purge, plan a controlled purge window aligned with deployment canaries, or consider a blue/green origin swap to swap caches cleanly.</li>\n<li>HTML and index pages: use short TTLs or revalidate policies to keep the shell fresh while static assets remain cached.</li>\n</ul>\n<hr />\n<h2>Routing and URL Structure</h2>\n<h3>Example URLs</h3>\n<ul>\n<li>API: https://api.example.com/health, https://api.example.com/users</li>\n<li>Static assets: https://cdn.example.com/static/app.abc123.js, https://cdn.example.com/static/styles.efg456.css</li>\n<li>Bootstrapping HTML: the HTML that references CDN assets should pull in the fingerprinted files, often via a manifest or template context loader.</li>\n</ul>\n<p>Canonicalization and clarity matter here. Separate API paths from static assets, keep a predictable structure for assets, and ensure the HTML references always point to the fingerprinted versions.</p>\n<pre>\n// Manifest example (JSON)\n{\n  \"app.js\": \"app.abc123.js\",\n  \"styles.css\": \"styles.efg456.css\",\n  \"logo.png\": \"logo.hijk789.png\"\n}\n</pre>\n<h3>Cross-origin considerations</h3>\n<ul>\n<li>If assets load from a different domain than the API, ensure CORS headers on the API do not block asset loading and the CDN serves assets with proper cache-control headers.</li>\n<li>Keep TLS coverage consistent across domains to avoid mixed content warnings and to simplify policy enforcement.</li>\n</ul>\n<p>CORS is usually straightforward for static assets, but API endpoints may require precise control. If you serve assets via a different domain, consider a wildcard or explicit origins in your policy, then test across common browsers.</p>\n<h3>TLS termination and domains</h3>\n<ul>\n<li>Decide whether TLS ends at the CDN edge or at the origin. Both patterns work, but the key is that public-facing URLs resolve correctly and security policies (CSP, HSTS, etc.) remain consistent.</li>\n<li>If you terminate at the CDN edge, ensure proper certificate management and SNI support. If at the origin, keep end-to-end encryption and align with your certificate strategy.</li>\n</ul>\n<p>To reduce risk, set strict HSTS policies and ensure you have a robust CSP that aligns with the asset origins. Keep redirects minimal and deterministic to avoid surprising clients during deployments.</p>\n<hr />\n<h2>Deployment Workflow</h2>\n<h3>Build steps</h3>\n<ul>\n<li>Build frontend assets: compile, minify, and bundle.</li>\n<li>Run fingerprinting to produce content-hashed filenames.</li>\n<li>Create or update a manifest mapping logical asset names to hashed file names.</li>\n<li>Validate that the manifest is stored where templates can access it at runtime.</li>\n</ul>\n<p>The build should be a repeatable, auditable process with explicit artifact names and provenance. Treat the manifest as a source of truth for the delivered HTML.</p>\n<h3>Asset fingerprinting</h3>\n<ul>\n<li>Generate a manifest like:</li>\n</ul>\n  - {\"app.js\": \"app.abc123.js\", \"styles.css\": \"styles.efg456.css\"}\n<ul>\n<li>Use the manifest to update template references so the delivered HTML pulls in the correct asset URLs.</li>\n<li>Validate that every asset reference in your HTML templates points to a fingerprinted asset (or to a string that resolves to one).</li>\n</ul>\n<pre>\n// Minimal manifest example\n{\n  \"app.js\": \"app.abc123.js\",\n  \"styles.css\": \"styles.efg456.css\"\n}\n</pre>\n<p>The manifest should be produced by your build system and consumed by your templating layer during deploys. A small integrity check that renders the final HTML and the asset URLs can catch mismatch early.</p>\n<h3>Deployment checks</h3>\n<ul>\n<li>Validate that all asset URLs point to the CDN domain and that the manifest is wired into the HTML templates.</li>\n<li>Smoke test: load the main page, verify that the correct fingerprinted assets load (check network activity in dev tools or automated checks).</li>\n<li>Run a quick health check on the API and on a basic page load to detect obvious integration issues before users hit the site.</li>\n</ul>\n<p>Automated checks should confirm that fingerprinted filenames appear in the HTML markup and that the static asset requests resolve to the CDN with the expected headers.</p>\n<h3>Rollback considerations</h3>\n<ul>\n<li>Maintain a previous manifest and asset set so you can revert quickly if the new assets fail.</li>\n<li>Ensure the CDN can revert to previous versions without downtime by restoring or re-pointing to the prior fingerprinted assets.</li>\n<li>Keep a simple rollback plan: revert manifest to the previous commit, re-deploy, and rerun tests. Rollback should be deterministic and fast.</li>\n</ul>\n<p>To minimize risk, tie rollbacks to both the manifest and the origin versioned assets. Have a clear plan to switch back to the prior fingerprinted set and to reroute traffic while the issue is diagnosed.</p>\n<hr />\n<h2>Security and Headers</h2>\n<h3>CORS</h3>\n<ul>\n<li>If the frontend and API are on different domains, configure CORS on the API origin to allow safe data fetching by the frontend while keeping asset delivery unblocked. Ensure the CDN path for assets remains fast and uninterrupted.</li>\n</ul>\n<h3>TLS, HSTS, and security headers</h3>\n<ul>\n<li>Use TLS for both API and CDN asset delivery.</li>\n<li>Apply security headers appropriate to your app (for example, Content-Security-Policy, X-Content-Type-Options, X-Frame-Options). Tune values to balance security with practicality.</li>\n<li>Consider enabling HSTS to prevent protocol downgrade attacks when serving over HTTPS.</li>\n</ul>\n<h3>Hotlink protection</h3>\n<ul>\n<li>Some CDNs offer hotlink protection to stop other sites from embedding your assets. Enable if needed, and specify safe domains if cross-origin embedding is required.</li>\n</ul>\n<hr />\n<h2>Observability and Performance Metrics</h2>\n<h3>Core frontend metrics to monitor</h3>\n<ul>\n<li>LCP (Largest Contentful Paint): measure the time to render the largest visible asset, often the main JS bundle or a large image.</li>\n<li>TTI (Time to Interactive): time until the page becomes fully usable.</li>\n<li>CLS (Cumulative Layout Shift): stability of the page as assets load.</li>\n<li>Add a note: consider per-route variations and real-user monitoring data to separate meaningful differences from noise.</li>\n</ul>\n<h3>Asset load waterfalls</h3>\n<ul>\n<li>Track how asset requests load over time and how they interact with API requests.</li>\n<li>Use CDN-provided analytics alongside application traces to get a complete picture of user experience.</li>\n<li>Visualize waterfalls to pinpoint where latency accumulates, and keep a cross-reference with API timings.</li>\n</ul>\n<h3>Observability strategies</h3>\n<ul>\n<li>Instrument frontend pages to emit timing information that can be correlated with backend traces.</li>\n<li>Correlate CDN cache hits and misses with user-perceived latency to identify opportunities for improvement.</li>\n<li>Tag traces with route-level metadata (version, user segment) to compare performance across deployments.</li>\n</ul>\n<hr />\n<h2>Operational Considerations and Pitfalls</h2>\n<h3>Potential issues to watch for</h3>\n<ul>\n<li>Stale assets due to caching: fingerprinting helps, but purge logic must be correct and timely.</li>\n<li>Incorrect asset paths: ensure a well-defined manifest or loader resolves asset paths consistently across templates.</li>\n<li>Mixed content if CDN domain differs: ensure HTTPS everywhere and proper asset URL selection.</li>\n<li>Cache invalidation complexity: partial asset updates require careful purges to avoid serving old content.</li>\n<li>Deployment coupling risk: tightly coupling frontend and backend deployment can complicate rollbacks; keep deployment pipelines as decoupled as possible.</li>\n</ul>\n<h3>Operational hygiene</h3>\n<ul>\n<li>Maintain a clean separation between API origin and CDN assets.</li>\n<li>Keep asset generation and deployment scripts under version control with clear audit trails.</li>\n<li>Regularly test asset delivery in a staging environment that mirrors production CDN behavior.</li>\n</ul>\n<h2>Concrete Patterns and Examples</h2>\n<h3>Production patterns</h3>\nPattern A: FastAPI origin with S3/CloudFront for static assets. Assets are fingerprinted and uploaded to an S3 bucket; CloudFront caches assets from S3 and serves them to users; templates reference CDN URLs for assets while API endpoints remain on the origin domain.\n<p>Pattern B: FastAPI origin with a general-purpose CDN (e.g., Cloudflare) for static assets. Assets are uploaded to a storage origin or served directly from an edge cache; the CDN provides edge caching with long TTLs and cache purges on changes; the API remains on the origin and the frontend references CDN-hosted assets.</p>\n<h3>Directory structure example</h3>\n<ul>\n<li>Frontend assets</li>\n</ul>\n  - dist/\n    - app.abc123.js\n    - styles.efg456.css\n  - manifest.json\n<ul>\n<li>Templates</li>\n</ul>\n  - index.html\n  - layout.html\n<ul>\n<li>Backend</li>\n</ul>\n  - app/\n    - main.py\n  - templates/ (if server-side templates are used)\n<ul>\n<li>Deployment</li>\n</ul>\n  - deploy/\n    - scripts/\n      - upload_assets.sh\n      - purge_cache.sh\n      - update_manifest.sh\n<ul>\n<li>Documentation</li>\n</ul>\n  - architecture.md\n  - operations.md\n<h3>Minimal code examples</h3>\n<ul>\n<li>FastAPI: mount StaticFiles during development</li>\n</ul>\n<pre><code class=\"language-python\">  from fastapi import FastAPI\n  from fastapi.staticfiles import StaticFiles\n\n  app = FastAPI()\n\n  # Development: serve local assets\n  app.mount(\"/static\", StaticFiles(directory=\"static\"), name=\"static\")\n\n  @app.get(\"/\")\n  async def read_root():\n      return {\"message\": \"Hello, FastAPI with CDN-backed assets\"}\n  </code></pre>\n<ul>\n<li>Template reference to CDN asset (HTML snippet)</li>\n</ul>\n<pre><code class=\"language-html\">  &lt;!doctype html&gt;\n  &lt;html lang=\"en\"&gt;\n  &lt;head&gt;\n    &lt;meta charset=\"utf-8\" /&gt;\n    &lt;title&gt;FastAPI CDN Demo&lt;/title&gt;\n    &lt;!-- Production: CDN-hosted fingerprinted assets --&gt;\n    &lt;link rel=\"stylesheet\" href=\"https://cdn.example.com/static/styles.efg456.css\" /&gt;\n  &lt;/head&gt;\n  &lt;body&gt;\n    &lt;div id=\"root\"&gt;&lt;/div&gt;\n    &lt;script src=\"https://cdn.example.com/static/app.abc123.js\"&gt;&lt;/script&gt;\n  &lt;/body&gt;\n  &lt;/html&gt;\n  </code></pre>\n<ul>\n<li>Simple asset fingerprinting script (conceptual)</li>\n</ul>\n<pre><code class=\"language-bash\">  #!/bin/bash\n  set -euo pipefail\n  DIST_DIR=\"dist\"\n  MANIFEST=\"$DIST_DIR/manifest.json\"\n\n  mkdir -p \"$DIST_DIR\"\n\n  for f in \"$DIST_DIR\"/*; do\n    [ -f \"$f\" ] || continue\n    hash=$(sha256sum \"$f\" | cut -d' ' -f1)\n    base=$(basename \"$f\")\n    newname=\"${base%.*}.$hash.${base##*.}\"\n    cp \"$f\" \"$DIST_DIR/$newname\"\n  done\n\n  # Generate a minimal manifest (example; replace with your templating system)\n  echo '{ \"app.js\": \"app.{hash}.js\" }' &gt; \"$MANIFEST\"\n  echo \"Fingerprinting complete, manifest updated.\"\n  </code></pre>\n<ul>\n<li>Minimal deployment checklist (table)</li>\n</ul>\n<table>\n<thead>\n<tr><th>Step</th><th>Action</th><th>Verification</th></tr>\n</thead>\n<tbody>\n<tr><td>1</td><td>Build frontend assets</td><td>All fingerprinted files exist in dist/</td></tr>\n<tr><td>2</td><td>Upload to CDN origin</td><td>CDN edge serves new files with proper headers</td></tr>\n<tr><td>3</td><td>Update manifest/templates</td><td>HTML references latest fingerprinted filenames</td></tr>\n<tr><td>4</td><td>Purge CDN caches</td><td>CDN returns 200 for new assets, old ones invalidated</td></tr>\n<tr><td>5</td><td>Smoke test</td><td>Main page loads, assets load from CDN, API responds</td></tr>\n</tbody>\n</table>\n<h3>Migration steps</h3>\n<ul>\n<li>Step-by-step migration from origin-only assets to CDN-backed assets</li>\n</ul>\n  1. Prepare fingerprinted assets locally and generate a manifest.\n  2. Upload fingerprinted files to the CDN origin (e.g., S3 bucket or object storage).\n  3. Update templates or asset loader to reference CDN URLs using the fingerprinted names.\n  4. Validate TLS and CORS settings across domains.\n  5. Purge or invalidate CDN caches to ensure the latest assets are served.\n  6. Run end-to-end tests to ensure asset loading, API access, and page rendering are correct.\n  7. Monitor performance metrics (LCP, TTI) to verify improvements.\n<hr />\nIf you’d like, I can tailor the sections to your preferred CDN (for example CloudFront vs Cloudflare) and your specific FastAPI deployment stack, and I can add more concrete code snippets or a fuller migration script aligned with your existing build system.\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
