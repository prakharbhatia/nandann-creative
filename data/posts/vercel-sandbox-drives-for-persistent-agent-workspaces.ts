import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "vercel-sandbox-drives-for-persistent-agent-workspaces",
  title: "Vercel Sandbox Drives for Persistent Agent Workspaces",
  description: "Design persistent Vercel Sandbox workspaces with safe single-writer mounts, read-only snapshots, regional placement, lifecycle controls, and cost limits.",
  date: "2026-09-24",
  readTime: "21 min",
  category: "Engineering",
  tags: ["Vercel Sandbox","AI Agents","Persistent Storage","Cloud Architecture","DevOps","TypeScript"],
  faqs: [
      {
          "question": "What is a Vercel Sandbox Drive?",
          "answer": "A Drive is persistent storage mounted as a directory inside Vercel Sandbox. Its lifecycle is separate from a sandbox, so data can survive a stopped run and be attached to a later sandbox."
      },
      {
          "question": "Can multiple sandboxes write to the same Drive?",
          "answer": "No. Vercel permits one read-write mount at a time. Multiple sandboxes can read concurrent point-in-time snapshots after the Drive has been written at least once."
      },
      {
          "question": "Do Drive snapshots include later writes?",
          "answer": "No. A read-only snapshot reflects the Drive when it is mounted. A consumer must mount a new snapshot to see changes written afterward."
      },
      {
          "question": "How many Drives can one sandbox mount?",
          "answer": "A sandbox can mount up to four Drives at separate paths. This supports separating a workspace, dependency cache, model data, and another durable dataset."
      },
      {
          "question": "Can a Drive move between Vercel regions?",
          "answer": "A Drive stays in its creation region. Any sandbox mounting it must run in that region and cannot use failover regions for that mount."
      },
      {
          "question": "How large can a Vercel Sandbox Drive be?",
          "answer": "The public beta defaults to 1 TiB, or 1 GiB on Hobby. Vercel says Drives can be configured up to 16 TiB, with higher limits available by request."
      },
      {
          "question": "Should an agent store secrets on a Drive?",
          "answer": "Long-lived secrets should come from a secret manager at runtime instead of being written into a persistent workspace. Treat Drive contents as durable data that requires tenant isolation, cleanup, and audit controls."
      }
  ],
  coverImage: "/images/vercel-sandbox-drives-for-persistent-agent-workspaces.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/vercel-sandbox-drives-for-persistent-agent-workspaces.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/vercel-sandbox-drives-for-persistent-agent-workspaces.webp\" alt=\"Vercel Sandbox Drives for Persistent Agent Workspaces\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>Vercel Sandbox Drives add a durable directory to an otherwise disposable compute model. An agent can clone a repository, install dependencies, build an index, stop its sandbox, and attach the same Drive to a later run. The storage survives independently of the sandbox instance.</p>\n<p>That convenience changes the architecture. A persistent workspace can carry stale dependencies, untrusted files, customer data, and broken state from one run to the next. Vercel also enforces one read-write mount at a time, pins each Drive to its creation region, and bills storage, reads, and writes separately. A useful design has to account for ownership, concurrency, isolation, cleanup, and cost from the start.</p>\n<h2>What Vercel Released</h2>\n<p>Vercel announced the public beta for Sandbox Drives on 23 September 2026 for Hobby, Pro, and Enterprise plans. The feature mounts persistent storage at a chosen directory when a sandbox starts.</p>\n<h3>Persistence is independent of compute</h3>\n<p>A Drive is not tied to one sandbox identity. The same named Drive can be attached across runs and sandbox instances, so the application can discard compute without discarding workspace state.</p>\n<p>This is a better fit than copying an entire repository and dependency tree to object storage after every agent turn. The agent continues to use ordinary filesystem operations under the mount path, while orchestration decides which Drive and mode it receives.</p>\n<h3>One writer, several snapshot readers</h3>\n<p>Vercel permits one read-write mount at a time. After the Drive has received its first write, other sandboxes can mount point-in-time, read-only snapshots concurrently.</p>\n<p>The snapshot is fixed when mounted. Writes made later by the active writer do not appear inside already running readers. A reader must mount a fresh snapshot to observe the newer state.</p>\n<h3>Capacity and region are explicit constraints</h3>\n<p>One sandbox can mount up to four Drives at distinct paths. The public beta defaults to a 1 TiB maximum, except Hobby where the default is 1 GiB. Vercel says the size can be configured up to 16 TiB and higher limits can be requested.</p>\n<p>Every Drive stays in its creation region. Sandboxes that mount it must run in that region and cannot use failover regions. Storage architecture and compute placement are therefore the same decision for Drive-backed work.</p>\n<h2>Start with the Smallest Useful Mount</h2>\n<p>Creating and mounting a Drive takes little code. The design around the mount needs more thought than the SDK call.</p>\n<pre><code class=\"language-ts\">import { Drive, Sandbox } from '@vercel/sandbox'\n\nconst workspace = await Drive.getOrCreate({\n  name: 'agent-workspace',\n})\n\nconst sandbox = await Sandbox.create({\n  mounts: {\n    '/workspace': workspace,\n  },\n})\n</code></pre>\n<p>Anything written beneath <code>/workspace</code> persists on the Drive after the sandbox stops. Files elsewhere follow the sandbox's normal lifecycle.</p>\n<h3>Mount at a clear boundary</h3>\n<p>Use a top-level path such as <code>/workspace</code>, <code>/cache</code>, or <code>/models</code>. A clear boundary makes backup, cleanup, quotas, and incident review easier. It also prevents durable state from being scattered through the sandbox filesystem.</p>\n<p>Application code should know which paths persist. A build script that writes credentials or temporary test data into <code>/workspace</code> has created durable data even if the author assumed the sandbox would disappear.</p>\n<h3>Keep transient files outside</h3>\n<p>Logs needed only for the current request, extracted secrets, temporary archives, browser profiles, and scratch downloads should remain outside the Drive or be deleted before the writer releases it. Persist only what earns the recovery or reuse benefit.</p>\n<p>This distinction reduces storage charges and cross-run contamination. It also makes a workspace easier to inspect because every durable file has an intended purpose.</p>\n<h3>Name Drives by ownership</h3>\n<p>A global name such as <code>agent-workspace</code> is fine for a demo and dangerous in a multitenant service. Encode the tenant, project, environment, and workspace identity in the orchestration layer, then validate the mapping before creating or retrieving a Drive.</p>\n<p>Do not derive a storage name directly from unchecked user input. Use an internal identifier so two customers, branches, or environments cannot collide through naming.</p>\n<h2>Choose What Deserves Persistence</h2>\n<p>Four common categories benefit from Drive storage: working trees, dependency caches, model or dataset artifacts, and on-disk agent memory. They have different safety and lifecycle requirements.</p>\n<h3>Repository workspaces</h3>\n<p>A durable clone avoids fetching the repository and rebuilding local indexes for every session. It also preserves uncommitted changes when an agent pauses and resumes.</p>\n<p>The risk is hidden drift. The working tree may contain an old branch, stale generated files, modified hooks, an interrupted merge, or malicious content introduced by an earlier task. Run a workspace health check before every new writer attaches.</p>\n<pre><code class=\"language-bash\">git status --porcelain=v1\ngit remote -v\ngit rev-parse --show-toplevel\ngit fsck --no-progress\n</code></pre>\n<p>Do not automatically erase unexpected changes. Quarantine the workspace or require a recovery decision so legitimate uncommitted work is not lost.</p>\n<h3>Dependency caches</h3>\n<p>Package caches can save the most time while carrying less business data than a whole workspace. Give each ecosystem its own Drive or directory and include runtime, architecture, and lockfile information in the cache key.</p>\n<p>A cache should be replaceable. If deleting it would destroy the only copy of important data, it is a database in disguise and needs stronger controls than a build cache.</p>\n<h3>Models and datasets</h3>\n<p>Large model files and datasets are natural Drive candidates because repeated downloads consume time and bandwidth. Record the source, checksum, license, version, and creation date beside the artifact.</p>\n<p>Avoid letting an agent overwrite a shared model directory during inference. A controlled writer can publish a versioned artifact, and many workers can mount snapshots for read-only use.</p>\n<h3>Agent memory</h3>\n<p>On-disk memory can preserve summaries, indexes, plans, and checkpoints. Keep this data structured and scoped to the correct subject. A shared free-form memory directory can easily leak one user's context into another user's run.</p>\n<p>Store provenance with each record. The next session should know who created it, which repository and commit it describes, and when it expires.</p>\n<h2>Design Around the Single-Writer Rule</h2>\n<p>The one-writer constraint is a consistency primitive. It prevents two sandboxes from changing the same filesystem concurrently, but the application must decide what happens when a second writer arrives.</p>\n<h3>Acquire ownership before compute</h3>\n<p>Do not create an expensive sandbox and only then discover that the Drive is busy. Resolve the workspace, acquire an application-level lease, and attach the Drive after ownership is clear.</p>\n<p>The lease should include a writer ID, task ID, acquisition time, expiry, and fencing token. The fencing token prevents an expired writer from continuing to publish state after a new writer takes over.</p>\n<pre><code class=\"language-ts\">type WorkspaceLease = {\n  driveName: string\n  writerId: string\n  taskId: string\n  fencingToken: number\n  expiresAt: string\n}\n</code></pre>\n<p>Vercel enforces the mount rule, while the lease gives your product a useful queue, timeout, and audit model.</p>\n<h3>Queue or fork competing writers</h3>\n<p>If two tasks need the same workspace, you can queue the second task or give it a separate Drive. Queueing preserves a linear history. Forking improves throughput but creates a merge problem at the repository or application layer.</p>\n<p>Use a new Drive for independent branches, experiments, and speculative agent runs. Use one serialized writer for a shared mutable workspace whose exact filesystem state matters.</p>\n<h3>Release ownership in a finally block</h3>\n<p>Writer cleanup must run after success, failure, cancellation, and timeout. Stop the sandbox, confirm the mount is detached, persist a final status record, and release the application lease.</p>\n<p>A crashed orchestrator can leave uncertainty even if the platform eventually tears down compute. A reconciliation job should compare active leases, running sandboxes, and Drive attachment state, then repair stale ownership safely.</p>\n<h2>Use Snapshots as Published Read Views</h2>\n<p>Read-only Drive snapshots let many sandboxes consume a consistent point in time without competing for the writer.</p>\n<pre><code class=\"language-ts\">const workspace = await Drive.getOrCreate({\n  name: 'tenant-42-repo-7-main',\n})\n\nconst [review, test] = await Promise.all([\n  Sandbox.create({ mounts: { '/workspace': workspace.snapshot() } }),\n  Sandbox.create({ mounts: { '/workspace': workspace.snapshot() } }),\n])\n</code></pre>\n<p>Vercel notes that the Drive must have been written at least once before snapshots can be mounted.</p>\n<h3>Treat a snapshot like a release</h3>\n<p>A writer should publish a marker only after the filesystem is coherent. For a repository workspace, that can mean no lock operation is active, indexes are flushed, and a manifest records the commit and dependency state.</p>\n<p>Readers should validate the marker on startup. A point-in-time view is consistent at the storage layer, but it may still represent an application-level operation that was interrupted halfway through.</p>\n<h3>Readers do not follow the writer</h3>\n<p>An existing snapshot never sees later writes. This is useful for repeatable tests and reviews. It can surprise an application that expects a live shared folder.</p>\n<p>Pass a snapshot or generation identifier to every consumer. When the writer publishes a new generation, start new readers or explicitly remount instead of assuming files will refresh underneath them.</p>\n<h3>Fan out immutable work</h3>\n<p>Snapshot readers are a good fit for test matrices, static analysis, review agents, search indexing, and evaluation runs. Each consumer sees the same starting state and cannot modify it.</p>\n<p>Write results to external storage or to per-task Drives. Do not funnel every result back through the shared workspace unless one controlled writer validates and applies it.</p>\n<h2>Separate Workspace, Cache, and Results</h2>\n<p>A sandbox can mount four Drives. Use that limit to create boundaries instead of treating one large volume as a universal disk.</p>\n<h3>A practical mount layout</h3>\n<pre><code class=\"language-ts\">const sandbox = await Sandbox.create({\n  mounts: {\n    '/workspace': workspace,\n    '/cache': dependencyCache.snapshot(),\n    '/models': modelStore.snapshot(),\n    '/results': taskResults,\n  },\n})\n</code></pre>\n<p>The workspace and results Drive may be writable depending on the workflow. Shared cache and model mounts should usually be snapshots. This prevents a task from poisoning a widely reused artifact.</p>\n<h3>Make cache promotion explicit</h3>\n<p>If a task discovers new dependencies, write them into a task-local cache. A separate validation job can verify checksums, scan packages, and promote the cache through a controlled writer.</p>\n<p>Direct writes from arbitrary agent runs to a global cache save a step but allow one compromised task to influence many later sandboxes.</p>\n<h3>Put outputs under retention policy</h3>\n<p>Results often contain logs, patches, screenshots, test artifacts, or generated packages. Define how long each class survives and which system owns the canonical copy.</p>\n<p>A Drive is useful working storage. Long-term records may belong in object storage, an artifact registry, or a database with retention, legal hold, and access logging.</p>\n<h2>Pin Compute to the Drive Region</h2>\n<p>Regional placement is part of correctness because a Drive cannot move with a failing sandbox. Vercel says mounted sandboxes must run in the Drive's creation region and cannot use failover regions.</p>\n<h3>Choose the region from the data owner</h3>\n<p>Create the Drive in the region selected for the tenant or project, then store that region with the Drive record. Every sandbox request should read the stored value rather than recomputing placement from current traffic.</p>\n<p>This prevents a user routed to another geography from creating compute that cannot attach the existing workspace.</p>\n<h3>Plan for regional failure</h3>\n<p>Automatic compute failover cannot carry a region-pinned Drive. Decide whether the service will pause, rebuild from a remote canonical source, or maintain a separately replicated recovery copy.</p>\n<p>For source-code agents, the Git remote and artifact registry may provide enough recovery. For unique on-disk memory or unpushed changes, the recovery objective requires an explicit export or backup process.</p>\n<h3>Account for data residency</h3>\n<p>A persistent workspace can contain source code, customer files, logs, and model inputs. Region selection must match contractual and regulatory requirements. Include Drive creation and deletion in your data inventory.</p>\n<p>Do not assume that the region of the web request determines the region of durable storage. The Drive's creation record is the reliable source.</p>\n<h2>Prevent Cross-Task Contamination</h2>\n<p>Persistence preserves good work and bad residue equally. Every writer startup should evaluate the previous run before trusting the directory.</p>\n<h3>Use a workspace manifest</h3>\n<pre><code class=\"language-json\">{\n  \"schemaVersion\": 1,\n  \"tenantId\": \"tenant_42\",\n  \"repositoryId\": \"repo_7\",\n  \"branch\": \"main\",\n  \"commit\": \"4d53c9e\",\n  \"runtime\": \"node-24\",\n  \"lockfileHash\": \"sha256:...\",\n  \"lastWriterTask\": \"task_981\",\n  \"updatedAt\": \"2026-09-24T12:00:00Z\"\n}\n</code></pre>\n<p>Validate the tenant and repository before doing anything else. Rebuild indexes or dependencies when the runtime or lockfile hash changes.</p>\n<h3>Clean known transient state</h3>\n<p>Browser profiles, test databases, sockets, PID files, lock files, temporary credentials, and tool-specific crash state can break the next session. Maintain a narrow cleanup list based on the tools you run.</p>\n<p>Avoid broad recursive deletion at startup. It can erase legitimate agent work and makes recovery harder. Quarantine unknown files when their provenance is unclear.</p>\n<h3>Scan durable content</h3>\n<p>Run secret detection, malware scanning where appropriate, and repository policy checks before publishing a workspace to snapshot readers. Treat dependency lifecycle scripts and executable artifacts as untrusted across runs.</p>\n<p>Persistence turns an isolated compromise into a potential future-session compromise. Scanning at the writer-to-reader boundary reduces that risk.</p>\n<h2>Keep Secrets Out of Durable Storage</h2>\n<p>Inject credentials at runtime and remove them before the sandbox stops. A token written beneath the mount path may outlive the compute and appear in future snapshots.</p>\n<h3>Use short-lived credentials</h3>\n<p>Issue a credential for the task, tenant, repository, and environment. Keep its lifetime close to the expected run duration. Prefer an identity exchange over a static secret copied from application configuration.</p>\n<p>Configure tools to read credentials from environment variables or an in-memory agent rather than writing them to <code>.env</code>, Git credential files, shell history, or package-manager configuration on the Drive.</p>\n<h3>Redact logs and checkpoints</h3>\n<p>Agent logs often capture commands, HTTP headers, environment fragments, and tool output. Apply redaction before writing logs to <code>/results</code> or another durable path.</p>\n<p>Checkpoints should contain references to credentials, not credential values. A resumed session can request a new token for the stored subject and scope.</p>\n<h3>Delete on revocation</h3>\n<p>When a user disconnects a repository or requests data deletion, find every Drive associated with that subject. Stop active writers, remove derived snapshots or exports, delete the Drive, and record completion outside the deleted storage.</p>\n<p>Ownership metadata in a database makes this possible. Names alone are not a sufficient inventory.</p>\n<h2>Model Drive Costs Before Scaling</h2>\n<p>Vercel bills Drive storage, reads, and writes by region. In <code>iad1</code>, the launch announcement lists $0.05 per GB-month for storage, $0.0015 per GB read, and $0.004 per GB written. Hobby includes 15 GB of storage and 30 GB each of reads and writes per month.</p>\n<h3>Calculate all three meters</h3>\n<p>A 100 GB dependency tree retained for a month has a storage charge. Mounting it across many cold sandboxes can add read volume, while repeated installs or generated outputs add write volume. The cheapest design depends on reuse frequency and churn.</p>\n<p>Measure bytes stored by Drive, bytes read by task type, bytes written by task type, and the useful cache-hit time saved. A cache that changes every run may cost more than downloading a compact artifact.</p>\n<h3>Set quotas below platform limits</h3>\n<p>The 1 TiB default and 16 TiB configurable maximum are capacity ceilings, not product defaults. Give each tenant and workspace a much smaller quota based on the workload.</p>\n<p>Alert at soft limits, reject unexpected growth, and expose the largest directories in operations tooling. Agents can generate runaway logs or recursive build output quickly.</p>\n<h3>Garbage collect by last useful access</h3>\n<p>Track the last writer, last reader, associated branch, and canonical source. Delete abandoned branch workspaces after a recovery window. Rebuildable caches can expire sooner than unpushed work.</p>\n<p>Deletion should respect active leases and mounts. Mark a Drive for deletion, block new writers, wait for or stop active sandboxes, then remove it.</p>\n<h2>Operate the Lifecycle Explicitly</h2>\n<p>Drive creation is the beginning of a resource lifecycle. Production systems need inventory, health, migration, and deletion paths.</p>\n<h3>Maintain a control-plane record</h3>\n<p>Store Drive ID or name, tenant, project, environment, region, size limit, state, current lease, creation time, last access, and retention class. Use this record to authorize every mount.</p>\n<p>The control plane should be the only component allowed to call <code>getOrCreate</code> for user workspaces. Otherwise a naming bug can create orphaned storage that no cleanup job knows about.</p>\n<h3>Reconcile platform and application state</h3>\n<p>Run a periodic job that finds records without Drives, Drives without records, expired writer leases, old snapshots or exports, and workspaces above quota. Produce an operator-visible report before deleting uncertain resources.</p>\n<p>Reconciliation is also where you catch a sandbox that died before updating the task database or a deployment that changed naming rules.</p>\n<h3>Version workspace formats</h3>\n<p>Agent tools, runtimes, indexes, and manifests evolve. Put a schema version in the workspace and write migrations that can run without destroying source work.</p>\n<p>For large or risky migrations, fork to a new Drive, validate it, then switch the control-plane pointer. The old Drive becomes a rollback copy for a bounded period.</p>\n<h2>Test Failure and Recovery</h2>\n<p>The happy path proves that files persist. The production test is whether state remains understandable after interruption.</p>\n<h3>Kill the writer mid-operation</h3>\n<p>Stop a sandbox during Git checkout, dependency installation, index creation, and result publication. Start a new writer and verify that the manifest and health checks detect incomplete work.</p>\n<p>Do not rely on a process exit hook to restore consistency. The orchestrator should be able to recover from a sandbox that disappears without cleanup.</p>\n<h3>Confirm snapshot isolation</h3>\n<p>Mount a reader snapshot, change a file through the writer, and prove the existing reader still sees the old version. Then mount a new reader and prove it sees the new version.</p>\n<p>This test catches code that assumes live shared storage and establishes the generation semantics for developers.</p>\n<h3>Exercise region loss</h3>\n<p>Simulate the Drive region being unavailable. Confirm whether tasks pause, rebuild from Git and artifacts, or fail with a clear recovery status. Make sure global routing does not repeatedly start sandboxes in incompatible regions.</p>\n<p>Document the recovery point for unpushed changes. If the only copy is on the Drive, say so plainly in the product and operational runbook.</p>\n<h2>A Reference Architecture</h2>\n<p>A reliable agent workspace service separates control, compute, and durable state:</p>\n<ol>\n<li>The API authenticates the user and resolves a tenant-scoped workspace record.</li>\n<li>The scheduler reads the Drive region and acquires a fenced writer lease.</li>\n<li>The sandbox starts in that region with the workspace mounted read-write.</li>\n<li>Runtime credentials arrive from a secret service and remain outside the mount.</li>\n<li>Startup checks validate the manifest, repository, and transient state.</li>\n<li>The agent works and writes results to a separate task-scoped path or Drive.</li>\n<li>The writer flushes state, updates the manifest, and publishes a generation.</li>\n<li>Test and review sandboxes mount read-only snapshots of that generation.</li>\n<li>The orchestrator stops compute, releases the lease, and updates usage records.</li>\n<li>Retention jobs delete abandoned workspaces and rebuildable caches.</li>\n</ol>\n<p>This design keeps the single writer intentional and turns snapshot readers into repeatable consumers. It also gives cost, data deletion, and incident response a dependable inventory.</p>\n<p>For related architecture decisions, compare this persistence model with <a href=\"/blog/vercel-sandbox-routing-nearest-region-domain-lookups\">Vercel Sandbox Routing and Region Selection</a>. The evaluation side is covered in <a href=\"/blog/coding-agent-evals-harbor-vercel-sandbox\">Coding Agent Evals with Harbor and Vercel Sandbox</a>, while <a href=\"/blog/openai-agents-api-harness\">OpenAI Agents API Harness Patterns</a> covers broader orchestration concerns.</p>\n<h2>Production Checklist</h2>\n<p>Before attaching a Drive to a production agent workflow:</p>\n<ol>\n<li>Define a tenant-scoped name and control-plane record.</li>\n<li>Pick the region from data residency and recovery requirements.</li>\n<li>Mount only the directories that need persistence.</li>\n<li>Keep temporary files and credentials outside the mount.</li>\n<li>Acquire a fenced application lease before the read-write mount.</li>\n<li>Decide whether competing writers queue or fork.</li>\n<li>Publish a versioned manifest before snapshot readers start.</li>\n<li>Treat snapshots as immutable generations that never follow later writes.</li>\n<li>Separate workspace, cache, model, and result storage where practical.</li>\n<li>Validate persistent content for secrets and unsafe executable residue.</li>\n<li>Set tenant quotas well below platform maximums.</li>\n<li>Meter storage, reads, writes, and actual time saved.</li>\n<li>Reconcile leases, sandboxes, and Drives on a schedule.</li>\n<li>Test interrupted writers, stale snapshots, and regional failure.</li>\n<li>Implement deletion for disconnects, expired branches, and user requests.</li>\n</ol>\n<p>Drives remove repeated setup work, but they also remove the clean slate that made disposable sandboxes easy to reason about. Keep the writer serialized, make every readable state a named generation, and treat durable agent files as real customer data.</p>\n<h2>Official Sources</h2>\n<ul>\n<li><a href=\"https://vercel.com/changelog/drives-for-vercel-sandbox-are-now-in-public-beta\">Vercel changelog: Drives for Vercel Sandbox public beta</a></li>\n<li><a href=\"https://vercel.com/docs/vercel-sandbox\">Vercel Sandbox documentation</a></li>\n<li><a href=\"https://vercel.com/docs/vercel-sandbox/pricing\">Vercel Sandbox pricing</a></li>\n<li><a href=\"https://vercel.com/changelog/vercel-sandbox-is-now-available-in-all-regions\">Vercel changelog: Sandbox in all regions</a></li>\n<li><a href=\"https://vercel.com/docs/vercel-sandbox/sdk-reference\">Vercel Sandbox SDK reference</a></li>\n<li><a href=\"https://vercel.com/kb/guide/vercel-drives\">Vercel knowledge base: Vercel Drives</a></li>\n<li><a href=\"https://vercel.com/changelog/vercel-sandbox-persistent-sandboxes-beta\">Vercel changelog: persistent sandboxes beta</a></li>\n<li><a href=\"https://vercel.com/changelog/drives-for-vercel-sandbox-in-private-beta\">Vercel changelog: Drives private beta</a></li>\n</ul>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
