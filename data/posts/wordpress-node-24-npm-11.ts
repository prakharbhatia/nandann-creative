import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "wordpress-node-24-npm-11",
  title: "WordPress Core Is Moving to Node 24 and npm 11. Your Plugin Build Assumptions Are Next.",
  description: "WordPress Core's Node 24 and npm 11 move is a practical prompt to audit plugin toolchains, CI images, lockfiles, and supported engines.",
  date: "2026-09-13",
  readTime: "20 min",
  category: "WordPress",
  tags: ["WordPress","Node.js","npm","Gutenberg","CI/CD","Plugin development"],
  faqs: [
      {
          "question": "Is WordPress changing its PHP runtime to Node.js?",
          "answer": "No. WordPress continues to run on PHP. The Node.js and npm update applies to JavaScript tooling used to build, test, lint, and package Core and related development workflows."
      },
      {
          "question": "Why does npm 11 matter to a WordPress plugin?",
          "answer": "npm releases can alter peer-dependency handling, lockfile behavior, package metadata interpretation, and supported Node versions. Plugins with local build tooling should test the upgrade instead of assuming Core's change covers them."
      },
      {
          "question": "Should every plugin immediately require Node 24?",
          "answer": "Not automatically. Choose a supported range based on your own dependencies, contributors, CI images, and release process. Test the declared range and document it in package metadata and contributor instructions."
      },
      {
          "question": "What should be checked first in CI?",
          "answer": "Pin the Node runtime in the CI matrix, use a reproducible install command, verify the lockfile, run build and test scripts, and inspect generated distribution files before changing the supported default."
      },
      {
          "question": "Does npm install replace npm ci in CI?",
          "answer": "For a committed lockfile and repeatable CI, npm ci is generally the safer baseline because it installs from the lockfile rather than updating it. Confirm the behavior with the npm version you support."
      },
      {
          "question": "What is the safest rollout?",
          "answer": "Add Node 24 and npm 11 to a CI matrix, fix incompatibilities, then make it the default after builds and packaged artifacts are stable. Keep a documented fallback during the contributor transition."
      }
  ],
  coverImage: "/images/wordpress-node-24-npm-11.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/wordpress-node-24-npm-11.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/wordpress-node-24-npm-11.webp\" alt=\"WordPress Core Is Moving to Node 24 and npm 11. Your Plugin Build Assumptions Are Next.\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>WordPress is not becoming a Node application. It is doing the more ordinary and more consequential thing: updating the JavaScript toolchain that builds, tests, lints, and packages parts of a huge PHP product. The <a href=\"https://make.wordpress.org/core/2026/09/09/updating-wordpress-to-use-node-js-24-and-npm-11/\">Core proposal to move to Node.js 24 and npm 11</a> is a reminder that modern WordPress development has two runtimes. PHP serves the site; Node builds much of what developers ship.</p>\n<p>That distinction is exactly why upgrades are easy to postpone. A plugin may work in production while its local build quietly relies on an old Node image, a permissive dependency resolution quirk, or a lockfile nobody has regenerated in months. Then a routine contributor update becomes a release problem. The fix is not heroic migration work. It is treating the build chain as part of the supported product.</p>\n<h2>Read the Change for What It Is</h2>\n<p>The Core post proposes updating project tooling to Node.js 24 and npm 11, with the goal of keeping contributor environments and automation on current supported releases. It is not a change to WordPress's server-side requirements. Users do not install Node to run a normal WordPress site.</p>\n<h3>PHP Runtime and Build Runtime Are Different Contracts</h3>\n<p>Your hosting compatibility page describes PHP, database, caching, and web-server support. Your contributor contract describes Node, npm, package manager settings, build commands, and generated artifacts. Both are real. They just fail in different places.</p>\n<p>When the build contract is vague, the package build happens to succeed on one maintainer's laptop until a security update, a new runner image, or a clean checkout makes the assumption visible.</p>\n<h3>Core's Toolchain Is Not Your Toolchain</h3>\n<p>Core can update its own dependencies and CI. A plugin or theme may use <code>@wordpress/scripts</code>, custom webpack configuration, Playwright, TypeScript, a monorepo, or a third-party block package with its own engine requirements. \"WordPress supports Node 24\" is a useful reason to test. It is not proof that every dependency is ready.</p>\n<h2>Inventory Before You Upgrade</h2>\n<p>Start by finding the places where the old runtime is declared or implied. Search <code>package.json</code>, lockfiles, CI workflows, Dockerfiles, devcontainers, GitHub Actions setup steps, documentation, and release scripts. Look for cached toolchain artifacts as well: old build containers can leave a project appearing stable after configuration has changed.</p>\n<h3>Make Engines Honest</h3>\n<p>If a project works only with a particular Node range, say so. <code>engines</code> is not a perfect enforcement mechanism, but it gives package consumers and contributors a visible contract.</p>\n<pre><code class=\"language-json\">{\n  \"engines\": {\n    \"node\": \"&gt;=24 &lt;25\",\n    \"npm\": \"&gt;=11 &lt;12\"\n  },\n  \"scripts\": {\n    \"build\": \"wp-scripts build\",\n    \"test\": \"wp-scripts test-unit-js\"\n  }\n}\n</code></pre>\n<p>Use a wider range only if the matrix proves it. A fictional compatibility range is worse than a narrow honest one.</p>\n<h3>Ask Who Generates the Release Files</h3>\n<p>Many WordPress plugins commit a <code>build/</code> directory or package generated assets for distribution. Verify whether a release is built in CI from a clean checkout, by a maintainer's local machine, or by both. A runtime upgrade that changes the generated asset graph, source maps, or minification output should be seen before it becomes a ZIP sent to customers.</p>\n<h2>npm 11 Makes Reproducibility the Main Event</h2>\n<p>The case for an npm upgrade is not that every new major release is magically faster. It is that an untested package manager is an untracked dependency on developer history.</p>\n<h3>Use the Lockfile as a Release Input</h3>\n<p>In CI, use a clean deterministic install against the committed lockfile. <code>npm ci</code> is a sensible baseline for repositories that use npm lockfiles because it installs from the lockfile rather than treating CI as a moment to rewrite dependency resolution.</p>\n<pre><code class=\"language-yaml\">- uses: actions/setup-node@v4\n  with:\n    node-version: 24\n    cache: npm\n- run: npm ci\n- run: npm run lint\n- run: npm test\n- run: npm run build\n</code></pre>\n<p>Then inspect the result. A green build that accidentally changed the packaged files is not a green release.</p>\n<h3>Do Not Solve Peer Warnings by Habit</h3>\n<p>Older projects often carry installation flags because one dependency once complained. npm major upgrades are a good time to find out whether those flags are still needed. A blanket compatibility flag can conceal a package relationship that should be fixed or removed.</p>\n<p>Treat peer warnings as design feedback. Some will be harmless metadata lag. Others point to two plugins or build packages expecting incompatible versions of the same capability. Record the decision rather than leaving the next contributor to rediscover it.</p>\n<h2>Add Node 24 Before Making It the Only Lane</h2>\n<p>The safest migration is a matrix, not a cliff. Add Node 24 and npm 11 to CI while the currently supported combination remains visible. Let the project demonstrate what actually breaks.</p>\n<h3>Test the Whole Contributor Loop</h3>\n<p>Run install, linting, unit tests, integration or browser tests where available, production build, packaging, and a smoke test in a disposable WordPress instance. A build tool update often succeeds at compilation and fails in the packaged plugin because an asset path, module format, or runtime assumption changed after compilation.</p>\n<h3>Separate Dependency Updates From Runtime Updates</h3>\n<p>If the build fails, resist upgrading twenty unrelated packages at once. First establish the runtime delta. Then make one dependency change at a time with a clear reason. This keeps the pull request reviewable and helps distinguish a Node change from an unrelated transitive dependency shift.</p>\n<h2>Gutenberg Packages Deserve Direct Testing</h2>\n<p>Modern block development often uses packages from the WordPress ecosystem, but custom configuration can move just enough of the stack out of the default path to matter.</p>\n<h3>Check the Actual Build Entry Points</h3>\n<p>List every command contributors run: <code>start</code>, <code>build</code>, <code>lint</code>, unit test, e2e test, storybook if present, and release packaging. Run them with Node 24 in a clean environment. Do not stop at the command that happens to be called by the main CI workflow.</p>\n<h3>Keep Generated Code Out of the Argument</h3>\n<p>If generated build output changes, review whether the difference is expected. Hash churn, source-map paths, license banners, or ordering may be harmless. A missing dynamic import, changed dependency bundle, or new runtime polyfill needs explanation. Generated files are still what browsers execute.</p>\n<h2>CI Images Are Part of the Support Matrix</h2>\n<p>A local <code>.nvmrc</code> cannot save a project whose CI runner chooses a different Node release. Pin the version used in GitHub Actions, containers, and deployment build jobs. Cache package downloads, not ambiguous <code>node_modules</code> directories that encode an old ABI or install state.</p>\n<h3>Make Failure Messages Useful</h3>\n<p>Print the relevant versions early in the job.</p>\n<pre><code class=\"language-sh\">node --version\nnpm --version\nnpm ci\nnpm run build\n</code></pre>\n<p>This is not verbose for its own sake. When a future runner update changes the actual runtime, the logs will show it before a maintainer starts blaming the block compiler.</p>\n<h3>Test on a Clean Checkout</h3>\n<p>Run one job with no dependency cache beyond the package download cache. A clean install catches missing declared dependencies and package-manager behavior that a long-lived workspace can hide. The project should be buildable by a new contributor, not just by the machine that remembers it.</p>\n<h2>Release Artifacts Need Their Own Check</h2>\n<p>WordPress work often crosses a boundary from source repository to plugin ZIP. Test both sides. Build the archive in CI, list its contents, install it into a disposable site, and exercise the block or integration it provides.</p>\n<h3>Verify What Ships</h3>\n<p>The release should contain compiled assets and required metadata, not development caches, credentials, source-only dependencies, or missing runtime files. A small scripted check can catch surprisingly ordinary mistakes.</p>\n<pre><code class=\"language-sh\">npm run build\nnpm run package-plugin\nunzip -l dist/my-plugin.zip\n</code></pre>\n<p>Pair this with a smoke test that activates the plugin. The package being valid ZIP syntax is a very low bar.</p>\n<h2>Communicate the Contributor Migration</h2>\n<p>An upgrade is less painful when developers can see it coming. Update the README, editor configuration, container files, and contribution guide together. State the Node and npm versions, the installation command, and the temporary compatibility position if one exists.</p>\n<h3>Avoid the Silent Break</h3>\n<p>If contributors need Node 24 for new changes but a released branch remains on an older runtime, say so. Separate branch-specific instructions are kinder than a generic command that fails after ten minutes of dependency installation.</p>\n<h3>Keep the Rollback Small</h3>\n<p>The upgrade branch should make it easy to restore the previous CI default if a blocker appears. That means avoiding unrelated refactors and preserving test results. A rollback is not a failure of ambition. It is a disciplined response to evidence.</p>\n<h2>The Upgrade Is an Audit Opportunity</h2>\n<p>WordPress Core moving its tooling forward is not a request for every plugin to modernize by superstition. It is a strong prompt to find the build assumptions your project already has.</p>\n<p>Node 24 and npm 11 should become boring after the matrix is green, artifacts are reviewed, and contributor instructions match reality. That is the right outcome. The infrastructure has stopped being tribal knowledge and started being a maintained part of the plugin.</p>\n<hr />\n<div style=\"background: rgba(99,102,241,0.08); border: 1px solid rgba(99,102,241,0.25); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #818cf8; margin-top: 0;\">Need help with your WordPress site?</h3>\n  <p>Whether you're dealing with security vulnerabilities, plugin conflicts, slow load times, or Core Web Vitals failures — we handle the technical side so you can focus on your business. Custom plugin development, performance hardening, Wasm integrations, and ongoing maintenance retainers.</p>\n  <p><a href=\"/contact?ref=wordpress-blog\" style=\"display: inline-block; background: linear-gradient(135deg, #6366f1, #4f46e5); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Talk to Us About Your WordPress Site &rarr;</a></p>\n</div>\n",
};

export default post;
