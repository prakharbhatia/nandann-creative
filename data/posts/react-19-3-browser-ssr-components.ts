import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "react-19-3-browser-ssr-components",
  title: "React 19.3 browser(): Replace Mounted Flags in SSR",
  description: "Use React 19.3 browser() with Suspense for localStorage, time zones, layout APIs, client caches, and deliberate browser-only rendering without hydration hacks.",
  date: "2026-09-25",
  readTime: "22 min",
  category: "Engineering",
  tags: ["React 19.3","Server Rendering","Suspense","Hydration","Next.js","Frontend Engineering"],
  faqs: [
      {
          "question": "What does browser() do in React 19.3?",
          "answer": "browser returns an opaque value that a Client Component passes to use. During server rendering, React stops rendering that component and leaves the nearest Suspense fallback in the HTML. In the browser, the call does not suspend."
      },
      {
          "question": "Does use(browser()) replace the use client directive?",
          "answer": "No. In frameworks with Server Components, browser must be called from a Client Component. The use client directive defines the component boundary, while use(browser()) controls whether that component renders on the server."
      },
      {
          "question": "Does browser() prevent the component's JavaScript from reaching the client?",
          "answer": "No. The component is still client code and must be downloaded. browser changes server-rendering behavior; it is not a code-splitting API."
      },
      {
          "question": "Does use(browser()) require Suspense?",
          "answer": "Yes during server rendering. React requires a surrounding Suspense boundary so it has fallback HTML to leave in place. Without one, the server render fails."
      },
      {
          "question": "When is next/dynamic with ssr false still useful?",
          "answer": "Use next/dynamic when a Next.js application needs both client-only rendering and lazy module loading, especially for a browser-dependent third-party package. browser is useful when the component is already loaded and Suspense should own the fallback."
      },
      {
          "question": "Should localStorage always use browser()?",
          "answer": "No. If the server can render a meaningful default, render it and synchronize after hydration. Use browser when the browser value defines the initial UI and a server guess would be misleading or cause a mismatch."
      },
      {
          "question": "How can teams observe browser-only rendering?",
          "answer": "When using React's server renderer directly, pass onBrowserBailout and provide a reason to browser. The callback receives an error with the reason as its cause plus a component stack."
      }
  ],
  coverImage: "/images/react-19-3-browser-ssr-components.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/react-19-3-browser-ssr-components.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/react-19-3-browser-ssr-components.webp\" alt=\"A frontend developer compares a resolved web interface across a laptop and phone beside a loading skeleton\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>React 19.3 gives server-rendered applications a first-class way to say that a component can only produce meaningful UI in a browser. Call <code>use(browser())</code> inside a Client Component, place it under <code>&lt;Suspense&gt;</code>, and React leaves the fallback in the server HTML. During hydration, the component renders normally with access to <code>localStorage</code>, the device time zone, layout measurements, or a browser-side data cache.</p>\n<p>This replaces a familiar pile of workarounds: <code>mounted</code> state set in an effect, <code>typeof window !== 'undefined'</code> branches, and framework-specific SSR opt-outs used only to avoid a mismatch. Those patterns are not universally wrong. React's new API gives the choice an explicit rendering meaning and integrates it with Suspense.</p>\n<p>The important decision remains architectural. Most components should still render useful, matching HTML on the server. Browser-only rendering is an escape hatch for the small parts of an interface whose initial value genuinely exists only on the device.</p>\n<h2>The Problem React Is Solving</h2>\n<p>Server rendering asks one component tree to run in two environments with different capabilities.</p>\n<h3>The server produces the initial HTML</h3>\n<p>The server has request headers, route parameters, backend credentials, and data it can fetch before sending a response. It does not have the user's <code>localStorage</code>, viewport size, browser extension state, media devices, or current DOM layout.</p>\n<p>React sends HTML so the user can see content before all client JavaScript loads. The browser then hydrates that markup by attaching React behavior to the existing DOM.</p>\n<h3>The first client render must match</h3>\n<p><code>hydrateRoot</code> expects the client tree to produce the same initial markup as the server. React documents mismatches as bugs. Development builds warn, and React does not promise to patch every differing attribute because validating all markup would add cost to the common matching case.</p>\n<p>A component that reads the current hour on the server and the user's local hour in the browser can disagree. The same problem appears with random IDs, stored preferences, URL rewrites, device capabilities, and caches that exist only on the client.</p>\n<h3>Some UI has no honest server value</h3>\n<p>A saved draft editor can render an empty textarea on the server, but the user's actual draft may already be in <code>localStorage</code>. A map library may access <code>window</code> at module initialization. A tooltip may need element geometry before it can choose a position.</p>\n<p>The server can guess, delay, or show a placeholder. <code>browser()</code> formalizes the third option: keep the nearest Suspense fallback in the HTML and let the browser render the real component.</p>\n<h2>How browser() Works</h2>\n<p>React exports <code>browser</code> from <code>react-dom</code>, while the component reads its value through <code>use</code> from <code>react</code>.</p>\n<pre><code class=\"language-tsx\">'use client'\n\nimport { Suspense, use } from 'react'\nimport { browser } from 'react-dom'\n\nfunction DeviceTimeZone() {\n  use(browser('The time zone comes from the browser device.'))\n\n  const timeZone = Intl.DateTimeFormat().resolvedOptions().timeZone\n  return &lt;span&gt;{timeZone}&lt;/span&gt;\n}\n\nexport function TimeZoneField() {\n  return (\n    &lt;Suspense fallback={&lt;span&gt;Detecting time zone...&lt;/span&gt;}&gt;\n      &lt;DeviceTimeZone /&gt;\n    &lt;/Suspense&gt;\n  )\n}\n</code></pre>\n<h3>Server behavior</h3>\n<p>During server rendering, <code>use(browser())</code> stops rendering <code>DeviceTimeZone</code>. React finds the nearest Suspense boundary and leaves its fallback in the generated HTML.</p>\n<p>This is a controlled bailout, not an error. React does not send browser-only content and does not pretend that it knows the device value.</p>\n<h3>Browser behavior</h3>\n<p>In the browser, passing the value from <code>browser()</code> to <code>use</code> returns <code>undefined</code>. Rendering continues immediately, and the component may read browser APIs.</p>\n<p>The <code>use</code> call is what gives the value meaning. Calling <code>browser()</code> by itself does nothing, and React explicitly says not to throw the returned value.</p>\n<h3>A boundary is required</h3>\n<p>The call must sit under Suspense during server rendering. Without a boundary, React has no fallback to emit and the server render fails.</p>\n<p>Keep the boundary close to the browser-dependent region. Wrapping an entire page means one small device-only widget can replace useful server-rendered content with a large loading state.</p>\n<h2>browser() Is Not the Same as use client</h2>\n<p>The names are easy to confuse in a Server Components framework.</p>\n<h3>use client defines a module boundary</h3>\n<p>The <code>'use client'</code> directive tells a framework that a file exports Client Components. Those components may use state, effects, event handlers, and browser APIs after they reach the browser.</p>\n<p>Client Components can still be prerendered on the server. In Next.js, marking a file with <code>'use client'</code> does not automatically prevent its initial HTML from being generated during a server render or build.</p>\n<h3>browser() controls server rendering</h3>\n<p><code>use(browser())</code> tells React to stop server-rendering the current component and use Suspense fallback HTML instead. React requires the call to happen in a Client Component.</p>\n<p>The two controls answer separate questions:</p>\n<table>\n<thead>\n<tr><th>Control</th><th>Question it answers</th></tr>\n</thead>\n<tbody>\n<tr><td><code>'use client'</code></td><td>Does this component belong to the client component graph and use client capabilities?</td></tr>\n<tr><td><code>use(browser())</code></td><td>Should this Client Component skip server rendering and finish in the browser?</td></tr>\n</tbody>\n</table>\n<h3>Keep the client boundary narrow</h3>\n<p>Do not mark a large page as a Client Component only because one child uses <code>browser()</code>. Put the directive and the browser-only call in a focused child.</p>\n<pre><code class=\"language-tsx\">// app/settings/page.tsx, a Server Component\nimport { SavedDraftPanel } from './saved-draft-panel'\n\nexport default async function SettingsPage() {\n  const account = await loadAccount()\n\n  return (\n    &lt;main&gt;\n      &lt;h1&gt;Settings for {account.name}&lt;/h1&gt;\n      &lt;SavedDraftPanel /&gt;\n    &lt;/main&gt;\n  )\n}\n</code></pre>\n<p>The page heading and account data remain server-rendered. Only the saved draft island waits for the browser.</p>\n<h2>Replace the Mounted-State Pattern</h2>\n<p>Many applications delay browser-dependent UI with a state flag.</p>\n<pre><code class=\"language-tsx\">function OldPattern() {\n  const [mounted, setMounted] = useState(false)\n\n  useEffect(() =&gt; {\n    setMounted(true)\n  }, [])\n\n  if (!mounted) return &lt;DraftSkeleton /&gt;\n  return &lt;DraftEditor initialValue={localStorage.getItem('draft') ?? ''} /&gt;\n}\n</code></pre>\n<h3>What the effect is doing</h3>\n<p>The initial server render returns the skeleton. The first browser render must also return the skeleton so hydration matches. After the effect runs, state changes and React renders the editor.</p>\n<p>The technique works, but the state is not application state. It encodes a rendering phase. Every component reimplements the same convention, and tests need to wait for an extra effect-driven render.</p>\n<h3>The React 19.3 version</h3>\n<pre><code class=\"language-tsx\">'use client'\n\nimport { Suspense, use, useState } from 'react'\nimport { browser } from 'react-dom'\n\nfunction SavedDraft() {\n  use(browser('The saved draft is stored in localStorage.'))\n\n  const [draft, setDraft] = useState(\n    () =&gt; localStorage.getItem('draft') ?? ''\n  )\n\n  function updateDraft(value: string) {\n    setDraft(value)\n    localStorage.setItem('draft', value)\n  }\n\n  return (\n    &lt;textarea\n      aria-label=\"Saved draft\"\n      value={draft}\n      onChange={(event) =&gt; updateDraft(event.target.value)}\n    /&gt;\n  )\n}\n\nexport function SavedDraftPanel() {\n  return (\n    &lt;Suspense fallback={&lt;DraftSkeleton /&gt;}&gt;\n      &lt;SavedDraft /&gt;\n    &lt;/Suspense&gt;\n  )\n}\n</code></pre>\n<p>The fallback becomes part of the rendering contract. There is no temporary boolean and no effect whose only purpose is to detect hydration.</p>\n<h3>Do not convert every effect automatically</h3>\n<p>Effects remain correct for synchronizing with external systems after render. A chat connection, analytics subscription, DOM event listener, or media query observer may belong in an effect even when the component can render useful server HTML.</p>\n<p>Use <code>browser()</code> when the component cannot produce its initial UI on the server. Do not use it merely because the component contains an effect.</p>\n<h2>Render a Server Value When You Have One</h2>\n<p>Skipping server rendering discards useful work. If the server has an honest default, render it.</p>\n<h3>Conditional use is supported</h3>\n<p>React's <code>use</code> API may be called conditionally, unlike ordinary Hooks. The React docs show a time-zone helper that returns a provided default and calls <code>use(browser())</code> only when no default exists.</p>\n<pre><code class=\"language-tsx\">'use client'\n\nimport { use } from 'react'\nimport { browser } from 'react-dom'\n\nfunction useTimeZone(defaultTimeZone?: string) {\n  if (defaultTimeZone !== undefined) {\n    return defaultTimeZone\n  }\n\n  use(browser('No server time zone was provided.'))\n  return Intl.DateTimeFormat().resolvedOptions().timeZone\n}\n</code></pre>\n<h3>Prefer request-derived values for important UI</h3>\n<p>A server may infer locale or time zone from a user profile, cookie, or explicit setting. That value can produce stable HTML and remain accessible before hydration.</p>\n<p>Device detection is useful for convenience, but it should not silently replace an account setting that affects deadlines, billing, or compliance. The data model decides which source is authoritative.</p>\n<h3>Decide whether the value should switch</h3>\n<p>If the server renders <code>Europe/London</code> and the device reports <code>Asia/Kolkata</code>, should the label change during hydration? Sometimes the answer is no. A conditional early return in the example above keeps the server value in both environments.</p>\n<p>If the product needs to show both values, model them separately. Hidden hydration behavior is a poor place to define business semantics.</p>\n<h2>Browser-Side Data Caches</h2>\n<p>The API is also useful when a data library can read from a browser cache but the server did not receive initial data.</p>\n<h3>Render initial data when available</h3>\n<p>React's documentation shows a conditional query wrapper. It server-renders when <code>initialData</code> exists and bails to the browser when it does not.</p>\n<pre><code class=\"language-tsx\">function useBrowserQuery&lt;T&gt;(\n  queryKey: string,\n  options: { initialData?: T }\n) {\n  if (options.initialData === undefined) {\n    use(browser(`No initial data for ${queryKey}`))\n  }\n\n  return useQuery({\n    queryKey: [queryKey],\n    initialData: options.initialData,\n  })\n}\n</code></pre>\n<p>In production code, avoid placing sensitive query identifiers in a reason that may reach logging. Use a stable category such as <code>product-query-without-initial-data</code>.</p>\n<h3>Browser-only fetching changes loading behavior</h3>\n<p>When no initial data exists, the server sends fallback HTML. After hydration, the query library may return cached data immediately or begin a request.</p>\n<p>This can be appropriate for personalized secondary panels. It is a poor default for the main article, product details, pricing, or other content that should be visible to users and crawlers in the initial response.</p>\n<h3>Keep cache semantics explicit</h3>\n<p>A browser cache can contain stale or user-specific data. The component should still define freshness, error, and empty states. <code>browser()</code> chooses the rendering environment; it does not make the query correct.</p>\n<p>Pass server-fetched data for critical content. Use the browser-only path for data whose absence from the server response is intentional.</p>\n<h2>Layout and DOM-Dependent Components</h2>\n<p>Tooltips, canvases, editors, charts, and maps often need browser primitives.</p>\n<h3>Separate the shell from the measurement</h3>\n<p>A chart card can render a heading, description, legend, and accessible table on the server. Only the canvas renderer may need a measured width.</p>\n<pre><code class=\"language-tsx\">export function SalesCard({ rows }: { rows: SalesRow[] }) {\n  return (\n    &lt;section aria-labelledby=\"sales-title\"&gt;\n      &lt;h2 id=\"sales-title\"&gt;Sales by week&lt;/h2&gt;\n      &lt;SalesTable rows={rows} /&gt;\n      &lt;Suspense fallback={&lt;ChartPlaceholder /&gt;}&gt;\n        &lt;MeasuredSalesChart rows={rows} /&gt;\n      &lt;/Suspense&gt;\n    &lt;/section&gt;\n  )\n}\n</code></pre>\n<p><code>MeasuredSalesChart</code> can call <code>use(browser())</code>, while the semantic content remains in the response.</p>\n<h3>useLayoutEffect has several remedies</h3>\n<p>React's <code>useLayoutEffect</code> documentation lists browser-only rendering as one option for components that need layout information. Other options include switching to <code>useEffect</code>, rendering after hydration, or using <code>useSyncExternalStore</code> when synchronizing with an external store.</p>\n<p>Choose based on behavior. A tooltip that appears only after a click does not need to exist in the initial HTML. A navigation menu does.</p>\n<h3>Third-party modules may fail before rendering</h3>\n<p><code>use(browser())</code> runs during component rendering. It cannot protect against a module that reads <code>window</code> at top-level import time on the server.</p>\n<p>For those packages, use the framework's client-only dynamic import or isolate the import behind a browser entry point. The failure happens before React reaches the component body.</p>\n<h2>browser() vs next/dynamic</h2>\n<p>Next.js applications now have two clear browser-only mechanisms, plus the older mounted-state technique.</p>\n<h3>Use browser() for rendering semantics</h3>\n<p>Choose <code>use(browser())</code> when the component module can load safely, the fallback belongs in Suspense, and you want the same React primitive across server-rendering environments.</p>\n<p>The component remains in the client bundle. React simply avoids rendering its content on the server.</p>\n<h3>Use next/dynamic for lazy module loading</h3>\n<p>Next.js documents <code>next/dynamic</code> with <code>{ ssr: false }</code> for a Client Component that should load only in the browser. This is especially useful for a heavy editor, map SDK, or package that touches browser globals when imported.</p>\n<pre><code class=\"language-tsx\">'use client'\n\nimport dynamic from 'next/dynamic'\n\nconst MapEditor = dynamic(() =&gt; import('./map-editor'), {\n  ssr: false,\n  loading: () =&gt; &lt;MapPlaceholder /&gt;,\n})\n</code></pre>\n<p>The option must live in a Client Component. Next.js does not allow <code>ssr: false</code> in a Server Component.</p>\n<h3>Compare the trade-offs</h3>\n<table>\n<thead>\n<tr><th>Pattern</th><th>Server HTML</th><th>Code splitting</th><th>Fallback owner</th><th>Best fit</th></tr>\n</thead>\n<tbody>\n<tr><td><code>use(browser())</code></td><td>Nearest Suspense fallback</td><td>No additional split by itself</td><td>React Suspense</td><td>Browser-only render with a safe module</td></tr>\n<tr><td><code>next/dynamic({ ssr: false })</code></td><td>Dynamic loading fallback</td><td>Yes</td><td>Next dynamic loader</td><td>Browser-dependent or heavy module</td></tr>\n<tr><td>Mounted state in <code>useEffect</code></td><td>Initial branch output</td><td>No</td><td>Component state</td><td>Legacy code or behavior tied to post-hydration effect</td></tr>\n</tbody>\n</table>\n<p>The techniques can coexist, but layering all three around one component usually signals that the boundary needs simplification.</p>\n<h2>Design Better Suspense Fallbacks</h2>\n<p>The fallback is the server-rendered experience, not decorative loading chrome.</p>\n<h3>Preserve layout</h3>\n<p>Give the fallback dimensions close to the final component so hydration does not move surrounding content. A canvas or editor placeholder should reserve height, while a short text label can use a stable inline width.</p>\n<p>Avoid a full-page spinner for a small browser-only control. It throws away the speed and accessibility benefits of server rendering.</p>\n<h3>Preserve meaning</h3>\n<p>If the final widget is optional, the fallback can explain that it appears after the page becomes interactive. If the value affects a form, label the pending field so assistive technology does not encounter an unexplained blank region.</p>\n<p>Use <code>aria-busy</code> on an appropriate container when the state will update. Do not announce every small island as a live-region event after hydration.</p>\n<h3>Expect no-JavaScript behavior</h3>\n<p>Browser-only content never replaces its fallback when JavaScript is unavailable or blocked. Decide whether that is acceptable.</p>\n<p>Critical navigation, purchase information, legal text, and primary article content should not depend on <code>browser()</code>. A saved local draft, interactive chart, or device capability control often can.</p>\n<h2>Observe Browser Bailouts</h2>\n<p>An explicit API makes browser-only rendering measurable.</p>\n<h3>Provide a reason</h3>\n<p><code>browser</code> accepts an optional string or function. React passes the value as the <code>cause</code> of the error reported to <code>onBrowserBailout</code> during server rendering.</p>\n<pre><code class=\"language-tsx\">use(browser(() =&gt; new Error('Local draft requires browser storage.')))\n</code></pre>\n<p>A function is useful when constructing the reason is expensive because React does not call it in the browser.</p>\n<h3>Use onBrowserBailout in a custom renderer</h3>\n<p>Applications that call <code>renderToPipeableStream</code> directly can record intentional bailouts separately from failures.</p>\n<pre><code class=\"language-tsx\">const { pipe } = renderToPipeableStream(&lt;App /&gt;, {\n  onShellReady() {\n    pipe(response)\n  },\n  onBrowserBailout(error, info) {\n    logger.info('browser-only-render', {\n      cause: String(error.cause),\n      componentStack: info.componentStack,\n    })\n  },\n  onError(error) {\n    logger.error('server-render-failed', { error })\n  },\n})\n</code></pre>\n<p>Frameworks may decide how much of this callback they expose. Check the framework release that adds React 19.3 support instead of assuming a low-level option is configurable.</p>\n<h3>Watch for expanding boundaries</h3>\n<p>Count bailouts by reason and route. A sudden increase may mean a shared component began calling <code>browser()</code> higher in the tree, removing more server-rendered content than intended.</p>\n<p>Do not treat every bailout as an error. Alert on unknown reasons, missing boundaries, or large shifts in frequency.</p>\n<h2>Test Both Rendering Environments</h2>\n<p>A client-only unit test proves only half the contract.</p>\n<h3>Assert the server fallback</h3>\n<p>Render the tree through a supported server renderer and confirm that the fallback appears while the browser-only content does not. Include a test without Suspense and verify that it fails in the expected way.</p>\n<p>If the framework owns the renderer, use an integration test against a built route. Static snapshots of the component function will not model Suspense behavior accurately.</p>\n<h3>Assert hydration behavior</h3>\n<p>Load the server HTML in a real browser, seed <code>localStorage</code> or the relevant device API, hydrate, and assert that the component replaces the fallback without hydration warnings.</p>\n<p>For time zones and locales, run the test in at least two browser contexts. A test that happens to use the same zone as the build machine misses the original class of bug.</p>\n<h3>Test accessibility before and after</h3>\n<p>Run automated checks on the server HTML and hydrated page. Verify focus order when a browser-only control appears. A fallback button that disappears and is replaced by another focused element can strand keyboard users.</p>\n<p>Prefer stable surrounding structure and avoid auto-focusing the newly rendered component unless the user's action requested it.</p>\n<h2>Migration Strategy</h2>\n<p>Do not search for every <code>useEffect(() =&gt; setMounted(true))</code> and replace it mechanically.</p>\n<h3>Inventory the reason for each workaround</h3>\n<p>Classify candidates by dependency: storage, time zone, layout, URL, third-party module import, browser cache, media device, or historical habit. Some components can be server-rendered after moving data loading up the tree.</p>\n<p>Remove workarounds that no longer protect anything. Keep effects that perform real synchronization.</p>\n<h3>Start with leaf components</h3>\n<p>Convert small widgets under existing layout boundaries. Add Suspense with a purposeful fallback and tests for server HTML plus hydration.</p>\n<p>Avoid starting with a root layout or provider. A browser bailout high in the tree can turn an entire page into fallback HTML.</p>\n<h3>Compare the production output</h3>\n<p>Measure HTML completeness, JavaScript size, layout shift, time to visible content, and hydration warnings before and after. <code>browser()</code> can simplify code while worsening the initial experience if used too broadly.</p>\n<p>The best migration sometimes removes the browser-only condition entirely by passing a server value. Code reduction is useful, but preserved server content matters more.</p>\n<h2>When browser() Is the Wrong Choice</h2>\n<p>The API is attractive because it turns several lines of workaround code into one call. That does not mean the component should stop rendering on the server.</p>\n<h3>Do not hide deterministic content</h3>\n<p>If a value comes from props, route data, a cookie available to the server, or a database query, render it normally. Moving deterministic content behind a browser boundary increases the amount of empty or placeholder HTML and delays useful information until hydration.</p>\n<p>This matters for product names, prices, availability, article text, account status, and form instructions. Search crawlers and link unfurlers may not execute the same client path as a browser. More importantly, a person on a slow device sees the fallback for longer even though the server already knew the answer.</p>\n<h3>Do not use it as an error suppressor</h3>\n<p>A hydration warning may reveal an unstable render, invalid nesting, a CSS-in-JS configuration error, or data that changed between response generation and hydration. Wrapping the component in <code>use(browser())</code> can remove the warning by removing the server output, but it does not explain the underlying difference.</p>\n<p>Identify the mismatched value first. If both environments should agree, fix the data flow. A deliberate browser-only boundary is appropriate only when agreement is impossible or the server output would be misleading.</p>\n<h3>Do not move authorization into the browser</h3>\n<p>A browser-only component is not a security boundary. Its JavaScript and network requests are visible to the user, and skipping server rendering does not protect private data.</p>\n<p>Authenticate and authorize requests on the server. The client may decide how to present an allowed action, but the API must enforce whether the action is permitted. Never use <code>browser()</code> to conceal an admin control while leaving its endpoint unprotected.</p>\n<h3>Do not confuse rendering with capability detection</h3>\n<p>A component may render a stable shell on the server and check a capability after hydration. For example, a camera upload page can show instructions and an ordinary file picker in the initial HTML, then add direct camera capture when <code>navigator.mediaDevices</code> is available.</p>\n<p>In that case, an effect or external-store subscription may be a better fit because the base interface remains useful. Skip the whole component only when its purpose depends on the browser capability.</p>\n<h3>Do not expand the boundary to simplify imports</h3>\n<p>If one dependency fails during server import, isolate that dependency. Moving a parent layout behind <code>browser()</code> may appear to solve the problem while causing unrelated headings, forms, and data to disappear from the server response.</p>\n<p>Use a client-only dynamic import for the unsafe module, wrap the smallest component that owns it, or replace the package with one that supports server environments. The boundary should describe the UI requirement, not the location where an import error was easiest to silence.</p>\n<h2>Production Checklist</h2>\n<p>Before shipping a component with <code>use(browser())</code>:</p>\n<ol>\n<li>Confirm the component is a Client Component.</li>\n<li>Place it under the smallest useful Suspense boundary.</li>\n<li>Provide a fallback with stable dimensions and meaningful content.</li>\n<li>Verify the module itself does not access browser globals during server import.</li>\n<li>Check whether a real server value could avoid the bailout.</li>\n<li>Keep primary content and critical navigation server-rendered.</li>\n<li>Add a reason that contains no secrets or user data.</li>\n<li>Test generated server HTML, browser hydration, and the no-JavaScript state.</li>\n<li>Check focus order and layout shift when the component appears.</li>\n<li>Use <code>next/dynamic</code> instead when lazy import behavior is part of the requirement.</li>\n<li>Track bailouts where the renderer or framework exposes them.</li>\n<li>Revisit the boundary if a leaf requirement starts removing a large region of HTML.</li>\n</ol>\n<p><code>browser()</code> removes the fake state often used to model whether hydration has happened. It also makes the cost visible: the server emits a fallback and the browser owns the real render. Use that trade deliberately, keep the boundary small, and let the rest of the page benefit from server rendering.</p>\n<p>For broader framework choices, see <a href=\"/blog/react-vs-nextjs-which-to-choose\">React vs Next.js</a>. <a href=\"/blog/react-typescript-vite-vitest-setup-guide-2026\">React, TypeScript, Vite, and Vitest Setup</a> covers a client-rendered baseline, while <a href=\"/blog/nextjs-16-2-complete-guide\">Next.js 16.2</a> covers current server-rendered application patterns.</p>\n<h2>Official Sources</h2>\n<ul>\n<li><a href=\"https://react.dev/blog/2026/09/09/react-19-3\">React 19.3 release</a></li>\n<li><a href=\"https://react.dev/reference/react-dom/browser\">React browser API reference</a></li>\n<li><a href=\"https://react.dev/reference/react/Suspense\">React Suspense reference</a></li>\n<li><a href=\"https://react.dev/reference/react/use\">React use API reference</a></li>\n<li><a href=\"https://react.dev/reference/react-dom/client/hydrateRoot\">React hydrateRoot reference</a></li>\n<li><a href=\"https://react.dev/reference/react-dom/server/renderToPipeableStream\">React renderToPipeableStream reference</a></li>\n<li><a href=\"https://react.dev/reference/react/useLayoutEffect\">React useLayoutEffect troubleshooting</a></li>\n<li><a href=\"https://nextjs.org/docs/app/guides/single-page-applications\">Next.js guide to single-page applications</a></li>\n<li><a href=\"https://nextjs.org/docs/app/guides/lazy-loading\">Next.js lazy loading guide</a></li>\n<li><a href=\"https://nextjs.org/docs/app/getting-started/server-and-client-components\">Next.js Server and Client Components</a></li>\n</ul>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
