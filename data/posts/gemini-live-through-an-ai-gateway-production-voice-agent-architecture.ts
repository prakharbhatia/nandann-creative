import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "gemini-live-through-an-ai-gateway-production-voice-agent-architecture",
  title: "Gemini Live Through an AI Gateway: A Production Voice-Agent Architecture",
  description: "Learn how to build a production Gemini Live gateway with WebSockets, session state, interruptions, tools, routing, privacy, observability, and fallbacks.",
  date: "2026-09-16",
  readTime: "35 min",
  category: "Agentic AI",
  tags: ["Gemini Live","voice agents","AI gateways","WebSockets","real-time AI","agent architecture","Extended Thinking","production engineering"],
  faqs: [
      {
          "question": "What is the difference between a normal AI gateway and a live voice gateway?",
          "answer": "A normal AI gateway handles request-level concerns such as model selection, authorization, cost tracking, retries, caching, and rejection. A live voice gateway manages an ongoing session with continuous audio, model events, tool requests, interruptions, timers, backpressure, and connection failures."
      },
      {
          "question": "Why should a live voice gateway be designed around sessions rather than HTTP requests?",
          "answer": "Voice interactions contain several independent streams and clocks, including transport time, turn-detection time, model time, and business-operation time. Treating the interaction as a live session lets the gateway represent states such as speaking, interrupted, waiting for a tool, degrading, and closing without forcing every transition into a request-and-response model."
      },
      {
          "question": "What connections are used in a Gemini Live gateway architecture?",
          "answer": "The usual design has one WebSocket between the client and the gateway and a second long-lived live connection between the gateway and the model provider. The gateway owns the relationship between those connections and applies authentication, routing, session state, backpressure, cancellation, tool handling, and policy checks rather than blindly piping bytes."
      },
      {
          "question": "How should audio and control events be handled in a voice gateway?",
          "answer": "Audio frames and control events should use separate internal message types. Audio is frequent and can be buffered with bounded capacity, while commands such as cancellation, stopping, muting, and resuming are sparse and consequential and should not be delayed behind an audio backlog."
      },
      {
          "question": "How should standard Gemini Live and Extended Thinking be routed?",
          "answer": "A practical first version uses session-level routing, selecting a standard live route for normal conversations and an Extended Thinking route when requested or permitted by policy. Extended Thinking should have a separate latency and cost budget, and selected complex tasks can be delegated to a reasoning route while the live conversational path remains responsive."
      },
      {
          "question": "Should a gateway switch models on every audio turn?",
          "answer": "No. Switching models during a live turn can lose conversational state, tool context, audio assumptions, or interruption state. If adaptive routing is needed, use explicit boundaries such as a new session, a completed user turn, a separate reasoning job, or a controlled handoff containing a compact session summary."
      },
      {
          "question": "What should happen when audio arrives faster than the upstream model can process it?",
          "answer": "The gateway should use a bounded, observable audio buffer and make an explicit session-level decision when it fills. Possible responses include asking the client to reduce capture rate, pausing or rejecting input, entering degraded mode, terminating cleanly with reconnect support, or switching to a safe fallback route when the session can be re-established."
      },
      {
          "question": "What responsibilities should the gateway own for tool calls?",
          "answer": "The gateway should authorize tool requests, validate arguments, enforce tenant and user policy, apply deadlines, execute tools through a controlled runner, and enforce idempotency for side effects. The model may request a tool, but it should not independently determine whether the caller is authorized to perform the underlying action."
      }
  ],
  coverImage: "/images/gemini-live-through-an-ai-gateway-production-voice-agent-architecture.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/gemini-live-through-an-ai-gateway-production-voice-agent-architecture.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/gemini-live-through-an-ai-gateway-production-voice-agent-architecture.webp\" alt=\"Gemini Live Through an AI Gateway: A Production Voice-Agent Architecture\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<h2>A Live Voice Gateway Is A Runtime, Not A Model Proxy</h2>\n<p>A normal AI gateway answers request-level questions:</p>\n<ul>\n<li>Which model should receive this request?</li>\n<li>Is the caller allowed to use it?</li>\n<li>How much did it cost?</li>\n<li>Should it be retried, cached, or rejected?</li>\n</ul>\n<p>A voice gateway answers those questions while a conversation is already running. Audio arrives continuously. The model emits audio, text, tool requests, and errors. The user interrupts. Tools finish late. Connections disappear halfway through a turn.</p>\n<p>The unit of architecture is therefore not an HTTP request. It is a live session with several independent streams:</p>\n<pre><code class=\"language-text\">client microphone\n      |\n      | audio frames\n      v\n+-------------------+\n| Edge WebSocket    |\n| authentication    |\n| rate limits       |\n+-------------------+\n      |\n      v\n+-------------------+\n| Session manager   |\n| routing policy    |\n| state + timers    |\n+-------------------+\n      |\n      +----------------------+\n      |                      |\n      v                      v\n+-------------------+  +-------------------+\n| Gemini Live       |  | Tool runner       |\n| upstream session  |  | APIs, queues, DB  |\n+-------------------+  +-------------------+\n      |\n      | audio events, text,\n      | tool requests, errors\n      v\nclient speaker\n</code></pre>\n<p>The gateway should not sit in the audio path merely because every component is called an “AI service.” Keep the hot path small. Authentication, session ownership, routing, backpressure, cancellation, and policy checks belong there. Slow business operations usually don't.</p>\n<p>That distinction matters for Gemini 3.8 Live and Gemini 3.8 Live Extended Thinking. A voice session needs fast turn-taking even when some work requires deeper reasoning. If Extended Thinking blocks the conversational path, the assistant may be correct and still feel broken.</p>\n<h3>A Session Has More Than One Clock</h3>\n<p>A live session has at least four clocks:</p>\n<ol>\n<li><strong>Transport time:</strong> when audio frames arrive and leave.</li>\n<li><strong>Turn-detection time:</strong> when the system decides the user has started or stopped speaking.</li>\n<li><strong>Model time:</strong> when the upstream session emits audio, text, or a tool request.</li>\n<li><strong>Business-operation time:</strong> when a tool or downstream service finishes.</li>\n</ol>\n<p>These clocks should not be collapsed into one request timeout.</p>\n<p>A tool call may take longer than the acceptable delay for acknowledging the user. Depending on the supported interaction model, the assistant can keep the session responsive while the tool runs rather than holding the WebSocket in a vague waiting state. If the model or gateway can't provide useful interim behavior, track the tool operation separately and enforce its own deadline.</p>\n<p>An internal state model can make those distinctions explicit:</p>\n<pre><code class=\"language-ts\">type SessionState =\n  | \"connecting\"\n  | \"active\"\n  | \"user_speaking\"\n  | \"assistant_speaking\"\n  | \"tool_pending\"\n  | \"interrupting\"\n  | \"degraded\"\n  | \"closing\"\n  | \"closed\";\n\ntype LiveSession = {\n  id: string;\n  userId: string;\n  state: SessionState;\n  route: {\n    provider: string;\n    model: string;\n    mode: \"standard\" | \"extended-thinking\";\n  };\n  startedAt: number;\n  lastClientAudioAt?: number;\n  lastUpstreamEventAt?: number;\n  activeToolCallId?: string;\n  interruptionGeneration: number;\n};\n</code></pre>\n<p>The exact upstream event names depend on the Gemini Live interface exposed by the provider or the implementation. The important part is separation of concerns. A session manager should know whether the session is active, interrupted, waiting on a tool, or degrading without pretending that every transition is an ordinary request and response.</p>\n<h3>Keep Control Events Separate From Audio</h3>\n<p>Audio frames are frequent and relatively boring. Control events are sparse and consequential.</p>\n<p>Treat them differently in the internal protocol:</p>\n<pre><code class=\"language-ts\">type ClientMessage =\n  | {\n      type: \"audio\";\n      sessionId: string;\n      sequence: number;\n      payload: ArrayBuffer;\n    }\n  | {\n      type: \"control\";\n      sessionId: string;\n      command:\n        | \"start\"\n        | \"stop\"\n        | \"cancel_response\"\n        | \"mute\"\n        | \"resume\";\n    }\n  | {\n      type: \"tool_result\";\n      sessionId: string;\n      callId: string;\n      result: unknown;\n    };\n</code></pre>\n<p>This gives policy a clear home. A client should not be able to smuggle a control action into an audio message, and an audio queue should not delay a cancellation command behind a large backlog of frames.</p>\n<p>The same rule applies in the other direction. Model audio, model text, tool requests, usage events, and errors should not all arrive as one untyped stream.</p>\n<pre><code class=\"language-ts\">type GatewayEvent =\n  | { type: \"audio_out\"; payload: Uint8Array; sequence: number }\n  | { type: \"transcript\"; role: \"user\" | \"assistant\"; text: string }\n  | { type: \"tool_request\"; callId: string; name: string; args: unknown }\n  | { type: \"turn_started\"; role: \"user\" | \"assistant\" }\n  | { type: \"turn_ended\"; role: \"user\" | \"assistant\" }\n  | { type: \"interruption_applied\"; generation: number }\n  | { type: \"session_error\"; code: string; retryable: boolean };\n</code></pre>\n<p>A gateway that treats every event as a log line will eventually lose the difference between “the user stopped speaking,” “the model stopped speaking,” and “the upstream connection died.” Those are different operational events and need different responses.</p>\n<h2>The Core WebSocket Flow</h2>\n<p>The most reliable design is usually a pair of long-lived connections:</p>\n<ul>\n<li>one WebSocket between the client and the gateway</li>\n<li>one upstream live connection between the gateway and the model provider</li>\n</ul>\n<p>The gateway owns the relationship between them. It shouldn't blindly pipe bytes in both directions.</p>\n<pre><code class=\"language-ts\">async function handleLiveConnection(\n  client: WebSocket,\n  request: Request,\n) {\n  const identity = await authenticate(request);\n  const session = await createSession(identity);\n\n  const upstream = await connectToLiveModel({\n    sessionId: session.id,\n    model: session.route.model,\n    thinkingMode: session.route.mode,\n  });\n\n  const abort = new AbortController();\n\n  client.addEventListener(\"message\", async (event) =&gt; {\n    try {\n      const message = decodeClientMessage(event.data);\n\n      if (message.type === \"audio\") {\n        await session.audioIn.push(message);\n        return;\n      }\n\n      if (message.type === \"control\") {\n        await session.control.handle(message.command);\n        return;\n      }\n\n      if (message.type === \"tool_result\") {\n        await session.tools.resolve(message);\n      }\n    } catch (error) {\n      await reportProtocolError(client, error);\n    }\n  });\n\n  upstream.onEvent(async (event) =&gt; {\n    const gatewayEvents = await session.handleUpstreamEvent(event);\n\n    for (const gatewayEvent of gatewayEvents) {\n      sendToClient(client, gatewayEvent);\n    }\n  });\n\n  client.addEventListener(\"close\", async () =&gt; {\n    abort.abort();\n    await upstream.close();\n    await session.close(\"client_disconnected\");\n  });\n}\n</code></pre>\n<p>This sample deliberately leaves provider-specific connection and event names behind an adapter. That adapter is valuable even if there is only one model today.</p>\n<pre><code class=\"language-ts\">interface LiveModelAdapter {\n  connect(input: ConnectInput): Promise&lt;LiveModelConnection&gt;;\n}\n\ninterface LiveModelConnection {\n  sendAudio(frame: AudioFrame): Promise&lt;void&gt;;\n  sendControl(command: UpstreamCommand): Promise&lt;void&gt;;\n  sendToolResult(result: ToolResult): Promise&lt;void&gt;;\n  onEvent(handler: (event: ModelEvent) =&gt; void): void;\n  close(): Promise&lt;void&gt;;\n}\n</code></pre>\n<p>The adapter isn't pretending all providers behave the same. It is a boundary where differences become explicit. One provider may support native interruption semantics. Another may require the gateway to stop forwarding audio and discard buffered output. One may expose detailed usage events. Another may provide aggregate usage later.</p>\n<p>Represent those differences as capabilities rather than scattering conditionals through the session manager:</p>\n<pre><code class=\"language-ts\">type LiveCapabilities = {\n  nativeCancellation: boolean;\n  serverTurnDetection: boolean;\n  toolCallStreaming: boolean;\n  usageEvents: boolean;\n  resumableSessions: boolean;\n};\n</code></pre>\n<h3>Backpressure Is A Product Decision</h3>\n<p>Audio keeps arriving when the upstream model slows down. Without backpressure, the gateway has only bad choices:</p>\n<ul>\n<li>grow memory until the process is killed</li>\n<li>add latency by buffering indefinitely</li>\n<li>drop frames silently</li>\n<li>disconnect the user</li>\n</ul>\n<p>There's no universal answer because dropping audio and dropping control events have very different consequences.</p>\n<p>A practical buffer is bounded and observable:</p>\n<pre><code class=\"language-ts\">class AudioBuffer {\n  private frames: AudioFrame[] = [];\n  private readonly maxFrames: number;\n\n  constructor(maxFrames: number) {\n    this.maxFrames = maxFrames;\n  }\n\n  push(frame: AudioFrame) {\n    if (this.frames.length &gt;= this.maxFrames) {\n      throw new Error(\"audio_backpressure_limit\");\n    }\n\n    this.frames.push(frame);\n  }\n\n  shift(): AudioFrame | undefined {\n    return this.frames.shift();\n  }\n\n  clear() {\n    this.frames = [];\n  }\n\n  get size() {\n    return this.frames.length;\n  }\n}\n</code></pre>\n<p>In production, a full buffer should produce a metric and a session-level decision, not an unhandled exception.</p>\n<p>Possible decisions include:</p>\n<ul>\n<li>ask the client to reduce capture rate, if the protocol supports it</li>\n<li>pause or reject new audio input</li>\n<li>enter a degraded mode</li>\n<li>terminate the session cleanly and offer reconnect</li>\n<li>switch to a fallback route if the session can be re-established safely</li>\n</ul>\n<p>Be conservative about automatic retries. Retrying a stateless text request is one thing. Retrying a live audio stream can duplicate speech, reorder events, or cause the model to answer an old turn after the user has moved on.</p>\n<h3>Session Affinity Has To Be Explicit</h3>\n<p>A live session needs a clear owner. If the gateway runs on multiple instances, a later WebSocket event cannot depend on a random load-balancer decision.</p>\n<p>Common approaches include:</p>\n<ul>\n<li>keeping the entire session on one gateway process</li>\n<li>using WebSocket connection affinity</li>\n<li>storing session metadata in a shared store while stream handling stays local</li>\n<li>using a dedicated session-worker model</li>\n</ul>\n<p>The shared store should contain coordination data, not necessarily every audio frame. Persisting raw audio in a general-purpose session database creates cost, privacy, and throughput problems. If audio retention is required, treat it as a separate data pipeline with its own policy.</p>\n<pre><code class=\"language-ts\">type SessionRecord = {\n  id: string;\n  ownerInstance: string;\n  userId: string;\n  createdAt: string;\n  expiresAt: string;\n  route: string;\n  status: \"active\" | \"closing\" | \"closed\" | \"failed\";\n  lastHeartbeatAt: string;\n  interruptionGeneration: number;\n};\n</code></pre>\n<p>The owner instance should renew the lease while the session is healthy. If the lease expires, another instance may clean up stale state, but it should not automatically take over the audio stream unless the protocol and provider support safe resumption.</p>\n<h2>Routing Standard Live And Extended Thinking</h2>\n<p>Multi-model routing is useful when it solves a real constraint. HydraFusion is a useful reference point: its stated goal is runtime orchestration across models for a task-specific balance of performance, cost, and latency, with pricing based on token consumption by model. That is the general shape of a gateway policy, not a reason to add model hopping to every turn.</p>\n<p>Voice sessions make routing harder because the decision is usually made at session start while the user's needs change during the conversation.</p>\n<p>A sensible first version uses session-level routing:</p>\n<pre><code class=\"language-ts\">type RouteRequest = {\n  tenantId: string;\n  userId: string;\n  requestedMode: \"standard\" | \"extended-thinking\";\n  useCase: \"support\" | \"assistant\" | \"workflow\";\n  region: string;\n};\n\nfunction chooseRoute(input: RouteRequest): Route {\n  if (input.requestedMode === \"extended-thinking\") {\n    return {\n      provider: \"configured-provider\",\n      model: \"gemini-3.8-live-extended-thinking\",\n      reason: \"requested_by_policy\",\n    };\n  }\n\n  return {\n    provider: \"configured-provider\",\n    model: \"gemini-3.8-live\",\n    reason: \"default_live_route\",\n  };\n}\n</code></pre>\n<p>The model names represent the product distinction in this architecture. They should be configured rather than hard-coded because the available identifiers and provider endpoints must come from the actual service contract.</p>\n<h3>Don't Route Every Audio Turn Independently</h3>\n<p>Switching models in the middle of a live turn is risky. The new model may not have the same conversational state, tool context, audio assumptions, or interruption state.</p>\n<p>If adaptive routing is necessary, prefer explicit boundaries:</p>\n<ul>\n<li>route a new session differently</li>\n<li>route a new user turn after a completed turn</li>\n<li>route a separate tool or reasoning job</li>\n<li>create a controlled handoff with a compact session summary</li>\n</ul>\n<pre><code class=\"language-ts\">type HandoffContext = {\n  sessionId: string;\n  conversationSummary: string;\n  activeTask?: string;\n  userPreferences?: Record&lt;string, string&gt;;\n  pendingToolState?: {\n    callId: string;\n    name: string;\n    status: \"pending\";\n  };\n  reason: \"latency_degradation\" | \"capability_required\" | \"provider_failure\";\n};\n</code></pre>\n<p>The summary is data with privacy implications. Don't copy an entire transcript into every route by default. Decide what context is necessary, how long it remains available, and whether the user or tenant has opted out of retention.</p>\n<h3>Extended Thinking Needs A Separate Budget</h3>\n<p>Extended Thinking should not be configured as a free upgrade for every voice interaction. It can change both latency and cost, and it may be unnecessary for simple turns.</p>\n<p>One useful pattern is to keep the live conversational route responsive and delegate selected tasks to a reasoning route:</p>\n<pre><code class=\"language-text\">user asks a simple question\n    |\n    v\nstandard live route responds in the conversation\n\nuser asks for a complex comparison\n    |\n    +--&gt; live route acknowledges and frames the task\n    |\n    +--&gt; reasoning route evaluates the task\n    |\n    +--&gt; gateway returns a bounded result to the live session\n</code></pre>\n<p>The exact behavior depends on the supported model and tool protocol. The architectural point is to make the expensive path deliberate.</p>\n<pre><code class=\"language-ts\">function shouldUseExtendedThinking(input: {\n  requested: boolean;\n  taskClass: \"simple\" | \"multi_step\" | \"high_risk\";\n  tenantAllowsIt: boolean;\n}) {\n  if (!input.tenantAllowsIt) return false;\n  if (input.requested) return true;\n\n  return (\n    input.taskClass === \"multi_step\" ||\n    input.taskClass === \"high_risk\"\n  );\n}\n</code></pre>\n<p>This policy doesn't claim that a classifier can reliably identify every difficult request. It gives the system an auditable place to make the decision and a place to add limits later.</p>\n<h3>Model Hopping Makes Latency Less Predictable</h3>\n<p>HydraFusion's multi-model approach highlights the upside of runtime orchestration, but the trade-off matters for voice: every additional hop adds another possible delay and failure domain.</p>\n<pre><code class=\"language-text\">client\n  -&gt; gateway\n  -&gt; router\n  -&gt; model A\n  -&gt; tool\n  -&gt; model B\n  -&gt; gateway\n  -&gt; client\n</code></pre>\n<p>That can be the right architecture for a complex workflow. It is a poor default for every spoken turn.</p>\n<p>Measure the complete path:</p>\n<pre><code class=\"language-ts\">type TurnTiming = {\n  sessionId: string;\n  turnId: string;\n  captureStartedAt?: number;\n  gatewayReceivedAt?: number;\n  upstreamSentAt?: number;\n  firstModelEventAt?: number;\n  firstAudioOutAt?: number;\n  userInterruptAt?: number;\n  responseCompletedAt?: number;\n  toolStartedAt?: number;\n  toolCompletedAt?: number;\n};\n</code></pre>\n<p>First audio and completed response are separate measurements. A session can have a good time to first audio and still feel poor if it pauses repeatedly, ignores interruptions, or waits too long for a tool result.</p>\n<h2>What The Gateway Should Own</h2>\n<p>The gateway should own decisions that need consistency across clients, models, and providers.</p>\n<h3>Identity And Tenant Policy</h3>\n<p>Authenticate the client before opening an upstream live session. Bind that session to the authenticated tenant and user rather than trusting identifiers supplied in arbitrary client messages.</p>\n<p>Tenant policy can control:</p>\n<ul>\n<li>which live models are available</li>\n<li>whether Extended Thinking is allowed</li>\n<li>which tools may be called</li>\n<li>maximum concurrent sessions</li>\n<li>audio retention</li>\n<li>transcript retention</li>\n<li>regional routing</li>\n<li>budget limits</li>\n<li>fallback behavior</li>\n</ul>\n<p>Evaluate these policies at session creation and, where relevant, at each tool request. A session that was valid at connection time should not gain access to a newly enabled tool because the client changed a field in a message.</p>\n<h3>Tool Authorization And Execution</h3>\n<p>The model can request a tool, but it should not decide whether the caller is authorized to perform the underlying action.</p>\n<pre><code class=\"language-ts\">async function handleToolRequest(\n  session: LiveSession,\n  request: ToolRequest,\n) {\n  const policy = await loadToolPolicy(session.userId, request.name);\n\n  if (!policy.allowed) {\n    return {\n      callId: request.callId,\n      status: \"denied\",\n      error: \"tool_not_allowed\",\n    };\n  }\n\n  const args = validateToolArguments(request.name, request.args);\n\n  const result = await runTool({\n    tenantId: session.tenantId,\n    userId: session.userId,\n    name: request.name,\n    args,\n    deadlineMs: policy.deadlineMs,\n  });\n\n  return {\n    callId: request.callId,\n    status: \"completed\",\n    result,\n  };\n}\n</code></pre>\n<p>The tool runner should enforce idempotency where an operation has side effects. A reconnect, duplicate model event, or client retry must not create two orders, send two messages, or execute the same administrative action twice.</p>\n<h3>Redaction And Data Handling</h3>\n<p>The gateway is one of the few places where audio, transcripts, identity, routing decisions, and tool activity meet. That makes it a useful privacy control point and a tempting place to log too much.</p>\n<p>Default logs should contain metadata rather than raw content:</p>\n<pre><code class=\"language-json\">{\n  \"event\": \"live_turn_completed\",\n  \"session_id\": \"session_opaque_id\",\n  \"tenant_id\": \"tenant_opaque_id\",\n  \"route\": \"standard_live\",\n  \"duration_ms\": 0,\n  \"interrupted\": true,\n  \"tool_count\": 1,\n  \"audio_retained\": false\n}\n</code></pre>\n<p>The zero duration is illustrative, not a measurement. In a real event, populate it from the timing record.</p>\n<p>If transcript or audio capture is enabled, make it an explicit policy decision. Record:</p>\n<ul>\n<li>whether capture was enabled</li>\n<li>why it was enabled</li>\n<li>who can access it</li>\n<li>how long it is retained</li>\n<li>how deletion works</li>\n<li>whether sensitive fields are redacted</li>\n<li>whether provider-side retention is controlled separately</li>\n</ul>\n<p>Turning off application logging does not necessarily turn off provider retention. Provider data handling is a separate contract and needs to be verified against the actual service documentation.</p>\n<h3>Health, Capacity, And Failover</h3>\n<p>A live gateway needs health signals at several levels:</p>\n<ul>\n<li>edge health</li>\n<li>WebSocket acceptance rate</li>\n<li>upstream connection success</li>\n<li>active sessions</li>\n<li>audio backlog</li>\n<li>model event delay</li>\n<li>tool latency</li>\n<li>clean versus abnormal session termination</li>\n<li>provider errors</li>\n<li>budget consumption</li>\n</ul>\n<p>The August 2026 GitHub availability report is a useful reminder that capacity monitoring, retry policy, and resiliency planning are not abstract infrastructure concerns. A gateway that depends on one cloud path or one database primary inherits that dependency's failure modes. The report's discussion of Azure migration and database-primary considerations is a relevant example of why “we have a second application instance” is not the same as end-to-end failover.</p>\n<p>For live sessions, failover should be stated precisely. It might mean:</p>\n<ul>\n<li>new sessions are routed to another provider</li>\n<li>existing sessions receive a clean reconnect option</li>\n<li>tool execution continues while the audio route is rebuilt</li>\n<li>the gateway stops accepting new sessions during capacity pressure</li>\n<li>the client gets a degraded text-only mode</li>\n</ul>\n<p>It usually does not mean transparently moving an active audio stream to another model with no user-visible effect. That requires explicit support for state transfer and careful testing. A generic retry loop is not a session failover strategy.</p>\n<h2>Real-Time Audio Flow And Session Semantics</h2>\n<p>The user experience is determined by the complete path, not model latency alone:</p>\n<pre><code class=\"language-text\">microphone\n  -&gt; capture buffer\n  -&gt; encode\n  -&gt; client WebSocket\n  -&gt; edge\n  -&gt; session runtime\n  -&gt; model input\n  -&gt; model output\n  -&gt; gateway output buffer\n  -&gt; client decoder\n  -&gt; speaker\n</code></pre>\n<p>Every boundary can add delay. Some delays are unavoidable. Others are accidental, such as waiting for a database write before forwarding audio or buffering too much input while waiting for a turn to end.</p>\n<p>The gateway should measure each segment separately. Otherwise, a provider improvement can be hidden by a client-side buffer, or a network problem can be misdiagnosed as slow generation.</p>\n<h3>Audio Capture, Encoding, And Transport</h3>\n<p>The client should capture audio in the format expected by the live model integration or convert it once at a clearly defined boundary. Sample rate, channel layout, sample format, and codec affect bandwidth, CPU usage, voice activity detection, and buffering.</p>\n<p>Use binary WebSocket frames for audio where the integration supports them. Keep control events in a separate typed envelope.</p>\n<pre><code class=\"language-ts\">type ControlEvent =\n  | {\n      type: \"session.start\";\n      sessionId: string;\n      clientVersion: string;\n    }\n  | {\n      type: \"turn.cancel\";\n      turnId: string;\n      reason: \"user_interrupt\" | \"timeout\" | \"policy\";\n    }\n  | {\n      type: \"tool.confirm\";\n      requestId: string;\n    };\n\ntype AudioFrame = {\n  sequence: number;\n  capturedAtMs: number;\n  bytes: Uint8Array;\n};\n</code></pre>\n<p>The client should attach a monotonically increasing sequence number to audio frames. The gateway can use it to detect gaps, estimate transport delay, and distinguish a late frame from a duplicate.</p>\n<p>Don't assume every missing frame should be retransmitted. In conversational audio, waiting for an old frame can be worse than dropping it and continuing. The correct behavior depends on the model protocol and product tolerance for gaps. Make the policy explicit.</p>\n<h3>Session Lifecycle And WebSocket Semantics</h3>\n<p>A session should have a state machine rather than a collection of boolean flags:</p>\n<pre><code class=\"language-text\">NEW\n  |\n  v\nAUTHENTICATING --&gt; REJECTED\n  |\n  v\nSTARTING_MODEL --&gt; FAILED\n  |\n  v\nACTIVE &lt;------+\n  |           |\n  |           v\n  |      RECONNECTING\n  |           |\n  v           +----&gt; ACTIVE\nINTERRUPTING\n  |\n  v\nCLOSING --&gt; CLOSED\n</code></pre>\n<p>A handshake should establish:</p>\n<ul>\n<li>authenticated principal</li>\n<li>tenant and policy context</li>\n<li>session identifier</li>\n<li>supported client capabilities</li>\n<li>selected model class or routing policy</li>\n<li>audio contract</li>\n<li>session expiry or maximum duration</li>\n<li>resume behavior, if supported</li>\n</ul>\n<p>Keep-alives should prove that both the WebSocket and the session runtime are alive. A WebSocket can remain open while the model connection behind it has failed. Application-level health events can detect that distinction.</p>\n<p>Reconnect handling is where many stateful designs become vague. Decide what the client sends after reconnect:</p>\n<ul>\n<li>last received server event sequence</li>\n<li>last played audio sequence</li>\n<li>session identifier and reconnect token</li>\n<li>client capability information</li>\n<li>whether the microphone was still active</li>\n</ul>\n<p>The gateway can then choose among several strategies:</p>\n<ol>\n<li>Resume the existing runtime.</li>\n<li>Reattach the client to a still-running runtime.</li>\n<li>Start a new model session with a compact summary.</li>\n<li>Reject resume and require a new session.</li>\n</ol>\n<p>Don't promise transparent resume unless the system can preserve the relevant state. Replaying all previous audio frames is an expensive and semantically risky substitute for recovery.</p>\n<h3>Latency Budgets And Measurement</h3>\n<p>The supplied material does not provide Gemini 3.8 Live-specific latency targets, so any production target must come from measurement rather than a provider guarantee.</p>\n<p>A useful budget breaks the user-visible delay into components:</p>\n<pre><code class=\"language-text\">time to first playable response\n  = capture and turn detection\n  + client-to-gateway transport\n  + gateway scheduling\n  + model time to first audio\n  + gateway-to-client transport\n  + client playback buffer\n</code></pre>\n<p>For a tool-backed response:</p>\n<pre><code class=\"language-text\">tool path\n  = tool authorization\n  + tool execution\n  + result transfer\n  + model continuation\n</code></pre>\n<p>Measure at least:</p>\n<ul>\n<li>user speech end to gateway turn detection</li>\n<li>turn detection to provider request</li>\n<li>provider request to first model audio</li>\n<li>first model audio to first speaker playback</li>\n<li>user interruption to output cancellation</li>\n<li>tool request to tool result</li>\n<li>tool result to resumed model audio</li>\n<li>reconnect start to usable session</li>\n</ul>\n<p>Use client and gateway clocks carefully. Clock skew can make distributed timestamps look precise while being wrong. Where possible, measure durations inside one process or use a synchronized tracing system.</p>\n<p>Latency percentiles matter more than averages. A smooth median with a long tail can still produce a frustrating voice experience. Break down tail latency by model route, tool, region, network condition, and interruption status.</p>\n<p>Avoid hiding queueing time inside “model latency.” If the gateway waits for a worker before opening the provider request, that is gateway scheduling delay.</p>\n<h3>Interruption Handling</h3>\n<p>Interruption is normal conversation behavior. It is not an exceptional error.</p>\n<p>When the user begins speaking while the model is producing audio, the gateway should:</p>\n<ol>\n<li>detect or receive the interruption signal</li>\n<li>stop or mark current output as cancelled</li>\n<li>cancel the active model turn where supported</li>\n<li>cancel or quarantine tool work that is no longer relevant</li>\n<li>advance the session to the new user turn</li>\n<li>prevent late events from the old turn reaching the speaker</li>\n</ol>\n<p>The last step is easy to miss. Cancellation is asynchronous. A provider may emit an output event after the gateway requests cancellation. Every output event should carry a turn or generation identifier.</p>\n<pre><code class=\"language-ts\">type OutputEvent = {\n  sessionId: string;\n  turnId: string;\n  generation: number;\n  kind: \"audio\" | \"transcript\" | \"tool_request\";\n  payload: unknown;\n};\n\nclass TurnGate {\n  private activeGeneration = 0;\n\n  beginTurn(): number {\n    this.activeGeneration += 1;\n    return this.activeGeneration;\n  }\n\n  accepts(event: OutputEvent): boolean {\n    return event.generation === this.activeGeneration;\n  }\n}\n</code></pre>\n<p>A tool call needs its own interruption policy. A read-only lookup may be allowed to finish and populate a short-lived cache. A side-effecting action should be cancelled if possible, or protected by an idempotency key if cancellation races with execution.</p>\n<p>Fallback behavior should preserve the conversational contract:</p>\n<ul>\n<li>if the live model is temporarily unavailable, speak a short service message</li>\n<li>if a tool is slow, acknowledge the request and wait within a bounded time or explain that the information is unavailable</li>\n<li>if audio output fails but text is available, offer a text response</li>\n<li>if repeated interruptions make the stream unstable, reset the turn or ask the user to repeat</li>\n<li>if the session cannot be safely resumed, close it clearly and provide a way to start again</li>\n</ul>\n<p>Graceful degradation is not pretending nothing failed. It is making the failure understandable without leaking internal details or speaking stale output.</p>\n<h2>Tool Calls And Selective Multi-Model Orchestration</h2>\n<p>The most useful multi-model design is selective.</p>\n<p>HydraFusion's central idea is runtime orchestration across models for task-specific tradeoffs between quality, cost, and latency. That maps naturally to a voice gateway, but the voice version needs stricter boundaries. A user is waiting in real time, and a model switch can alter timing, context handling, tool semantics, and recovery behavior.</p>\n<p>Treat routing as a state transition with a reason, not as a hidden optimization performed on every event.</p>\n<h3>Stable Routing Paths</h3>\n<p>A practical policy can start with a few stable paths:</p>\n<pre><code class=\"language-text\">ordinary conversation\n  -&gt; Gemini Live\n\nconversation requiring an approved tool\n  -&gt; Gemini Live\n  -&gt; tool executor\n  -&gt; Gemini Live continuation\n\ndeep or complex request\n  -&gt; policy check\n  -&gt; Extended Thinking path\n  -&gt; concise spoken answer\n\nprovider degradation\n  -&gt; approved fallback route\n  -&gt; constrained session behavior\n</code></pre>\n<p>Use signals available before the decision:</p>\n<ul>\n<li>session mode</li>\n<li>user or tenant policy</li>\n<li>request category</li>\n<li>tool requirement</li>\n<li>provider health</li>\n<li>current capacity</li>\n<li>budget remaining</li>\n<li>whether the current turn can tolerate additional latency</li>\n</ul>\n<p>Semantic routing can help, but an unconstrained classifier in the critical path may cost enough time to erase the benefit of the selected model. Classify at stable boundaries where possible.</p>\n<pre><code class=\"language-ts\">type RouteContext = {\n  sessionMode: \"conversation\" | \"assisted\";\n  requestClass: \"ordinary\" | \"tool\" | \"deep\" | \"unknown\";\n  providerHealthy: boolean;\n  budgetAllowsDeepPath: boolean;\n};\n\nfunction chooseRoute(ctx: RouteContext) {\n  if (!ctx.providerHealthy) return \"fallback\";\n  if (ctx.requestClass === \"deep\" &amp;&amp; ctx.budgetAllowsDeepPath) {\n    return \"extended-thinking\";\n  }\n  return \"live\";\n}\n</code></pre>\n<p>The fallback route should contain only integrations tested for audio contract, session semantics, privacy, and recovery. The supplied notes don't identify a specific alternate provider or model.</p>\n<p>Parallel model execution deserves caution. Running several agents at once can help offline exploration, and isolated worktrees are useful for preventing concurrent coding tasks from interfering with one another. In a live voice session, however, parallelism multiplies token usage and produces competing outputs.</p>\n<p>Use parallel calls for bounded, non-user-facing work where the result justifies the cost. Don't stream multiple speculative voices to the caller and hope the gateway chooses the right one later.</p>\n<h3>Tool Authorization, Execution, And Results</h3>\n<p>A tool call introduces a second runtime into the conversation. The model may request a search, summary, account lookup, or data fetch, but the gateway remains responsible for authorization, validation, timeout, and result handling.</p>\n<pre><code class=\"language-text\">model requests tool\n  -&gt; gateway validates tool name\n  -&gt; gateway validates arguments\n  -&gt; policy authorizes operation\n  -&gt; executor runs with timeout\n  -&gt; result is redacted and normalized\n  -&gt; model receives bounded result\n  -&gt; user receives response audio\n</code></pre>\n<p>The model should receive only fields required to complete the turn. A database row, internal error message, or full search-result payload should not pass through by default.</p>\n<pre><code class=\"language-ts\">type ToolResult =\n  | {\n      ok: true;\n      requestId: string;\n      data: unknown;\n      truncated: boolean;\n    }\n  | {\n      ok: false;\n      requestId: string;\n      code: \"timeout\" | \"denied\" | \"unavailable\" | \"invalid\";\n      retryable: boolean;\n    };\n\nasync function runTool(\n  request: ToolRequest,\n  policy: ToolPolicy\n): Promise&lt;ToolResult&gt; {\n  const decision = authorizeTool(request, policy);\n\n  if (decision === \"deny\") {\n    return {\n      ok: false,\n      requestId: request.requestId,\n      code: \"denied\",\n      retryable: false\n    };\n  }\n\n  if (decision === \"confirm\") {\n    return {\n      ok: false,\n      requestId: request.requestId,\n      code: \"denied\",\n      retryable: false\n    };\n  }\n\n  try {\n    const data = await executeWithTimeout(\n      request.toolName,\n      request.arguments,\n      policy.maxExecutionMs\n    );\n\n    return {\n      ok: true,\n      requestId: request.requestId,\n      data: limitToolResult(data),\n      truncated: wasTruncated(data)\n    };\n  } catch {\n    return {\n      ok: false,\n      requestId: request.requestId,\n      code: \"unavailable\",\n      retryable: true\n    };\n  }\n}\n</code></pre>\n<p>A confirmation state should be represented explicitly in the real protocol rather than collapsed into <code>denied</code>. Model intent, user approval, and execution authorization are separate decisions.</p>\n<p>For side-effecting tools, use a request ID or idempotency key. If the WebSocket disconnects after the tool executes but before the result reaches the client, a retry must not create a second side effect.</p>\n<h3>Cost-Aware Routing</h3>\n<p>HydraFusion's research-preview pricing model is based on model token consumption, which makes the cost implication of orchestration straightforward: every additional model path can add to the bill. Voice sessions add duration, transcript context, tool results, and parallel work.</p>\n<p>A cost policy should be attached to the session and checked at route boundaries:</p>\n<pre><code class=\"language-text\">session budget\n  |\n  +-- live conversation allocation\n  +-- tool-result allocation\n  +-- extended-thinking reserve\n  +-- recovery/fallback reserve\n</code></pre>\n<p>Don't treat the budget as a single number that the model can consume freely. Reserve capacity for recovery. If a session spends everything on an early expensive operation, the system may have no room for a final response or fallback.</p>\n<p>Useful controls include:</p>\n<ul>\n<li>cap Extended Thinking turns per session</li>\n<li>limit tool-call retries</li>\n<li>bound tool-result size</li>\n<li>avoid parallel model calls unless required</li>\n<li>summarize older context instead of growing it indefinitely</li>\n<li>stop or downgrade sessions that exceed policy limits</li>\n<li>apply tenant-level concurrency limits</li>\n<li>record route and usage metadata for cost attribution</li>\n</ul>\n<p>Cost and latency often point in the same direction, but not always. A cheaper model may require more retries or tool calls. A more expensive model may resolve a request in one pass. Measure cost per successful user task, not only cost per invocation.</p>\n<h2>Observability That Follows The User Experience</h2>\n<p>A live voice gateway can be technically available and still feel broken.</p>\n<p>The WebSocket may remain open. The model may return valid events. The tool may eventually succeed. Yet if the user waits too long after speaking, hears stale audio after an interruption, or gets silence while a downstream service retries, the system has failed from their point of view.</p>\n<p>Measure interactions rather than HTTP requests. Every user turn needs a correlation ID that follows audio frames, model events, tool calls, and playback state.</p>\n<h3>Latency Components And Budgets</h3>\n<p>A practical interaction budget includes:</p>\n<table>\n<thead>\n<tr><th>Component</th><th>What it measures</th><th>What to watch</th></tr>\n</thead>\n<tbody>\n<tr><td>Capture and encode</td><td>Speech becoming a transport frame</td><td>Client CPU and conversion</td></tr>\n<tr><td>Uplink transport</td><td>Client to gateway</td><td>Network distance and packet loss</td></tr>\n<tr><td>Gateway admission</td><td>Authentication, scheduling, forwarding</td><td>Queue depth and overloaded workers</td></tr>\n<tr><td>Model first audio</td><td>Forwarded input to first model audio</td><td>Model load and route</td></tr>\n<tr><td>Downlink transport</td><td>Model audio to client</td><td>Region placement and jitter</td></tr>\n<tr><td>Playback start</td><td>Audio becoming audible</td><td>Client buffer and decoder</td></tr>\n<tr><td>Turn completion</td><td>Final response or interruption</td><td>Turn detection and cancellation</td></tr>\n<tr><td>Tool round trip</td><td>Tool request through result</td><td>External latency and retries</td></tr>\n</tbody>\n</table>\n<p>For voice, time to first audible response is usually more important than total turn duration. A long answer can be acceptable if it starts promptly and can be interrupted cleanly. A short answer after a long silence feels much worse.</p>\n<p>Keep separate budgets for the normal path and degraded paths involving tools, model switches, retries, reconnects, or provider timeouts.</p>\n<pre><code class=\"language-text\">interaction_start\n  ├── audio_capture_ms\n  ├── uplink_ms\n  ├── gateway_queue_ms\n  ├── model_first_event_ms\n  ├── model_first_audio_ms\n  ├── downlink_ms\n  ├── playback_start_ms\n  ├── tool_wait_ms\n  └── interruption_to_playback_stop_ms\n</code></pre>\n<p>Track at least median, p95, and p99 for user-visible components. Break down the tail by model route, tool, region, network condition, and interruption status.</p>\n<h3>Telemetry Schema</h3>\n<p>Use traces for individual interactions, metrics for population behavior, and logs for state transitions that need investigation.</p>\n<p>A trace can show why one turn was slow. A metric can show that p95 first-audio latency increased across a region. A structured log can preserve the reason a session moved from active to degraded.</p>\n<pre><code class=\"language-text\">voice.session\n  ├── client.audio_ingest\n  ├── gateway.authenticate\n  ├── gateway.route\n  ├── provider.live_turn\n  │     ├── provider.first_event\n  │     ├── provider.first_audio\n  │     └── provider.turn_complete\n  ├── tool.invoke\n  │     └── tool.result\n  ├── gateway.interruption\n  └── client.playback\n</code></pre>\n<p>At the session level, record:</p>\n<ul>\n<li>session ID in a controlled form</li>\n<li>tenant or account reference</li>\n<li>gateway region</li>\n<li>provider route</li>\n<li>client and gateway protocol versions</li>\n<li>session start and end reason</li>\n<li>reconnect count</li>\n<li>total duration</li>\n<li>whether retention was enabled</li>\n</ul>\n<p>At the interaction level, record:</p>\n<ul>\n<li>interaction ID</li>\n<li>model route</li>\n<li>first event and first audio timestamps</li>\n<li>completion timestamp</li>\n<li>interruption state</li>\n<li>tool usage</li>\n<li>retry count</li>\n<li>terminal status</li>\n</ul>\n<p>Avoid transcript text, raw audio, authentication tokens, and tool arguments in default traces.</p>\n<p>A small metrics set is more useful than hundreds of unowned counters:</p>\n<pre><code class=\"language-text\">live_session_started_total\nlive_session_ended_total{reason}\nlive_session_active\nlive_session_reconnect_total{cause}\nlive_interaction_total{status}\nlive_first_audio_latency_ms{route,region}\nlive_turn_duration_ms{route}\nlive_interruption_stop_latency_ms{route}\nlive_tool_duration_ms{tool,status}\nlive_provider_error_total{provider,class}\nlive_gateway_queue_depth{region}\nlive_audio_buffer_underrun_total{client_version}\n</code></pre>\n<p>Don't use session IDs, user IDs, request IDs, or arbitrary tool arguments as metric labels. High-cardinality labels can turn an observability system into another availability problem.</p>\n<h3>Dashboards And Privacy-Aware Monitoring</h3>\n<p>Organize dashboards around user-visible outcomes.</p>\n<h4>Voice Experience</h4>\n<p>Show:</p>\n<ul>\n<li>active sessions</li>\n<li>unexpected session endings</li>\n<li>time to first audible response</li>\n<li>interruption stop latency</li>\n<li>audio underruns</li>\n<li>reconnect rate</li>\n<li>tool-related silence duration</li>\n</ul>\n<p>Split charts by region, client version, model route, and gateway version.</p>\n<h4>Provider And Routing</h4>\n<p>Show:</p>\n<ul>\n<li>provider error classes</li>\n<li>first-event and first-audio latency</li>\n<li>route switches</li>\n<li>fallback success rate</li>\n<li>provider connection saturation</li>\n<li>usage consumption where available</li>\n</ul>\n<p>Multi-model routing can balance performance, cost, and latency, as HydraFusion describes at a high level. It also adds variance. A route switch may improve availability while making the next turn slower or changing response behavior.</p>\n<h4>Capacity</h4>\n<p>Show:</p>\n<ul>\n<li>gateway worker utilization</li>\n<li>connection pressure</li>\n<li>queue depth</li>\n<li>active WebSocket count</li>\n<li>connection creation rate</li>\n<li>outbound provider connections</li>\n<li>memory pressure</li>\n<li>rate-limit responses</li>\n</ul>\n<p>The August 2026 GitHub availability material reinforces that capacity monitoring and retry behavior are availability features, not housekeeping.</p>\n<p>The default telemetry payload should contain metadata, not content. For most production debugging, engineers need to know that a response was interrupted or a tool timed out. They don't need the user's recording.</p>\n<pre><code class=\"language-json\">{\n  \"tool_name\": \"calendar_lookup\",\n  \"tool_status\": \"timeout\",\n  \"argument_policy\": \"redacted\",\n  \"result_class\": \"upstream_timeout\",\n  \"duration_ms\": 1800\n}\n</code></pre>\n<p>Redaction must happen before export. Once sensitive content enters a log sink, removing it from one dashboard does not remove it from replicas, exports, or backups.</p>\n<p>Use temporary diagnostics carefully:</p>\n<ul>\n<li>require an incident or change identifier</li>\n<li>restrict the session or tenant scope</li>\n<li>set automatic expiration</li>\n<li>show who enabled capture</li>\n<li>encrypt captured data</li>\n<li>delete it when the investigation ends</li>\n<li>record the deletion event</li>\n</ul>\n<h2>Privacy, Security, And Data Lifecycles</h2>\n<p>Privacy decisions belong in the session design, not in a policy document added after launch.</p>\n<p>A live voice system processes a continuous stream. Audio may be buffered in the client, gateway, provider connection, tool layer, monitoring system, and support workflow.</p>\n<pre><code class=\"language-text\">microphone\n  → client buffer\n  → encrypted gateway connection\n  → gateway transient buffer\n  → live model session\n  → optional tool request\n  → optional transcript or audit store\n  → deletion and retention controls\n</code></pre>\n<p>For every boundary, answer:</p>\n<ul>\n<li>Is the data copied?</li>\n<li>Is it encrypted?</li>\n<li>How long does it exist?</li>\n<li>Who can access it?</li>\n<li>Can it be deleted?</li>\n<li>What happens when the session fails halfway through?</li>\n</ul>\n<h3>Data Minimization And Retention</h3>\n<p>The safest audio retention policy is not retaining audio.</p>\n<p>In streaming-only mode, the client sends frames to the gateway, the gateway forwards them to the live session, and transient buffers are released after forwarding or playback. This does not eliminate provider-side handling or operational metadata, but it avoids accidentally creating an application-owned recording system.</p>\n<p>An ephemeral session should have a clear lifecycle:</p>\n<pre><code class=\"language-text\">created\n  → authenticated\n  → active\n  → draining\n  → closed\n  → expired\n</code></pre>\n<p>Buffers should be scoped to the session and released when it reaches <code>closed</code> or <code>expired</code>.</p>\n<p>Retention needs separate policies for separate data classes:</p>\n<table>\n<thead>\n<tr><th>Data class</th><th>Default purpose</th><th>Retention decision</th></tr>\n</thead>\n<tbody>\n<tr><td>Raw audio</td><td>Usually not required after streaming</td><td>Disable by default</td></tr>\n<tr><td>Temporary audio buffers</td><td>Transport and playback</td><td>Release at session close</td></tr>\n<tr><td>Transcript text</td><td>Continuity, support, or user history</td><td>Retain only when required</td></tr>\n<tr><td>Tool audit record</td><td>Security and accountability</td><td>Apply audit policy</td></tr>\n<tr><td>Aggregate metrics</td><td>Reliability and capacity trends</td><td>Retain without content</td></tr>\n<tr><td>Error traces</td><td>Incident investigation</td><td>Limited operational window</td></tr>\n<tr><td>Consent records</td><td>Proof of user choice</td><td>Apply legal and product requirements</td></tr>\n<tr><td>Deletion records</td><td>Lifecycle enforcement</td><td>Keep a non-content audit record</td></tr>\n</tbody>\n</table>\n<p>The actual periods must come from the requirements that apply to the service. The architecture should support different policies rather than one hard-coded duration.</p>\n<p>Deletion should be an idempotent workflow:</p>\n<pre><code class=\"language-text\">deletion_requested\n  → records_located\n  → application_copies_deleted\n  → indexes_updated\n  → downstream_deletions_requested\n  → completion_verified\n  → deletion_audited\n</code></pre>\n<p>Keep the audit entry about the action, not the deleted content:</p>\n<pre><code class=\"language-json\">{\n  \"event\": \"data.deletion.completed\",\n  \"request_id\": \"del_204\",\n  \"subject_ref\": \"user_hash_88\",\n  \"data_classes\": [\"transcript\", \"session_metadata\"],\n  \"completed_at\": \"2026-09-16T13:00:00Z\",\n  \"actor\": \"privacy_worker\",\n  \"verification\": \"store_and_index_checked\"\n}\n</code></pre>\n<p>If a provider or downstream store is unavailable, record the pending state and retry safely. Quietly marking the request complete because one store succeeded is not a lifecycle.</p>\n<h3>Auditability And Access Control</h3>\n<p>For every sensitive operation, record who or what performed it, what class of data it affected, why it was allowed, and whether it succeeded.</p>\n<p>Relevant operations include:</p>\n<ul>\n<li>starting a session</li>\n<li>enabling a tool</li>\n<li>changing a routing policy</li>\n<li>accessing a support capture</li>\n<li>exporting a transcript</li>\n<li>changing retention settings</li>\n<li>processing a deletion request</li>\n<li>granting temporary operator access</li>\n</ul>\n<p>A latency engineer may need route, region, and timing data without transcript access. A support operator may need a customer-visible transcript without provider credentials or raw audio. Define those paths explicitly.</p>\n<p>Consent should be represented as state that the gateway can enforce. If the user has not permitted retention, the downstream transcript store should not receive the data.</p>\n<p>A consent record should identify:</p>\n<ul>\n<li>user or account reference</li>\n<li>notice or policy version</li>\n<li>processing purpose</li>\n<li>time and channel of consent</li>\n<li>withdrawal state</li>\n<li>affected session or data classes</li>\n</ul>\n<p>Support access should be:</p>\n<ul>\n<li>explicitly requested</li>\n<li>approved by an authorized role</li>\n<li>limited to a session or case</li>\n<li>time-bound</li>\n<li>logged</li>\n<li>revoked automatically</li>\n</ul>\n<p>A dashboard that lets anyone search all voice sessions is not an audit system. It is a future incident.</p>\n<h3>Security At The Live Boundary</h3>\n<p>Authenticate the client before creating an upstream session. Don't treat possession of a WebSocket URL as sufficient authorization if it can be replayed or shared.</p>\n<p>Session credentials should be:</p>\n<ul>\n<li>short-lived where possible</li>\n<li>scoped to the intended session</li>\n<li>excluded from logs</li>\n<li>rotated according to the key policy</li>\n<li>invalidated when the session ends or is revoked</li>\n</ul>\n<p>Keep provider credentials on the server. A client should not receive a general-purpose provider key merely because it needs to send audio.</p>\n<p>Network controls still matter inside the service:</p>\n<ul>\n<li>isolate gateway workers from unrelated workloads</li>\n<li>restrict outbound destinations</li>\n<li>use private connectivity where supported</li>\n<li>apply egress policies to tool workers</li>\n<li>separate production and test credentials</li>\n<li>rotate secrets without dropping active sessions where possible</li>\n</ul>\n<p>Abuse controls should cover session creation, active session count, tool calls, and reconnect attempts. A reconnect storm can be more damaging than the original provider outage.</p>\n<p>Finally, treat prompts, tool results, and model-generated instructions as untrusted input to the control plane. Model output must not decide its own authorization, retention policy, routing override, or audit suppression.</p>\n<h2>Failure Modes, Fallbacks, And Recovery</h2>\n<p>A live gateway has more failure modes than a conventional API because work is continuous and stateful.</p>\n<p>The client can lose connectivity while the provider remains healthy. The provider can accept a session but stop producing audio. The model can return a tool request while the tool system is overloaded. The gateway can be healthy but unable to create upstream sessions because a quota or regional capacity limit has been reached.</p>\n<p>Write these failures down before implementing fallback behavior. Otherwise each component will retry according to its own assumptions, and the system will turn a small outage into a retry storm.</p>\n<table>\n<thead>\n<tr><th>Failure class</th><th>Example</th><th>User-visible result</th><th>Preferred response</th></tr>\n</thead>\n<tbody>\n<tr><td>Client transport</td><td>Wi-Fi change, mobile handoff</td><td>Silence or reconnect</td><td>Resume or restart with clear state</td></tr>\n<tr><td>Gateway capacity</td><td>Worker or queue saturation</td><td>Delayed start</td><td>Shed load or reject early</td></tr>\n<tr><td>Provider connection</td><td>Upstream close or timeout</td><td>Stalled response</td><td>Fail over if state permits</td></tr>\n<tr><td>Model response</td><td>Invalid or incomplete event</td><td>Partial answer</td><td>Stop safely and recover</td></tr>\n<tr><td>Tool dependency</td><td>Timeout or authorization failure</td><td>Missing capability</td><td>Explain limitation and continue</td></tr>\n<tr><td>Security or policy</td><td>Expired session or denied tool</td><td>Refused action</td><td>Fail closed</td></tr>\n<tr><td>Data path</td><td>Audit store unavailable</td><td>Core voice may still work</td><td>Degrade storage within policy</td></tr>\n<tr><td>Regional outage</td><td>Gateway or provider region impaired</td><td>Session loss</td><td>Reconnect or move new sessions</td></tr>\n</tbody>\n</table>\n<h3>Redundancy And Active Sessions</h3>\n<p>Use redundancy at session admission first. New sessions should avoid an unhealthy region or provider route. Existing live sessions are harder because moving them may require recreating state and interrupting playback.</p>\n<p>A practical design separates:</p>\n<ul>\n<li>a session directory that knows where a session is active</li>\n<li>gateway workers that own WebSocket connections</li>\n<li>a provider connection manager</li>\n<li>routing policy that selects an alternate path</li>\n<li>a client reconnect protocol</li>\n</ul>\n<p>The session directory should not carry audio:</p>\n<pre><code class=\"language-json\">{\n  \"session_id\": \"session_7f2\",\n  \"gateway_region\": \"asia-south\",\n  \"provider_route\": \"primary\",\n  \"state\": \"active\",\n  \"last_client_sequence\": 1842,\n  \"last_server_sequence\": 927,\n  \"expires_at\": \"2026-09-16T12:20:00Z\"\n}\n</code></pre>\n<p>Sequence numbers help detect gaps and duplicate delivery. They don't make a session resumable by themselves.</p>\n<p>A recovery attempt should have explicit stages:</p>\n<ol>\n<li>detect the broken connection</li>\n<li>stop accepting audio for the dead path</li>\n<li>flush or discard stale playback according to policy</li>\n<li>attempt supported session recovery</li>\n<li>otherwise create a replacement session</li>\n<li>notify the client of the new state</li>\n<li>resume only after authorization and routing checks</li>\n</ol>\n<p>Don't retry every failure. An authentication error will not be fixed by reconnecting five times. A regional network failure may justify a bounded attempt on another route. Classify errors first.</p>\n<h3>Graceful Degradation And Hard Failure</h3>\n<p>Graceful degradation is useful when the core interaction remains truthful:</p>\n<ul>\n<li>continue voice conversation without an optional transcript store</li>\n<li>answer without an unavailable nonessential tool</li>\n<li>switch to a lower-capability route</li>\n<li>return text when audio playback cannot continue</li>\n<li>stop a long tool call and offer a retry</li>\n<li>preserve the current turn while disabling a feature</li>\n</ul>\n<p>Hard fail when continuing would be misleading, unsafe, or impossible to authorize:</p>\n<ul>\n<li>expired or invalid credentials</li>\n<li>denied access to a requested tool</li>\n<li>uncertain identity for a sensitive action</li>\n<li>corrupted session state</li>\n<li>missing policy decision</li>\n<li>inability to protect or route audio securely</li>\n<li>provider output that cannot be associated with the active session</li>\n</ul>\n<p>A client-facing state machine might look like this:</p>\n<pre><code class=\"language-text\">active\n  → degraded\n  → recovering\n  → active\n\nactive\n  → interrupted\n  → recovering\n  → active\n\nactive\n  → failed\n  → reconnect_required\n</code></pre>\n<p>Every transition needs a reason class and an allowed next action. A degraded session may continue with tools disabled. A failed session should not accept more audio just because the WebSocket remains open.</p>\n<h3>Recovery Runbooks</h3>\n<h4>Provider Latency Or Timeout</h4>\n<ol>\n<li>Check first-audio latency by route and region.</li>\n<li>Compare the affected route with the fallback.</li>\n<li>Check gateway queue depth and outbound connection pressure.</li>\n<li>Determine whether failures affect new sessions, existing sessions, or both.</li>\n<li>Stop automatic retries if the error is systemic.</li>\n<li>Shift new sessions according to policy.</li>\n<li>Preserve a sample of error traces and provider response classes.</li>\n<li>Communicate the degraded capability and next review time.</li>\n</ol>\n<p>Don't start by restarting all workers. That can destroy healthy sessions and erase useful evidence.</p>\n<h4>Reconnect Storm</h4>\n<ol>\n<li>Chart reconnects by client version, region, and close reason.</li>\n<li>Check whether the gateway or provider is closing connections.</li>\n<li>Apply bounded backoff with jitter.</li>\n<li>Reject excess session creation early.</li>\n<li>Protect the provider with a connection-rate limit.</li>\n<li>Verify that session ownership leases aren't duplicated.</li>\n<li>Restore admission gradually.</li>\n</ol>\n<h4>Tool Dependency Failure</h4>\n<ol>\n<li>Check tool timeout and error rates.</li>\n<li>Separate authorization failures from upstream unavailability.</li>\n<li>Disable the affected tool if retries increase pressure.</li>\n<li>Keep voice sessions alive when the tool is optional.</li>\n<li>Return a truthful capability message.</li>\n<li>Re-enable after a controlled health check.</li>\n</ol>\n<p>Tool failures should not automatically restart the model session. Restarting loses context and can make a dependency problem look like a model problem.</p>\n<h4>Audio Playback Stall</h4>\n<ol>\n<li>Check whether model audio is being generated.</li>\n<li>Compare server output timestamps with client playback acknowledgements.</li>\n<li>Inspect downlink loss, buffer underruns, and decoder errors.</li>\n<li>Determine whether the client, gateway, or provider is stuck.</li>\n<li>Stop stale playback before recovery.</li>\n<li>Reconnect or switch to text according to client capability.</li>\n</ol>\n<p>Server traces alone cannot tell you whether bytes reached the speaker. Client-side telemetry is required.</p>\n<h3>Stale Output And Duplicate Speech</h3>\n<p>A late event from a failed route can arrive after the fallback has started. Use session, turn, and generation identifiers to reject stale output. The output sink should have one active producer at a time.</p>\n<pre><code class=\"language-ts\">type ActiveRoute = {\n  routeId: string;\n  turnId: string;\n  generation: number;\n};\n\nfunction canEmit(\n  active: ActiveRoute,\n  event: { routeId: string; turnId: string; generation: number }\n): boolean {\n  return (\n    active.routeId === event.routeId &amp;&amp;\n    active.turnId === event.turnId &amp;&amp;\n    active.generation === event.generation\n  );\n}\n</code></pre>\n<p>This check belongs close to the audio output boundary. Earlier checks help, but the final sink must refuse stale audio because that is where duplicate speech becomes visible.</p>\n<h2>Rollout From Beta To Production</h2>\n<p>A live gateway should earn production traffic in stages.</p>\n<p>The first milestone isn't “the model answered.” It is proving that the complete loop works under controlled conditions: audio enters, a session is authorized, model audio returns, interruptions stop stale playback, tools obey policy, and the system leaves no unexpected data behind.</p>\n<p>Every phase needs a way to reduce traffic, disable optional tools, switch routes, or stop new sessions without taking down unrelated services.</p>\n<h3>Phase 0: Contract And Harness Validation</h3>\n<p>Before real users, validate:</p>\n<ul>\n<li>client and gateway event schemas</li>\n<li>session ownership rules</li>\n<li>sequence numbers and reconnect behavior</li>\n<li>interruption and cancellation semantics</li>\n<li>tool authorization</li>\n<li>telemetry fields and redaction</li>\n<li>retention and deletion workflows</li>\n<li>provider error classification</li>\n</ul>\n<p>Use synthetic audio and controlled tool responses. A test harness should inject delays, disconnects, malformed events, duplicate events, and provider errors. Testing only the happy path gives a false sense of readiness because many live failures are ordering and lifecycle failures.</p>\n<h3>Phase 1: Internal Traffic</h3>\n<p>Run with tightly bounded concurrency and keep raw content capture disabled unless an approved test requires it. Focus on:</p>\n<ul>\n<li>session creation success</li>\n<li>first-audio timing</li>\n<li>interruption stop timing</li>\n<li>reconnect outcomes</li>\n<li>tool timeout behavior</li>\n<li>trace completeness</li>\n<li>resource usage per active session</li>\n</ul>\n<p>This phase should establish what healthy looks like before alerts are defined.</p>\n<h3>Phase 2: Small User Pilot</h3>\n<p>Choose a narrow audience and limited capability set. Keep optional tools behind feature flags. Route a controlled percentage of eligible sessions to the new gateway while the existing path remains available.</p>\n<p>Define exit criteria before starting:</p>\n<ul>\n<li>no unresolved security or privacy-critical findings</li>\n<li>session close behavior is understood</li>\n<li>fallback behavior is truthful</li>\n<li>latency tails fit the chosen budget</li>\n<li>on-call staff can execute recovery runbooks</li>\n<li>deletion and access workflows have been tested</li>\n</ul>\n<p>Don't expand because the median looks good if interruption failures or reconnect loops remain unexplained.</p>\n<h3>Phase 3: Gradual Scale</h3>\n<p>Increase traffic in steps. After each step, observe long enough to expose connection churn, tool usage, and delayed cleanup.</p>\n<p>Control the blast radius with:</p>\n<ul>\n<li>tenant or account allow-lists</li>\n<li>region-level rollout flags</li>\n<li>client-version gates</li>\n<li>maximum active sessions</li>\n<li>tool-specific enablement</li>\n<li>provider route caps</li>\n<li>automatic rollback thresholds</li>\n</ul>\n<p>Parallel-agent work benefits from isolated worktrees because concurrent tasks don't interfere with one another. The same principle applies operationally: isolate rollout configuration and experiments so one change doesn't silently alter every active session.</p>\n<h3>Phase 4: Production Default</h3>\n<p>Make the new path the default only while the fallback remains operational. A fallback that hasn't been exercised is documentation, not resilience.</p>\n<p>Keep:</p>\n<ul>\n<li>a tested rollback switch</li>\n<li>a provider route override</li>\n<li>a tool disable switch</li>\n<li>a session admission limit</li>\n<li>an incident owner for each dependency</li>\n<li>a change log tied to deployed versions</li>\n</ul>\n<p>Production readiness is the ability to detect, contain, explain, and recover from instability.</p>\n<h3>Backward Compatibility And Change Control</h3>\n<p>The client should not understand every provider-specific event. Put the compatibility boundary in the gateway and expose a versioned internal protocol.</p>\n<p>For each event, define:</p>\n<ul>\n<li>event type</li>\n<li>required and optional fields</li>\n<li>sequence behavior</li>\n<li>acknowledgement behavior</li>\n<li>retry and duplicate semantics</li>\n<li>behavior when the client does not understand a field</li>\n</ul>\n<p>Additive changes are easier than semantic changes. A new optional field is usually safe. Changing “turn complete” into “response complete” without versioning can break playback and tool state.</p>\n<p>Keep the gateway capable of serving older clients during migration. Measure use of old contracts, publish an end date, and remove them through a documented change process.</p>\n<p>Provider changes need the same adapter discipline:</p>\n<pre><code class=\"language-json\">{\n  \"type\": \"audio.delta\",\n  \"session_id\": \"session_7f2\",\n  \"interaction_id\": \"turn_018\",\n  \"sequence\": 927,\n  \"audio\": \"&lt;transient payload&gt;\",\n  \"is_final\": false\n}\n</code></pre>\n<p>The adapter can map provider-specific events into that form. The rest of the system should not need to know whether a provider calls the event a delta, chunk, part, or response segment.</p>\n<p>When changing routes or models, compare behavior as well as latency. A fallback can be technically successful while changing tool-call frequency, response length, interruption behavior, or cost.</p>\n<h3>Incident Readiness</h3>\n<p>Set service objectives around experience:</p>\n<ul>\n<li>successful session establishment</li>\n<li>time to first audible response</li>\n<li>interruption stop latency</li>\n<li>reconnect recovery rate</li>\n<li>tool-call success</li>\n<li>unexpected session termination</li>\n<li>percentage of sessions with complete telemetry</li>\n</ul>\n<p>Define rollback triggers before the incident:</p>\n<pre><code class=\"language-text\">rollback if:\n  first_audio_p95 exceeds the approved threshold\n  unexpected session endings rise above the approved rate\n  reconnect attempts create sustained admission pressure\n  privacy redaction checks fail\n  tool authorization errors indicate a policy regression\n</code></pre>\n<p>The actual thresholds belong in the approved operating policy. The important part is deciding them before production traffic.</p>\n<p>Keep the blast radius bounded. If a gateway version has a session-state bug, it should not own every region and client version at once. If a provider route is expensive, cap the sessions that can select it. If a tool integration is new, enable it for a narrow policy scope.</p>\n<p>The on-call handoff should include:</p>\n<ul>\n<li>rollout percentage</li>\n<li>enabled routes and tools</li>\n<li>known degraded modes</li>\n<li>dashboard links</li>\n<li>rollback controls</li>\n<li>dependency contacts</li>\n<li>recent changes</li>\n<li>open incidents</li>\n<li>data-capture status</li>\n</ul>\n<p>A live voice incident is a poor time to discover that the person who enabled debug audio capture has left for the day.</p>\n<h2>Implementation Boundaries And Open Questions</h2>\n<p>A gateway can make a live model easier to operate, but it can't remove the underlying uncertainty. It adds another distributed system between the user and the model. Every boundary gives you a policy point and another failure point.</p>\n<p>The supplied material does not include an official Gemini 3.8 Live API specification, detailed session diagrams, concrete latency budgets, or a production rollout case study for a Gemini-like live audio gateway. Those gaps should remain visible in the design review.</p>\n<p>The architecture assumes that:</p>\n<ul>\n<li>Gemini 3.8 Live and Gemini 3.8 Live Extended Thinking expose a contract that can be adapted behind a gateway interface</li>\n<li>the gateway can identify turn boundaries, interruptions, model output, and tool calls</li>\n<li>cloud-processing consent is established before audio is sent</li>\n<li>provider health and capacity can be measured well enough to support routing</li>\n<li>the organization accepts some implementation complexity in exchange for control over privacy, cost, and failover</li>\n</ul>\n<h3>Provider Contract Questions</h3>\n<p>Before implementation, verify:</p>\n<ul>\n<li>connection and authentication semantics</li>\n<li>audio encodings and framing rules</li>\n<li>session limits</li>\n<li>interruption behavior</li>\n<li>tool-call message format</li>\n<li>usage reporting</li>\n<li>Extended Thinking behavior in a live stream</li>\n<li>regional availability and data handling</li>\n<li>retry and resume semantics</li>\n</ul>\n<p>Until those answers are confirmed, keep the provider adapter narrow. Don't spread assumed message names or undocumented fields across the gateway.</p>\n<h3>State And Fallback Questions</h3>\n<p>Create and test a formal state machine for:</p>\n<ul>\n<li>connecting</li>\n<li>active input</li>\n<li>model output</li>\n<li>interruption</li>\n<li>tool execution</li>\n<li>provider failure</li>\n<li>route change</li>\n<li>draining</li>\n<li>closed</li>\n</ul>\n<p>Test every transition with delayed frames, duplicate events, client disconnects, provider errors, and late tool results.</p>\n<p>A fallback route is not equivalent merely because both providers generate audio. Differences in interruption, tool calling, context handling, voice behavior, and safety policy can change the product experience. A fallback contract needs capability checks and explicit user-facing behavior.</p>\n<h3>Capacity And Cost Questions</h3>\n<p>There are no Gemini 3.8 Live production benchmarks, latency distributions, or cost curves in the supplied notes. Don't publish a gateway SLA based on assumptions. Measure connection setup, first output, interruption response, tool latency, session duration, and settled usage during staged rollout.</p>\n<p>Token-based costs can escalate with long sessions, repeated tools, retries, duplicated context, and concurrent agents. Use budget controls at session, tenant, route, tool, and experiment levels.</p>\n<p>A budget check should happen before an expensive operation:</p>\n<pre><code class=\"language-ts\">type Usage = {\n  inputTokens?: number;\n  outputTokens?: number;\n  toolCalls: number;\n  estimatedCostUnits: number;\n};\n\ntype Budget = {\n  maxCostUnits: number;\n  maxToolCalls: number;\n};\n\nfunction budgetDecision(\n  usage: Usage,\n  budget: Budget,\n): \"continue\" | \"degrade\" | \"stop\" {\n  if (usage.estimatedCostUnits &gt;= budget.maxCostUnits) {\n    return \"stop\";\n  }\n\n  if (usage.toolCalls &gt;= budget.maxToolCalls) {\n    return \"degrade\";\n  }\n\n  return \"continue\";\n}\n</code></pre>\n<p>The gateway should distinguish estimated usage from settled provider usage. Estimates help admission control, but they are not invoices. Reconcile provider usage asynchronously and investigate divergence before widening traffic.</p>\n<h3>Multi-Agent Expansion</h3>\n<p>Parallel agent sessions can be isolated with separate Git worktrees in the Copilot example, which is useful as an isolation concept. It does not prove that parallel live voice agents should share a gateway session or provider connection.</p>\n<p>If the product later adds parallel agents, give each agent:</p>\n<ul>\n<li>an explicit budget</li>\n<li>a trace context</li>\n<li>a cancellation path</li>\n<li>an isolation boundary</li>\n<li>a clear output-selection rule</li>\n<li>a tool authorization scope</li>\n</ul>\n<p>The remaining work is clear in shape: validate the vendor contract, implement the provider adapter behind narrow interfaces, exercise the state machine under failure, and collect real latency and usage data before widening traffic. The architecture should make those unknowns cheap to test rather than pretending they have already been settled.</p>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
