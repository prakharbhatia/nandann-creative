import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "nextjs-august-2026-two-critical-rces",
  title: "Next.js Just Shipped Two Critical RCEs. If You Self-Host, Patch Today.",
  description: "Next.js 16.3.3 and 15.5.24 close two unauthenticated RCEs: AVIF image optimization via libheif, and a Windows-only Pages+App Router bug. What actually changed on August 25.",
  date: "2026-08-30",
  readTime: "8 min",
  category: "Next.js",
  tags: ["Next.js","Security","RCE","AVIF","self-hosted"],
  faqs: [
    {
      question: "Which Next.js versions should I install?",
      answer: "If you're on the 16 line, install next@16.3.3 (Active LTS). If you're staying on 15, install next@15.5.24 (Maintenance LTS). Updating package.json isn't enough — refresh the lockfile, rebuild, and confirm the running version in production."
    },
    {
      question: "Does the AVIF bug affect Linux servers?",
      answer: "Yes. The image-optimization RCE is in libheif, reached through sharp when Next.js processes an attacker-controlled AVIF. It isn't OS-specific. The patched releases disable AVIF optimization until an upstream libheif fix lands."
    },
    {
      question: "Am I safe if I only run Next.js on Linux or macOS?",
      answer: "You're not affected by CVE-2026-75604, which is Windows-filesystem only. You can still be affected by the AVIF issue if image optimization is on. Patch anyway."
    },
    {
      question: "Is there a workaround besides upgrading?",
      answer: "Vercel says there's no known workaround for Windows-hosted apps hit by CVE-2026-75604. For AVIF, the official fix is to upgrade so optimization is disabled."
    },
    {
      question: "When did this ship?",
      answer: "GitHub tagged v16.3.3 and v15.5.24 on August 25, 2026. Vercel moved the release forward a day after a second critical showed up in an upstream dependency during the disclosure window."
    }
  ],
  coverImage: "/images/nextjs-august-2026-two-critical-rces.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/nextjs-august-2026-two-critical-rces.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/nextjs-august-2026-two-critical-rces.webp\" alt=\"Next.js Just Shipped Two Critical RCEs. If You Self-Host, Patch Today.\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>Vercel moved the Next.js security release forward a day. That's the part worth paying attention to.</p>\n<p>They'd already flagged a patch window. Then they found a second critical in an upstream dependency and decided people shouldn't wait until August 26. GitHub tagged <code>v16.3.3</code> and <code>v15.5.24</code> on August 25. Both close two unauthenticated remote code execution bugs. Not one.</p>\n<p>If you self-host Next.js, this is a lockfile-and-redeploy job, not a \"we'll catch it in the next sprint\" note.</p>\n<h2>What actually shipped</h2>\n<p>Two patched lines:</p>\n<pre><code class=\"language-bash\">npm install next@15.5.24  # 15.x Maintenance LTS\nnpm install next@16.3.3   # 16.x Active LTS\n</code></pre>\n<p>The GitHub release notes (published by Sebastian Silbermann / eps1lon) point at the same pair of advisories on both tags:</p>\n<ul>\n<li>Unauthenticated RCE in the Image Optimization API when AVIF is involved — <a href=\"https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4\">GHSA-2xp9-vwfh-vxw4</a></li>\n<li>Unauthenticated RCE on Windows-hosted servers — <a href=\"https://github.com/vercel/next.js/security/advisories/GHSA-p293-qw3h-jr36\">GHSA-p293-qw3h-jr36</a> / CVE-2026-75604</li>\n</ul>\n<p>The official Next.js post is short. That's usual for these. The details live in the advisories.</p>\n<h2>The AVIF path: your optimizer is the attack surface</h2>\n<p>Next.js doesn't decode AVIF itself. <code>sharp</code> does. <code>sharp</code> talks to <code>libheif</code>. A heap overflow in that library (also tracked as GHSA-g89c-p67h-r497) is reachable when the Image Optimization API handles an attacker-controlled AVIF.</p>\n<p>You don't need a logged-in user. If the optimizer will fetch or accept an image you don't fully control — remote <code>src</code>, user uploads, a CMS that allows AVIF — that's the setup.</p>\n<p>The patched Next.js releases <strong>turn AVIF optimization off</strong> until a fixed <code>libheif</code> makes it through the stack. That's a product trade-off: you lose automatic AVIF transcoding for a while. Fine. RCE is worse.</p>\n<p>Don't assume \"we only use WebP\" saves you if something in the pipeline still accepts AVIF and hands it to the optimizer.</p>\n<h2>The Windows path: mixed routers, no Cache Components</h2>\n<p>CVE-2026-75604 is narrower and meaner in a different way.</p>\n<p>Vercel's post: apps using <strong>both</strong> the Pages Router and the App Router <strong>without Cache Components</strong>, on a <strong>Windows filesystem</strong>, can hit unauthenticated RCE. Linux and macOS are called out as not affected. They also say there's <strong>no known workaround</strong> for affected Windows-hosted apps.</p>\n<p>So: IIS, some Azure App Service Windows workers, a Windows VM behind IIS/ARR, that one internal box someone stood up in 2023. If that's you and you're on mixed routing, you patch. There isn't a clever <code>next.config</code> toggle in the official writeup.</p>\n<p>If you only run Linux containers, this specific CVE isn't your fire. The AVIF one still might be.</p>\n<h2>Patching is boring on purpose</h2>\n<pre><code class=\"language-bash\"># confirm what you think you're running\nnpx next --version\n\nnpm install next@16.3.3\n# or: npm install next@15.5.24\n</code></pre>\n<p>Then actually ship it. A <code>package.json</code> bump that never hits production is not a patch.</p>\n<p>Rebuild the image. Recycle the process. Check the version string in the running container, not just CI logs. Writeups after the release keep repeating that because people forget it every time.</p>\n<p>If you pin <code>sharp</code> yourself, look at what Next.js now ships. Fighting the framework's disable-AVIF change by re-enabling formats in config is a good way to put the hole back.</p>\n<h2>What this doesn't mean</h2>\n<p>It doesn't mean Next.js is uniquely cursed. Image pipelines and path handling on Windows are old failure modes. The interesting bit is the disclosure window: they already had a date, then a second critical showed up upstream, and they pulled the release forward so you only upgrade once.</p>\n<p>It also doesn't mean you should rewrite the app this weekend. Upgrade. Confirm. Then read the advisories if you mix App Router and Pages on Windows, or if you optimize untrusted images.</p>\n<p>Vercel's Open Source Bug Bounty is how a lot of this gets found. Questions on their program still go to security@vercel.com, per the post.</p>\n<p>If your Next.js is sitting on a VPS or a Windows host you haven't touched since the last minor, start with the install commands above. The blog post is two screens. The incident is not.</p>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
