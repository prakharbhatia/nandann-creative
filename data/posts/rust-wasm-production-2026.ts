import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "rust-wasm-production-2026",
  title: "Rust WASM in 2026: From Toy Demos to Real Production Apps",
  description: "Rust-to-WASM is shipping in products people actually use. The tooling has caught up. The browsers have caught up. And the production evidence is piling up. From Figma to Shopify, here are the numbers and patterns to know about WebAssembly in 2026.",
  date: "2026-04-11",
  lastUpdated: "2026-05-09",
  readTime: "14 min",
  category: "Engineering",
  tags: [
    "Rust",
    "WebAssembly",
    "Performance"
  ],
  coverImage: "/images/rust_wasm_banner.png",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/rust_wasm_banner.png 1x\" type=\"image/png\" />\n  <img src=\"/images/rust_wasm_banner.png\" alt=\"Rust WASM in 2026: From Toy Demos to Real Production Apps\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<h1>Rust WASM in 2026: From Toy Demos to Real Production Apps</h1>\n<p>Remember when WebAssembly was that thing you&#39;d see in a conference talk, running a spinning cube at 60fps, and everyone would nod politely and go back to writing JavaScript?</p>\n<p>That era is over.</p>\n<p>Rust-to-WASM is shipping in products people actually use. The tooling has caught up. The browsers have caught up. And the production evidence is piling up.</p>\n<h2>Who&#39;s actually shipping this?</h2>\n<p>I don&#39;t care about hype. I care about proof.</p>\n<p><strong>Figma</strong> pioneered this. Their rendering engine compiles to WASM (C++, same principle). Load time dropped <strong>3x</strong> when they moved from asm.js. WASM parses roughly 20x faster. That&#39;s not a marginal improvement — that&#39;s a different product.</p>\n<p><strong>1Password</strong> uses Rust WASM for their web extension&#39;s crypto. Near-native speed, in the browser, no server round-trips for sensitive data.</p>\n<p><strong>Shopify</strong> compiles Rust to WASM for Shopify Functions — custom logic that runs at the edge. No cold starts. No container overhead.</p>\n<p><strong>Cloudflare Workers</strong> — Rust is a first-class language, compiled to WASM, running on every edge node they have.</p>\n<p><strong>Google Sheets</strong> runs its entire calc engine in WasmGC now. When Google ships WASM for billions of users, the &quot;is it production-ready&quot; question answers itself.</p>\n<p><strong>These aren&#39;t startups experimenting. These are companies with real traffic.</strong></p>\n<h2>The tooling is actually good now</h2>\n<p>This isn&#39;t 2021 where you&#39;re fighting the build system.</p>\n<p><strong>wasm-bindgen v0.2.118</strong> — the de facto standard for Rust ↔ JS interop. 123 releases, 8.9K stars. You write Rust with <code>#[wasm_bindgen]</code> annotations, it generates clean JS bindings. One pain point: the crate version must exactly match the CLI version. One version off and you get cryptic errors.</p>\n<p><strong>wasm-pack v0.14.0</strong> — one command to build, test, and publish to npm. The <code>rustwasm</code> org was sunset in July 2025 (key projects moved to independent orgs), but the tools are still maintained.</p>\n<p><strong>wasm-opt from Binaryen</strong> — binary size optimization that actually works. <code>wasm-opt -Oz</code> typically reduces size by 10-30%.</p>\n<p><strong>Skip wasm-opt and your binary will be 2-3x larger than it needs to be.</strong></p>\n<h2>Real performance numbers</h2>\n<p>Not vibes. Measurements.</p>\n<table>\n<thead>\n<tr>\n<th>Workload</th>\n<th>Rust WASM vs JavaScript</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Image processing</td>\n<td>5-20x faster</td>\n</tr>\n<tr>\n<td>Parsing (JSON, CSV)</td>\n<td>3-10x faster</td>\n</tr>\n<tr>\n<td>Crypto</td>\n<td>10-50x faster</td>\n</tr>\n<tr>\n<td>Simulation/Numeric</td>\n<td>5-30x faster</td>\n</tr>\n<tr>\n<td>DOM manipulation</td>\n<td>No advantage</td>\n</tr>\n</tbody></table>\n<p>The 64-bit integer point is underappreciated. JavaScript can only represent integers up to 2^53 accurately. WASM has native 64-bit integers. For hashes, IDs, bit manipulation — this alone can justify WASM.</p>\n<p><strong>But if your bottleneck is the DOM, WASM won&#39;t save you.</strong> Every DOM call goes through JS interop. The overhead negates the speed advantage.</p>\n<h2>Binary sizes: the real cost</h2>\n<p>WASM binaries are not small.</p>\n<table>\n<thead>\n<tr>\n<th>Project</th>\n<th>Unoptimized</th>\n<th>Optimized</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Minimal hello world</td>\n<td>~80 KB</td>\n<td>~15 KB</td>\n</tr>\n<tr>\n<td>JSON parser</td>\n<td>~200 KB</td>\n<td>~50 KB</td>\n</tr>\n<tr>\n<td>Image processing</td>\n<td>~500 KB</td>\n<td>~150 KB</td>\n</tr>\n<tr>\n<td>Crypto</td>\n<td>~300 KB</td>\n<td>~80 KB</td>\n</tr>\n<tr>\n<td>Full game (Bevy)</td>\n<td>~5 MB+</td>\n<td>~2 MB+</td>\n</tr>\n</tbody></table>\n<p>To get the optimized column, you need this in Cargo.toml:</p>\n<pre><code class=\"language-toml\">[profile.release]\nopt-level = &quot;z&quot;\nlto = true\ncodegen-units = 1\nstrip = true\npanic = &quot;abort&quot;\n</code></pre>\n<p>Then run <code>wasm-opt -Oz</code> on the output. Skip either step and you&#39;re paying for it in download size.</p>\n<p><strong><code>std::fmt</code> alone adds 10-20KB.</strong> If you can go <code>#![no_std]</code>, do it.</p>\n<h2>Browser support: basically universal</h2>\n<p>WasmGC reached Phase 4 (full W3C standard) in April 2025. All major browsers support it: Chrome 119+, Firefox 120+, Safari 18.2+, Edge 119+.</p>\n<p>SIMD? Chrome 91+, Firefox 89+, Safari 16.2+. Tail calls? All modern versions.</p>\n<p>The Component Model is actively being standardized. WASI Preview 2 is stable. Preview 3 (async/thread support) is the next milestone. Rust has full support via <code>cargo-component</code>.</p>\n<p><strong>The browser is no longer the bottleneck. Your code is.</strong></p>\n<h2>The pain points nobody tells you about</h2>\n<p><strong>Async runtimes don&#39;t work.</strong> Tokio? Async-std? Forget it in browser WASM. You need <code>wasm-bindgen-futures</code> + <code>SpawnLocal</code>. No <code>std::thread</code>. This is the single biggest architectural constraint.</p>\n<p><strong>Version coupling is a trap.</strong> <code>wasm-bindgen</code> crate version must exactly match <code>wasm-bindgen-cli</code> version. The error messages don&#39;t tell you this. You just get cryptic failures.</p>\n<p><strong>No stable multithreading.</strong> You need <code>SharedArrayBuffer</code> + <code>Atomics</code>, which require specific HTTP headers (<code>Cross-Origin-Opener-Policy</code>, <code>Cross-Origin-Embedder-Policy</code>). Many hosting providers don&#39;t set these. Without them, no threads. Period.</p>\n<p><strong>String handling is expensive.</strong> Every string between Rust and JS gets copied. No zero-copy. For string-heavy workloads, this overhead is real and measurable.</p>\n<p><strong>Debugging is still painful.</strong> Chrome DevTools supports WASM debugging and WasmGC heap snapshots. But source maps for Rust→WASM are fragile and often break. The standard workaround is <code>console_error_panic::set_hook()</code> — file/line info in the console, nothing more.</p>\n<p><strong>Many crates don&#39;t compile to WASM.</strong> Anything using OS APIs (file I/O, networking, threads) won&#39;t work. You&#39;ll end up with platform-specific dependency sections in your Cargo.toml. It&#39;s a maintenance burden.</p>\n<h2>When to use it</h2>\n<p><strong>Use Rust WASM when:</strong></p>\n<ul>\n<li>You have computation-heavy workloads where 5-20x speedup matters</li>\n<li>You need deterministic performance without GC pauses</li>\n<li>You have existing Rust/C++ code you want in the browser</li>\n<li>You need 64-bit integer precision that JavaScript can&#39;t give you</li>\n</ul>\n<p><strong>Don&#39;t use Rust WASM when:</strong></p>\n<ul>\n<li>You&#39;re building a CRUD app. Use React. Seriously.</li>\n<li>Your team doesn&#39;t know Rust. The learning curve doesn&#39;t get shorter because you&#39;re targeting WASM.</li>\n<li>Your bottleneck is DOM manipulation. WASM doesn&#39;t help.</li>\n<li>You&#39;re optimizing before profiling. If your JS is fast enough, it&#39;s fast enough.</li>\n</ul>\n<p><strong>The hardest call:</strong> you have a specific hot path in an otherwise JavaScript app. Do you pull in Rust for it?</p>\n<p>If it&#39;s on the critical path and runs frequently — yes. The 5-10x speedup is real. Users notice.</p>\n<p>If it&#39;s a 200ms operation that runs once per page load — optimize your JavaScript first. The WASM overhead isn&#39;t worth it.</p>\n<h2>What I&#39;d reach for today</h2>\n<ol>\n<li><strong>wasm-pack</strong> for the build pipeline</li>\n<li><strong>wasm-bindgen</strong> for JS interop</li>\n<li><strong>wasm-opt</strong> for binary size optimization (non-negotiable)</li>\n<li><strong>twiggy</strong> for code size profiling when your binary is too large</li>\n</ol>\n<p>Debug as native Rust first, then compile to WASM. The debugging story for WASM directly is still not good enough.</p>\n<p><strong>And watch the Component Model.</strong> When WASI Preview 3 lands with async/thread support, it unlocks a fundamentally different programming model. That&#39;s the next inflection point.</p>\n<h2>The bottom line</h2>\n<p>Rust WASM is production-ready in 2026. Figma proved it. 1Password proved it. Shopify proved it. The tooling works, the performance is real, browser support is universal.</p>\n<p><strong>But &quot;production-ready&quot; doesn&#39;t mean &quot;use it everywhere.&quot;</strong> It means the baseline is solid enough that when you have a real use case, you can trust it.</p>\n<p>The spinning cube demos were fun. The production systems shipping today are something else entirely.</p>\n<hr>\n<p><em>This is an independent comparison with no affiliate links. I&#39;m not sponsored by any tool, company, or framework mentioned above. All benchmark data is from publicly available sources.</em></p>\n"
};

export default post;
