import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "wordpress-72-secrets-api",
  title: "WordPress Wants a Real Secrets API in 7.2. Plugins Still Dump Keys in wp-config.",
  description: "Eric Mann's August 25 proposal: encrypted secrets in WordPress 7.2, no admin UI, no retrieval filters. What it changes for plugins still storing keys in options and wp-config.",
  date: "2026-08-30",
  readTime: "25 min",
  category: "WordPress",
  tags: ["WordPress","Security","PHP","Plugins"],
  faqs: [
      {
          "question": "Is the Secrets API in WordPress 7.2 yet?",
          "answer": "No. It is a Make/Core proposal dated August 25, 2026. Eric Mann wants a feature plugin first, a Trac patch in late September, and a go/no-go before 7.2 Beta 1 on October 20-22. He would rather slip to 7.3 than rush it."
      },
      {
          "question": "Where do plugin API keys live today?",
          "answer": "Usually as plaintext rows in wp_options, or as constants in wp-config.php, or as environment variables on hosts that support them. Core has no first-class secret type, which is the gap the proposal is trying to close."
      },
      {
          "question": "Does encryption mean a stolen database is harmless?",
          "answer": "No. It raises the cost of dumps, backups, and SQL injection reads. Code running inside WordPress can still call wp_get_secret() and reveal(). The proposal says process execution is out of scope."
      },
      {
          "question": "Will there be a settings screen in 7.2?",
          "answer": "The proposal says no. Storage and retrieval semantics land first. An admin UI is planned for 7.3 after the API has real usage. WP-CLI is in scope for 7.2 so core has a first-party consumer in the same release."
      },
      {
          "question": "Can hosts plug in Vault or Parameter Store?",
          "answer": "The design is a secrets.php drop-in with separate storage and keyring providers. Pantheon and Altis already said the write path and in-PHP encryption do not match how their platforms work. That feedback is still open."
      },
      {
          "question": "Should plugin authors wait for core?",
          "answer": "Stop writing new plaintext options for keys. Prefer env or wp-config constants now. Watch the feature plugin and map one option to wp_import_option_as_secret() when the surface freezes. Do not invent a third encryption scheme if you can wait a release."
      },
      {
          "question": "What about salt rotation breaking secrets?",
          "answer": "Envelope encryption is meant so rotating the site wrapping key re-wraps a master key instead of every blob. Site Kit already documented salt rotation as a breakage mode at scale. Undecryptable secrets should surface in Site Health, not as silent empty strings."
      },
      {
          "question": "Is wp-config a secrets API?",
          "answer": "It is a file-backed constant bag. Better than the options table for not showing up in SQL dumps. Worse for rotation, inventory, and CLI hygiene. The proposal is trying to be an actual object with fingerprints, versions, and capabilities."
      }
  ],
  coverImage: "/images/wordpress-72-secrets-api.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/wordpress-72-secrets-api.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/wordpress-72-secrets-api.webp\" alt=\"WordPress Wants a Real Secrets API in 7.2. Plugins Still Dump Keys in wp-config.\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>WordPress still does not have a type called \"secret.\"</p>\n<p>That sounds like a nit until you look at a real <code>wp_options</code> dump. Stripe keys, SMTP passwords, Google tokens, OpenAI keys, webhook signing secrets — sitting next to the site tagline, <code>autoload</code> sometimes on, copied into every backup, every staging clone, every \"can you send me a DB so I can reproduce.\" Eric Mann's Make/Core proposal (August 25, 2026) says this is not a morality play about plugin authors. It is the only API they were given.</p>\n<p>The title of this post mentions <code>wp-config</code> because that is the other drawer. Agencies that got burned once moved keys into <code>define( 'STRIPE_SECRET', '...' )</code> or into host env. That is better than a plaintext option. It is still not inventory, rotation, or an audit trail. It is a PHP file that gets copied into git if someone is sloppy, and never copied if someone is careful, with no middle.</p>\n<p>7.2 is being asked to grow a small, encrypted, filter-hostile API. No settings screen in this release. WP-CLI in this release. Hosts already arguing in the comments. That combination is more interesting than a merge announcement.</p>\n<h2>The Options Table Is a Credential Dump</h2>\n<h3>How we got here</h3>\n<p><code>add_option</code> / <code>update_option</code> is how WordPress stores almost everything that isn't a post. Plugin settings UIs write strings. There is no <code>update_secret()</code>. So the Stripe secret is a string. The tagline is a string. Backup plugins copy strings. <code>wp option get stripe_secret_key</code> prints a string in a shared terminal.</p>\n<p>Mann's post is explicit: this was survivable when the average site had a Mailchimp key and a reCAPTCHA secret. Model-provider keys are metered spend. A leak is not just embarrassment. It is someone else's invoice.</p>\n<h3>What \"plugins dump keys in wp-config\" actually means</h3>\n<p>Two bad patterns live side by side:</p>\n<ol>\n<li>Settings page → <code>update_option( 'my_api_key', $raw )</code>.</li>\n<li>README: \"paste your key into <code>wp-config.php</code>.\"</li>\n</ol>\n<p>The second is often sold as the secure pattern. Delicious Brains' Connectors writeup (WordPress 7.0 era) still ranks environment variables first, PHP constants second, database options last. For Connectors, env short-circuits the DB lookup so the raw key is not read from <code>wp_options</code>. That is a host- and ops-shaped fix. It does not give plugin authors a core object. It also does not help the long tail of Woo gateways and SMTP plugins that never read env.</p>\n<p><code>wp-config.php</code> keys have their own failure mode: they travel with the filesystem. Database dumps stop including them. Git history might start including them. Neither layer has rotation semantics.</p>\n<h3>Independent encryption is not a platform</h3>\n<p>Site Kit encrypts Google credentials. Woo gateways handle payment keys. SMTP plugins hold mail passwords. Each is a cipher, a key-derivation story, and an incident report waiting for a unique bug. Mann's point: none of them can be reviewed once on behalf of the ecosystem. A core API is the review surface.</p>\n<p>A proof-of-concept has been floating for about six months, originally after Two Factor / TOTP token discussions, later overlapping the AI plugin's key-encryption experiment (Jeffrey Paul noted that in comments). The <code>wp_</code> names in the proposal are proposed, not shipping.</p>\n<h2>What the Proposal Actually Is</h2>\n<h3>Four functions and a value object</h3>\n<pre><code class=\"language-php\">wp_set_secret( string $name, string $value ): bool|WP_Error\nwp_get_secret( string $name, string $version = WP_Secret_Version::CURRENT ): WP_Secret|null|WP_Error\nwp_delete_secret( string $name ): bool|WP_Error\nwp_import_option_as_secret( string $option, string $name ): bool|WP_Error\n\nWP_Secret::reveal(): string\nWP_Secret::fingerprint(): string\n</code></pre>\n<p>Call site from the proposal:</p>\n<pre><code class=\"language-php\">wp_set_secret( 'my-plugin/api-key', $key_from_form );\n\n$secret = wp_get_secret( 'my-plugin/api-key' );\n\nif ( is_wp_error( $secret ) ) {\n    // Exists, but unusable. Key material changed. Tell the user.\n} elseif ( null === $secret ) {\n    // Doesn't exist. Show the connect flow.\n} else {\n    $client = new API_Client( $secret-&gt;reveal() );\n}\n</code></pre>\n<p>Three return shapes on get: object, <code>null</code>, <code>WP_Error</code>. Collapsing \"missing\" and \"cannot decrypt\" into <code>false</code> is how you re-run onboarding when the real problem is a rotated salt. That distinction is doing more work than the function count suggests.</p>\n<h3>Namespaces by convention</h3>\n<p><code>plugin-slug/secret-name</code>. Future admin UI can group. Cross-plugin reads are not cryptographically denied. Mann says slugs are not authentication. A malicious plugin that can run PHP can <code>wp_get_secret( 'woocommerce/...' )</code> as easily as it can read <code>DB_PASSWORD</code>. The API is not a sandbox.</p>\n<h3>Default storage is still the options table</h3>\n<p>Ciphertext blobs, <code>autoload=no</code>, excluded from <code>options.php</code> and the REST settings endpoint. Same idea as object cache versus options: default substrate, replaceable. If you expected Vault-by-default on a $5 VPS, you misread the audience. The audience is \"stop writing plaintext into the same table as the tagline.\"</p>\n<p>Capabilities: <code>manage_secrets</code> and <code>manage_network_secrets</code>. A change hook on write carries actor, timestamp, old and new fingerprints. Fingerprints are loggable. Values are not.</p>\n<h2>Encryption Is Not Optional</h2>\n<h3>Envelope, not a <code>WP_ENCRYPT_OPTIONS</code> constant</h3>\n<p>Per-secret data key, wrapped by a master key. No plaintext mode. Mann's line: an API that can be configured to store plaintext is an options API with a misleading name.</p>\n<p>The objection is data loss. Rotate salts, migrate hosts, secrets stop decrypting. Plaintext options never do that. Envelope encryption is the answer they want: rotate wrapping without rewriting every blob. Preferred key source is a dedicated constant (<code>WP_SECRETS_KEY</code> in secondary writeups). Fallback derived from <code>LOGGED_IN_KEY</code> / <code>LOGGED_IN_SALT</code> for sites that cannot set a constant. Site Kit already ships a version of that fallback and documents that salt rotation breaks stored credentials. The ecosystem lived with the docs instead of rejecting encryption.</p>\n<p>When key material is gone: Site Health should report undecryptable secrets. Recovery is re-enter. Nothing silently becomes <code>''</code>.</p>\n<p>libsodium has been bundled since PHP 7.2. WordPress still has <code>sodium_compat</code> when the extension is disabled. No new PECL story.</p>\n<h3>What a stolen dump becomes</h3>\n<p>In scope, from the proposal: database dumps, backups in object storage, SQL injection reading <code>wp_options</code>, options.php, export files, screenshots, debug logs, a compromised read-only replica.</p>\n<p>Out of scope: RCE in the WordPress process. If they can run PHP, they can <code>reveal()</code>. Masking in <code>var_dump</code> and logs is shoulder-surfing hygiene, not a privilege boundary. <code>WP_Secret</code> refuses serialization so it does not fall into a shared persistent object cache on cheap hosting.</p>\n<p>No export. Staging does not get a \"download all secrets\" button. Fingerprints let you confirm a re-entered value matches. Migrations mean re-entry at the destination. That will annoy agencies who clone prod to staging with one command. It is also the point.</p>\n<h2>No Filter on the Retrieval Path</h2>\n<h3>Why this will get pushback</h3>\n<p>WordPress extends by filters. A <code>wp_get_secret</code> filter would receive every credential in plaintext. Any plugin could register it. Mann expects this to be the loudest objection. Providers replace that flexibility: you swap storage or keyring, you do not intercept reveal.</p>\n<p>If you were planning to \"log secret access\" via a filter that receives the string, you will need the write hook and fingerprints instead, or an observer that does not see plaintext. That is stricter than the rest of core. Good.</p>\n<h3>Fail closed</h3>\n<p>Unreachable external store or key backend: reads and writes error. No fallback to local wrapping. Fail open would be the first incident report.</p>\n<h2>The Drop-In Hosts Already Hate</h2>\n<h3><code>secrets.php</code>: storage versus keyring</h3>\n<p>Two extension points, independently replaceable:</p>\n<ul>\n<li>Where ciphertext lives (Vault, Parameter Store, host API)</li>\n<li>What wraps the master key (KMS, HSM)</li>\n</ul>\n<p>Neither is handed plaintext. Neither can turn encryption off.</p>\n<p>That last pair of sentences is the fight in the comments.</p>\n<h3>Pantheon (Chris Reynolds, August 25)</h3>\n<p>They have run an external secrets store in production since 2024 (GA to all customers earlier in 2026). Application role is read-only. Writes go through Terminus or the dashboard. Typical Vault posture.</p>\n<p>Gaps they named:</p>\n<ol>\n<li>No way to express \"readable here, not writable here.\" Every plugin settings screen assumes <code>set()</code> works.</li>\n<li>Platform stores often <em>are</em> the encryption boundary. They need plaintext over an authenticated channel so the same secret works in other environments. If the drop-in only sees WordPress ciphertext, they double-encrypt and the value is opaque outside WP.</li>\n</ol>\n<h3>Altis (Ryan McCue, August 27)</h3>\n<p><code>-1</code> on the proposal as written. Their store is Parameter Store / HSM-backed. WordPress must not hold wrapping keys. API today is <code>get_secret( string $key ): string</code> and that's it. If encryption happens above the provider, he asked what this is besides encrypted options.</p>\n<p>Kaspars (August 29) tried to split the baby: standardized encrypted options for normal installs; hosts noop the setter and return <code>WP_Error</code> with platform instructions. Encryption key in the same PHP process as the ciphertext is not ideal. It is still better than plaintext.</p>\n<p>This is not a bikeshed. It is whether 7.2 is a shared-hosting hardening API, a host-integration API, or both. Mann asked hosts to say what the drop-in is missing <em>before</em> freeze. They did.</p>\n<h2>Two Version Slots and Import, Not Migrate</h2>\n<h3><code>CURRENT</code> and <code>PREVIOUS</code></h3>\n<p>Not unbounded history. Overwrite keeps the old value so a mistyped key is recoverable and in-flight requests can finish. Retiring <code>PREVIOUS</code> is an operator action. No cron guessing when Stripe has drained the old key.</p>\n<p>Native enums need PHP 8.1. Core's minimum is still 7.4, even if 8.3+ is recommended. String constants on a <code>final class WP_Secret_Version</code> are the portable stand-in.</p>\n<p>Named history of every credential ever held would live in backups forever. Two slots is the product decision.</p>\n<h3><code>wp_import_option_as_secret()</code></h3>\n<p>No automatic sweep. Core cannot tell a Stripe key from a widget setting. Plugin authors move one option, on purpose, on their upgrade schedule. Imported secrets are flagged for rotation because the plaintext is already in every backup you own. Encrypting in place without rotation is theater.</p>\n<pre><code class=\"language-php\">function myplugin_upgrade_320( string $old, string $new ): void {\n    if ( version_compare( $old, '3.2.0', '&lt;' ) ) {\n        $r = wp_import_option_as_secret( 'myplugin_api_key', 'myplugin/api-key' );\n        if ( is_wp_error( $r ) ) {\n            // leave the option; show an admin notice\n            return;\n        }\n        delete_option( 'myplugin_api_key' );\n        // Site Health / plugin UI: rotate, because backups still have plaintext\n    }\n}\n</code></pre>\n<p>Do not <code>delete_option</code> until you know get works. Do not skip the rotation flag in the UX. The proposal treats rotation as the actual fix.</p>\n<h2>Multisite Is in v0</h2>\n<p>Network-level secrets exist. Salts are network-wide, so separate option rows would be logical separation only. They want per-site subkeys via <code>sodium_crypto_kdf_derive_from_key()</code> from a network root key. Site and network APIs are separate functions, separate capabilities, no implicit fallback.</p>\n<p>Adding this later means a second key hierarchy and a migration. Doing it now means rotating a network wrapping key re-wraps one value, not 500 sites independently.</p>\n<p>If you run a 500-site network and keep putting the same SendGrid key in each site's options, the API is offering you a network secret. Use it. If each site has a different Stripe account, don't.</p>\n<h2>WP-CLI In, Admin UI Out</h2>\n<h3>Why CLI is in 7.2</h3>\n<p>First-party consumer in the same release. The fastest way to find out the API is awkward. Also closes a real leak: <code>wp option update my_key sk-live-...</code> lands in shell history and <code>ps</code> on shared hosts. Reading and writing through the secrets API is the same change as \"don't argv the credential.\"</p>\n<p>Exact command names are still feedback-wanted. Expect something in the shape of set/get/delete/list with fingerprints, not values, on list.</p>\n<pre><code class=\"language-bash\"># speculative shape — not frozen\nwp secret set myplugin/api-key --from-file=./key.txt\nwp secret fingerprint myplugin/api-key\nwp secret delete myplugin/api-key --retire-previous\n</code></pre>\n<p><code>--from-file</code> is the habit you want even before core ships. Never paste live keys into bash.</p>\n<h3>Why the screen waits for 7.3</h3>\n<p>Storage and retrieval have to be right once. Every plugin and every future screen inherit them. Designing a UI on a moving API is how you get two migration stories. Mann would rather slip the whole API to 7.3 than freeze a bad surface for Beta 1.</p>\n<p>Connectors security audit is named as a requirement this API helps. Connectors already have a settings UI that stores plaintext unless you override with env. A 7.3 screen that lists secrets, fingerprints, last rotation — that is the missing object Mann described: you cannot ask \"which credentials exist\" today because there is no object.</p>\n<h2>wp-config.php Is Still Going to Be There</h2>\n<h3>Constants are not going away</h3>\n<pre><code class=\"language-php\">// still valid, still useful on many hosts\ndefine( 'WP_SECRETS_KEY', getenv( 'WP_SECRETS_KEY' ) );\ndefine( 'MYPLUGIN_API_KEY', getenv( 'MYPLUGIN_API_KEY' ) );\n</code></pre>\n<p>Until plugins call <code>wp_get_secret()</code>, they will keep reading constants. A responsible 2026 plugin does:</p>\n<pre><code class=\"language-php\">function myplugin_key(): string {\n    if ( defined( 'MYPLUGIN_API_KEY' ) &amp;&amp; MYPLUGIN_API_KEY !== '' ) {\n        return MYPLUGIN_API_KEY;\n    }\n    if ( function_exists( 'wp_get_secret' ) ) {\n        $s = wp_get_secret( 'myplugin/api-key' );\n        if ( $s instanceof WP_Secret ) {\n            return $s-&gt;reveal();\n        }\n    }\n    $legacy = get_option( 'myplugin_api_key', '' );\n    return is_string( $legacy ) ? $legacy : '';\n}\n</code></pre>\n<p>Priority: env/constant, then Secrets API, then legacy option. Document that the option path is deprecated. Don't surprise-delete production keys on upgrade.</p>\n<h3>Why people still paste into wp-config</h3>\n<p>Shared hosting with no env UI. Agencies that refuse to put keys in the database after one leak. Cargo cult from \"don't put secrets in the DB\" Twitter threads that never mentioned envelope encryption.</p>\n<p>The proposal does not make <code>wp-config</code> wrong. It makes \"options table plaintext\" less defensible as the default plugin path. Hosts who already have a secret store will keep telling you to use <em>their</em> getter until the drop-in matches their write policy.</p>\n<h2>Plugin Author Checklist</h2>\n<h3>If you ship a settings field that is a credential</h3>\n<ul>\n<li>Namespace: <code>your-slug/purpose</code>.</li>\n<li>Do not add a <code>pre_option_</code> filter that logs the value \"for debugging.\"</li>\n<li>Handle <code>WP_Error</code> from get as a broken keyring, not as logged-out.</li>\n<li>Import once, rotate, then delete the plaintext option.</li>\n<li>Assume another plugin can read your name. Don't store a \"more secret\" secret next to a \"less secret\" one and expect isolation.</li>\n</ul>\n<h3>If you are Woo / SMTP / ESP scale</h3>\n<p>You already have incident history. Map one key in a beta of the feature plugin. Report whether two version slots are enough for your rotation. Mann asked that question on purpose.</p>\n<h3>If you are Two Factor / TOTP</h3>\n<p>Brian Haas's comment: strong +1, stop going your own way. TOTP secrets in options are a known smell. This API is closer to the right home than another <code>defuse/php-encryption</code> wrapper (emrl's comment: they already did that with hooks around specific options).</p>\n<h3>If you write host glue</h3>\n<p>Read Reynolds and McCue before you implement <code>secrets.php</code>. If your store is write-out-of-band, you need a read-only provider and plugin UX that does not sit in a spinner on <code>wp_set_secret()</code>. That UX is not in the proposal. Someone has to invent \"this key is managed in the panel.\"</p>\n<h2>Timeline and the Honest Slip Risk</h2>\n<p>Beta 1: October 20–22, 2026. Working backward from the post:</p>\n<ul>\n<li>Now through mid-September: feedback, feature plugin for testing.</li>\n<li>Late September: Trac patch matching the plugin surface.</li>\n<li>Before Beta 1: committed, or explicitly deferred to 7.3.</li>\n</ul>\n<p>Mann volunteered to implement, not to propose and vanish. That matters in WordPress more than in projects with a staffed secrets team.</p>\n<p>If host objections force a provider model that <em>does</em> take plaintext for KMS-style stores, the API will change before freeze. Do not screenshot the current function list into a client SOW as \"7.2 will have this.\" Write \"proposed for 7.2, feature plugin first.\"</p>\n<h2>What I Would Do on a Client Site This Week</h2>\n<p>Inventory: <code>wp option list</code> is the wrong tool but it's what you have. Search dumps for <code>sk-</code>, <code>rk_live</code>, <code>SG.</code>, <code>AKIA</code>, <code>ghp_</code>. Get those out of tickets and Slack.</p>\n<p>Move what you can to env or constants <em>now</em>. That is the Delicious Brains ordering and it still applies.</p>\n<p>Do not wait for 7.2 to stop emailing database dumps. Ciphertext in 7.2 does not un-send last year's gzip.</p>\n<p>When the feature plugin exists, run it on a staging site that has one real third-party key, not on a toy option. Practice the <code>WP_Error</code> path by rotating <code>LOGGED_IN_SALT</code> in a disposable clone (then throw the clone away).</p>\n<p>For WordPress 7.0 Connectors specifically, keep env overrides, and keep <code>wpmdb_preserved_options</code> so a pull does not overwrite local keys with production. That filter remains relevant until Connectors speak the Secrets API.</p>\n<h2>How This Fits the Rest of 7.x Security Work</h2>\n<p>The same week as this proposal, Make/Core also pushed the Core Security Initiative (AI-flooded HackerOne volume). Those are related only in the sense that WordPress is trying to grow first-party security machinery instead of hoping plugins invent it.</p>\n<p>Secrets API is not a WAF. It is not capability isolation between plugins. It is a type, encryption at rest by default, and a chokepoint you can log. That is a smaller claim than the title \"Secrets API\" sounds like. Smaller claims are the ones that can ship.</p>\n<p>If 7.2 lands the API and 7.3 lands a screen that still <code>echo</code>s a reveal into HTML, the plot was wasted. The no-filter rule has to survive the UI too.</p>\n<h3>Strings in, strings out, and other small knives</h3>\n<h3>No object graphs</h3>\n<p>The proposal will not serialize for you. You may <code>json_encode</code> a blob and store it. Core will not unpack it. That stops a secret from expanding into an object graph on read, which is how you accidentally hydrate a class that talks to the network. If your plugin wants structured credentials, you own the JSON. Keep it boring.</p>\n<h3>Nothing plaintext in the object cache</h3>\n<p>Shared Redis on cheap hosting is a side channel. <code>WP_Secret</code> refusing serialization is the mitigation they chose. If you <code>wp_cache_set</code> the revealed string yourself, you undid the API. Code review should grep <code>reveal()</code> and look at the next five lines.</p>\n<h3>Capabilities are not a design system</h3>\n<p><code>manage_secrets</code> / <code>manage_network_secrets</code> will get mapped badly on membership plugins. Super Admin versus Administrator versus a custom \"SEO manager\" role that currently <code>manage_options</code> is enough to see SMTP passwords. When you ship 7.2, audit who has <code>manage_options</code> today. Those people can already read plaintext options. After encryption, the capability split only helps if you stop granting <code>manage_options</code> like candy.</p>\n<h2>A Walk Through a Woo-Shaped Key</h2>\n<h3>Today</h3>\n<pre><code class=\"language-php\">// typical gateway: settings API writes the option\nregister_setting( 'woo_gw', 'woo_gw_secret' );\n\nfunction woo_gw_charge( $amount ) {\n    $key = get_option( 'woo_gw_secret' );\n    return WooGw\\Client::charge( $key, $amount );\n}\n</code></pre>\n<p><code>get_option</code> is also what <code>wp option get woo_gw_secret</code> prints. Support staff paste it into tickets. Staging clones production and charges real cards if the key is live. You already know this story.</p>\n<h3>After a careful import</h3>\n<pre><code class=\"language-php\">function woo_gw_charge( $amount ) {\n    if ( defined( 'WOO_GW_SECRET' ) &amp;&amp; WOO_GW_SECRET ) {\n        $key = WOO_GW_SECRET;\n    } else {\n        $secret = wp_get_secret( 'woo-gw/secret' );\n        if ( is_wp_error( $secret ) ) {\n            throw new RuntimeException( 'Payment key unreadable; check Site Health' );\n        }\n        if ( $secret === null ) {\n            throw new RuntimeException( 'Payment key missing' );\n        }\n        $key = $secret-&gt;reveal();\n    }\n    return WooGw\\Client::charge( $key, $amount );\n}\n</code></pre>\n<p>Throwing is better than charging with an empty string. The <code>WP_Error</code> branch is the salt-rotation case. If you map that to \"please reconnect Stripe\" you will train users to paste keys they already entered. Show \"encryption key changed\" copy instead.</p>\n<h3>Rotation with two slots</h3>\n<pre><code class=\"language-php\">wp_set_secret( 'woo-gw/secret', $new_from_stripe_dashboard );\n// CURRENT is new; PREVIOUS is old until you retire it\n$live = wp_get_secret( 'woo-gw/secret', WP_Secret_Version::CURRENT );\n$drain = wp_get_secret( 'woo-gw/secret', WP_Secret_Version::PREVIOUS );\n</code></pre>\n<p>Webhooks in flight might still sign with the old key. Keep <code>PREVIOUS</code> until Stripe says the old restricted key is unused. Then an explicit retire. Do not cron this on day 7 because a blog post said 7.</p>\n<h2>Backups, Migrate, and the Agency Habit</h2>\n<h3>WP Migrate, WP-CLI <code>db export</code>, phpMyAdmin</h3>\n<p>Ciphertext in <code>wp_options</code> still copies. The dump is less useful to a thief who only has SQL. The dump is still a map of which integrations you use (<code>woo-gw/secret</code> as a name is intelligence). Fingerprints in logs are intelligence. Name secrets like <code>woo-gw/secret</code>, not <code>woo-gw/sk_live_51H...</code>.</p>\n<p>Find-and-replace on dumps will not rotate envelope blobs. If your migrate plugin rewrites URLs inside options, confirm it will not touch ciphertext rows. Autoload is off; some migrate tools skip non-autoload. Test once.</p>\n<h3>The email-a-dump problem</h3>\n<p>Encrypting at rest does not stop someone attaching <code>prod.sql.gz</code> to Gmail. It changes what the attachment contains. Tell clients the new rule is still: no dumps through email. The API is not a substitute for access control on backups.</p>\n<h3>Object cache and HTML debug</h3>\n<p>Query Monitor, <code>WP_DEBUG_DISPLAY</code>, stack traces that interpolate options — those are the disclosure surfaces Mann listed. After 7.2, a plugin that <code>print_r( wp_get_secret( 'x' ) )</code> should print a mask. A plugin that <code>print_r( wp_get_secret( 'x' )-&gt;reveal() )</code> is a bug. Grep the second.</p>\n<h3>What hosts can ship without waiting</h3>\n<h3>Env remains the best override</h3>\n<pre><code class=\"language-nginx\"># sketch: php-fpm env\nenv WP_SECRETS_KEY;\nenv OPENAI_API_KEY;\n</code></pre>\n<p>Connectors already document this chain. Keep it. If 7.2's drop-in cannot express read-only stores, hosts should keep <code>pantheon_get_secret()</code>-style functions and map them in a mu-plugin until core catches up.</p>\n<h3>A mu-plugin shim for the gap year</h3>\n<pre><code class=\"language-php\">&lt;?php\n/**\n * Plugin Name: Secret get shim\n */\nfunction nandann_get_secret( string $name ): ?string {\n    $env = getenv( strtoupper( str_replace( '/', '_', $name ) ) );\n    if ( is_string( $env ) &amp;&amp; $env !== '' ) {\n        return $env;\n    }\n    if ( function_exists( 'wp_get_secret' ) ) {\n        $s = wp_get_secret( $name );\n        if ( $s instanceof WP_Secret ) {\n            return $s-&gt;reveal();\n        }\n    }\n    return null;\n}\n</code></pre>\n<p>Ugly names. Honest fallback. Delete it when plugins speak core.</p>\n<h3>Failure modes to tabletop</h3>\n<h3>You rotate <code>AUTH_KEY</code> in a hardening pass and half the site's integrations die</h3>\n<p>If they fell back to salts, this is the Site Kit lesson. Tabletop it before you run a \"refresh all salts\" security plugin on Monday. Dedicated <code>WP_SECRETS_KEY</code> in env is how you make salt rotation a session problem, not a Stripe problem.</p>\n<h3>Feature plugin API matches core, then core tweaks one return type</h3>\n<p>Mann's promise: core patch identical to plugin so nothing rewrites. If that promise slips, the rewrite is on plugin authors who adopted early. Pin the feature plugin version in composer or as a \"tested up to\" note.</p>\n<h3>A security plugin adds a retrieval filter \"for auditing\"</h3>\n<p>If core holds the no-filter line, that plugin cannot exist. If core folds, you get a documented steal hook. Watch the Trac discussion. This is the design decision most likely to get \"just one small filter.\"</p>\n<h3>7.2 versus 7.3 as a planning split</h3>\n<p>7.2, if it lands: storage, get/set/delete/import, CLI, capabilities, drop-in, Site Health for undecryptable blobs, no screen.</p>\n<p>7.3, if they keep the plan: a UI that must not become <code>options.php</code> for secrets. Group by plugin slug. Show fingerprints and timestamps. Never a \"view secret\" button without a loud confirm, and maybe not even then. Reveal is for code, not for HTML.</p>\n<p>Agencies should sell 7.2 work as \"stop writing keys into options\" and 7.3 work as \"teach the client's admin which keys exist.\" Different tickets. Different people.</p>\n<p>One more operational detail: <code>autoload=no</code> means secrets should not ride along with every page load's options cache the way <code>blogname</code> does. Confirm in object cache dumps after you import. If a plugin called <code>update_option( $k, $v, 'yes' )</code> on a key, the old autoload row may linger until you delete it. Import is not a vacuum.</p>\n<p>If you use a \"security hardening\" plugin that rewrites salts on a schedule, disable that schedule until <code>WP_SECRETS_KEY</code> is independent. Those plugins were written for cookie invalidation. They were not written for envelope keyrings.</p>\n<p>Multisite agencies: decide now whether SMTP is a network secret or a per-site secret. Getting that wrong in v0 means copying keys into 200 sites and then pretending the API failed. The API will do what you asked.</p>\n<p>Sources: Eric Mann, \"Proposal: A Secrets API for WordPress 7.2\" (Make/Core, August 25, 2026) and its comment thread (Pantheon, Altis, Two Factor, AI plugin); The WP Clan recap (August 26); The Repository (August 27); Delicious Brains on Connectors env vs database; WordPress 7.0 Connectors option naming; Site Kit's documented salt-rotation behavior as cited in the proposal; daily.dev's summary of the same Make/Core post. The feature plugin and Trac ticket may exist after this date — treat Make/Core as canonical until commit.</p>\n<hr />\n<div style=\"background: rgba(99,102,241,0.08); border: 1px solid rgba(99,102,241,0.25); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #818cf8; margin-top: 0;\">Need help with your WordPress site?</h3>\n  <p>Whether you're dealing with security vulnerabilities, plugin conflicts, slow load times, or Core Web Vitals failures — we handle the technical side so you can focus on your business. Custom plugin development, performance hardening, Wasm integrations, and ongoing maintenance retainers.</p>\n  <p><a href=\"/contact?ref=wordpress-blog\" style=\"display: inline-block; background: linear-gradient(135deg, #6366f1, #4f46e5); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Talk to Us About Your WordPress Site &rarr;</a></p>\n</div>\n",
};

export default post;
