import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "secure-tanstack-ai-mcp-oauth-with-vercel-connect",
  title: "Secure TanStack AI MCP OAuth Flows with Vercel Connect",
  description: "Connect TanStack AI to OAuth-protected MCP servers with per-user subjects, fresh runtime tokens, route-boundary consent, and safer tool errors in production.",
  date: "2026-09-24",
  readTime: "22 min",
  category: "Engineering",
  tags: ["TanStack AI","Model Context Protocol","OAuth 2.1","Vercel Connect","AI Agents","Next.js"],
  faqs: [
      {
          "question": "What does connectMCPTransport do?",
          "answer": "The @vercel/connect/tanstack-ai helper wraps a TanStack MCP transport configuration with a Vercel Connect-backed authentication provider. Vercel says the provider is called before every MCP request so the token is fresh."
      },
      {
          "question": "Why should MCP consent happen before the model runs?",
          "answer": "Creating the MCP client first lets the route catch a consent challenge and redirect the user. If consent is discovered only during a tool call, the failure may reach the model as an error string instead of reaching the user as an authorization step."
      },
      {
          "question": "How is OAuth access scoped to a user?",
          "answer": "Pass a stable application user ID as a Vercel Connect subject with type user. The server must derive that ID from an authenticated session and must never trust a user ID supplied directly by the browser."
      },
      {
          "question": "Should an MCP access token be sent to the model?",
          "answer": "No. Vercel Connect retrieves credentials in server-side code and the transport attaches them to MCP requests. Keep tokens out of prompts, model-visible tool arguments, logs, and client-side bundles."
      },
      {
          "question": "What HTTP status should an OAuth consent redirect use?",
          "answer": "Vercel's TanStack AI example uses a 303 redirect. That converts the original POST flow into a GET to the consent URL and avoids replaying the chat request body at the authorization destination."
      },
      {
          "question": "Can TanStack AI use OAuth without Vercel Connect?",
          "answer": "Yes. TanStack AI accepts an OAuthClientProvider from the official MCP SDK, or a custom StreamableHTTPClientTransport for interactive authorization. Vercel Connect manages token storage and consent lifecycle for the integration described here."
      },
      {
          "question": "How should an app handle expired or revoked MCP consent?",
          "answer": "Recreate or revalidate the MCP client at the route boundary, extract a new consent challenge when authorization is missing, and redirect the user. Treat repeated revocation as an authentication event, not a prompt for the model to solve."
      }
  ],
  coverImage: "/images/secure-tanstack-ai-mcp-oauth-with-vercel-connect.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/secure-tanstack-ai-mcp-oauth-with-vercel-connect.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/secure-tanstack-ai-mcp-oauth-with-vercel-connect.webp\" alt=\"Secure TanStack AI MCP OAuth Flows with Vercel Connect\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>TanStack AI agents can call OAuth-protected MCP servers through Vercel Connect without storing provider tokens in application code. The integration wraps a TanStack MCP transport with a Connect-backed authentication provider, scopes the connection to a subject such as the signed-in user, and retrieves a fresh token before each MCP request.</p>\n<p>The important part is where consent happens. Create the authenticated MCP client before starting the model stream. If the user has not authorized the provider, catch the consent challenge at the HTTP route and return a 303 redirect. If authorization is first attempted inside a model tool call, the user may see a tool error while the model receives an error string it cannot repair.</p>\n<h2>The Integration in One Request Flow</h2>\n<p>Vercel's 24 September 2026 launch example shows the complete shape: authenticate the application user, create an MCP client with <code>connectMCPTransport</code>, start <code>chat</code>, and handle consent around client creation.</p>\n<pre><code class=\"language-ts\">import { chat, toServerSentEventsResponse } from '@tanstack/ai'\nimport { createMCPClient } from '@tanstack/ai-mcp'\nimport { vercelGatewayText } from '@tanstack/ai-vercel-gateway'\nimport {\n  connectMCPTransport,\n  getConsentChallenge,\n} from '@vercel/connect/tanstack-ai'\n\nexport async function POST(request: Request) {\n  const userId = await requireUserId(request)\n  const { messages } = await request.json()\n\n  try {\n    const linear = await createMCPClient({\n      transport: connectMCPTransport(\n        { type: 'http', url: 'https://mcp.linear.app/mcp' },\n        'oauth/linear',\n        { subject: { type: 'user', id: userId } },\n      ),\n    })\n\n    const stream = chat({\n      adapter: vercelGatewayText('anthropic/claude-opus-5'),\n      messages,\n      mcp: { clients: [linear] },\n    })\n\n    return toServerSentEventsResponse(stream)\n  } catch (error) {\n    const challenge = getConsentChallenge(error)\n    if (challenge) return Response.redirect(challenge.url, 303)\n    throw error\n  }\n}\n</code></pre>\n<h3>Authentication comes first</h3>\n<p><code>requireUserId</code> is application code, not a Vercel helper shown by the integration. It must validate the signed-in session and return a stable internal user identifier. Do not accept <code>userId</code> from the JSON body, query string, or a model-generated argument.</p>\n<p>The Connect subject selects whose authorization should be used. A caller-controlled subject becomes an account-confusion vulnerability because one user could request another user's stored connection.</p>\n<h3>MCP client creation comes before chat</h3>\n<p><code>createMCPClient</code> performs the initial MCP interaction needed to discover and initialize the server. Vercel says a missing authorization grant raises a consent challenge at this point, before the model runs.</p>\n<p>The route can turn that challenge into a browser navigation. No model tokens are spent, no tool plan begins, and the consent state remains an ordinary web authentication concern.</p>\n<h3>Chat receives an already authenticated client</h3>\n<p>Once client creation succeeds, TanStack AI receives a ready MCP client in <code>mcp.clients</code>. Tool discovery and calls use that client. The model does not need to know how tokens are stored or refreshed.</p>\n<p>This boundary keeps authentication out of prompts and tool schemas. The application controls identity and consent, while the model decides when an available tool is useful.</p>\n<h2>What Vercel Connect Manages</h2>\n<p>Vercel Connect is a credential connection layer for OAuth, MCP, API keys, and managed integrations. For OAuth, it centralizes authorization and token exchange, then lets server-side code request credentials for a subject at runtime.</p>\n<h3>Tokens stay out of application storage</h3>\n<p>The Connect OAuth documentation positions the service as a way to keep access tokens out of prompts, source code, client-side bundles, and application-managed token tables. The runtime authenticates to Vercel and requests the credential associated with a configured connector and subject.</p>\n<p>This reduces the amount of refresh-token logic and encrypted storage an application must build. It does not remove the application's responsibility to authenticate users or authorize which connector each user may access.</p>\n<h3>The connector identifies the provider configuration</h3>\n<p>The second argument to <code>connectMCPTransport</code> is a connector identifier such as <code>oauth/linear</code>. It refers to the OAuth integration configured for the Vercel project and environment.</p>\n<p>Keep connector names in server configuration. A user should choose among product-approved integrations, not submit an arbitrary connector ID that the server forwards to Connect.</p>\n<h3>Tokens are requested for every MCP call</h3>\n<p>Vercel says the Connect-backed provider is called before every MCP request so the token is always fresh. The transport attaches the resulting authorization to the outbound MCP request.</p>\n<p>That is useful for rotation and refresh. It also means Connect availability and latency sit on the tool-call path, so timeouts, retry policy, and observability need to distinguish credential retrieval from MCP server execution.</p>\n<h2>What TanStack AI Manages</h2>\n<p>TanStack AI's MCP client supports Streamable HTTP, legacy SSE, stdio, in-memory, and custom transports. For remote HTTP servers, it can accept an OAuth provider compatible with the official MCP SDK.</p>\n<h3>Transport and auth remain replaceable</h3>\n<p>Without Vercel Connect, an application can pass an <code>OAuthClientProvider</code> in the HTTP transport configuration. The underlying MCP SDK handles token attachment, refresh, and retry after a 401 when the provider supplies valid or refreshable token state.</p>\n<p>Vercel's helper supplies that provider using Connect. The MCP client remains a TanStack AI component, which keeps the model and tool integration separate from credential storage.</p>\n<h3>Custom transport is the escape hatch</h3>\n<p>TanStack's documentation notes that interactive authorization can require retaining a <code>StreamableHTTPClientTransport</code> reference and calling <code>finishAuth(code)</code> after the OAuth callback. Because <code>createMCPClient</code> normally constructs the transport internally, advanced custom flows build the transport first and pass it to the client.</p>\n<p>The Connect adapter exists to avoid rebuilding that lifecycle in the common hosted case. Use a custom transport only when the provider or product flow genuinely needs behavior the adapter does not expose.</p>\n<h3>MCP errors still need application semantics</h3>\n<p>TanStack AI can surface MCP connection and tool errors, but it cannot decide whether an error means \"ask the user for consent,\" \"retry later,\" or \"the requested action is forbidden.\" The route and tool layer should classify those cases before raw text reaches the model.</p>\n<p>Authentication errors are control flow. Treating them as model context wastes tokens and can provoke repeated tool calls.</p>\n<h2>Use a Stable Per-User Subject</h2>\n<p>The subject binds an OAuth connection to the principal acting through the agent.</p>\n<pre><code class=\"language-ts\">const subject = {\n  type: 'user' as const,\n  id: session.user.id,\n}\n</code></pre>\n<h3>Derive it from a verified session</h3>\n<p>Resolve the subject after validating a secure cookie, bearer session, or trusted identity-provider assertion. Use the internal application ID rather than an email address that may change or be reassigned.</p>\n<p>If the application supports organizations, keep organization membership checks alongside user authentication. A valid user token does not automatically authorize work in every tenant.</p>\n<h3>Keep subjects tenant-aware in your own policy</h3>\n<p>The example subject is a user. Your application may need to verify that the user can access the requested workspace, project, or account before exposing its MCP tools.</p>\n<p>Do not encode all authorization into a clever subject string. Keep a normal policy check in application code, and use the subject to select the correct connection after access is approved.</p>\n<h3>Avoid service-wide shared connections for user actions</h3>\n<p>A single shared OAuth grant makes every tool call look like the same external identity. That weakens audit trails and can let one user exercise another user's external permissions.</p>\n<p>Use a service connection only for an explicitly service-owned workflow. User-initiated actions should use the user's authorization and scopes whenever the provider supports it.</p>\n<h2>Handle Consent at the Route Boundary</h2>\n<p>The consent branch is part of the route's response contract. Design the frontend for it instead of treating a redirect as an unexpected fetch error.</p>\n<h3>Why Vercel uses 303</h3>\n<p>The chat request is a POST. A 303 response tells the client to retrieve the consent URL with GET, avoiding a replay of the chat body at the authorization endpoint.</p>\n<p>Browser navigation and programmatic <code>fetch</code> handle redirects differently. If the chat library follows redirects inside an XHR-style request, the consent page may not replace the current page. Test the actual client behavior and return a structured authorization response when full-page navigation is required.</p>\n<h3>Preserve the user's intent safely</h3>\n<p>The user expects to continue after consent. Store a short-lived server-side continuation record containing the conversation ID, intended connector, and safe return path. Send only an opaque nonce through the redirect state.</p>\n<p>Do not put the full prompt, model messages, token, or arbitrary return URL in OAuth state. Validate the callback destination against an allowlist and bind the state to the same signed-in user.</p>\n<h3>Make denial a normal outcome</h3>\n<p>Users can deny access. The callback should return them to the application with a clear status and leave the conversation usable without that connector.</p>\n<p>Do not immediately send the model into the same failing tool path. Mark the connection unavailable until the user explicitly tries to connect again.</p>\n<h2>Keep OAuth Errors Out of the Agent Loop</h2>\n<p>Vercel's launch note calls out a subtle failure: if consent is raised during a tool call, the model receives it as an error string instead of the user receiving a redirect.</p>\n<h3>Preflight required connectors</h3>\n<p>If the route knows the conversation requires a specific MCP server, create that client before starting the stream. This catches missing consent early.</p>\n<p>For optional tools, you can initialize available clients at request start and omit any connector that needs consent. The UI can show a Connect control without forcing authorization for a tool the model may never use.</p>\n<h3>Classify errors by audience</h3>\n<p>An OAuth consent challenge is for the user interface. A rate limit may be retryable by the application. A validation error may be useful to the model so it can correct tool arguments. A forbidden operation should remain a policy denial.</p>\n<p>Create typed error categories rather than flattening every failure into text:</p>\n<pre><code class=\"language-ts\">type ToolBoundaryError =\n  | { kind: 'consent_required'; connector: string; url: string }\n  | { kind: 'temporarily_unavailable'; retryAfterMs?: number }\n  | { kind: 'forbidden'; policy: string }\n  | { kind: 'tool_validation'; message: string }\n</code></pre>\n<p>Only the final category normally belongs in model-visible correction context.</p>\n<h3>Cap repeated authentication attempts</h3>\n<p>A revoked grant or provider outage can cause repeated 401 responses. Limit automatic retries and record the connector, subject hash, MCP host, status class, and request correlation ID.</p>\n<p>Never log access tokens or authorization headers. Repeated failure should end in a user-facing reconnect action or an operator-visible incident, not an infinite tool loop.</p>\n<h2>Configure the OAuth Connector</h2>\n<p>Vercel documents a CLI setup for an OAuth or OIDC provider:</p>\n<pre><code class=\"language-bash\">vercel link\nvercel connect create your-service.com --name my-app\nvercel env pull\n</code></pre>\n<p>For a supported integration such as Linear, use the provider-specific connector path and follow its required redirect URI and scope configuration.</p>\n<h3>Separate environments</h3>\n<p>Development, preview, and production should use separate connector configuration or an explicit environment binding. A preview deployment must not retrieve a production user's credentials by accident.</p>\n<p>Register redirect URIs for each approved environment. Avoid wildcard callbacks when the provider offers stricter configuration.</p>\n<h3>Request minimum scopes</h3>\n<p>Start with the read scopes needed for tool discovery and retrieval. Add write scopes only for tools that change external state, and display those capabilities clearly during consent.</p>\n<p>Scope changes often require reauthorization. Version the connector policy and tell the user why a new grant is being requested.</p>\n<h3>Bind Connect access to the Vercel project</h3>\n<p>Vercel says Connect can bind connections to projects and environments while Vercel OIDC authenticates runtime requests. Keep production access limited to the production project and deployment identity.</p>\n<p>Review who can modify connector configuration in Vercel. A secure runtime can still be redirected to an unsafe provider if administrative access is too broad.</p>\n<h2>Harden the MCP Transport</h2>\n<p>OAuth authenticates the request, but transport security and server trust still matter.</p>\n<h3>Allowlist MCP origins</h3>\n<p>Keep the MCP URL in server-owned configuration. Do not let a browser or model provide an arbitrary URL to <code>connectMCPTransport</code>, because that can turn the server into an authenticated request proxy.</p>\n<p>Validate HTTPS, exact hostname, path, and expected connector pairing. A Linear token should never be attached to a host chosen from user input.</p>\n<h3>Set timeouts and cancellation</h3>\n<p>Chat requests are long lived, while OAuth retrieval and MCP initialization should have bounded latency. Propagate request cancellation to the MCP client and close transports when the stream ends.</p>\n<p>Separate timeout messages for Connect and the MCP server. Operators need to know whether credentials could not be fetched or the remote tool service did not respond.</p>\n<h3>Validate discovered tools</h3>\n<p>An authenticated MCP server can publish tool descriptions and schemas that influence the model. Pin or allowlist expected tool names for sensitive integrations, and require human confirmation for high-impact actions.</p>\n<p>OAuth proves which account is acting. It does not prove every tool call is safe or intended.</p>\n<h2>Authorize Every Tool Call</h2>\n<p>The external provider enforces its token scopes, while the application should enforce product and tenant policy before a tool runs.</p>\n<h3>Separate connection from permission</h3>\n<p>A connected Linear account may allow issue deletion, but your product may expose only search and comment tools. Filter the available tool set and add server-side checks for project ownership and action type.</p>\n<p>Do not rely on model instructions such as \"never delete.\" A tool that must not be used should be absent or denied in code.</p>\n<h3>Require confirmation for mutations</h3>\n<p>Read operations can often run automatically under narrow scopes. Creating, editing, deleting, sending, paying, or deploying should require a confirmation that names the target and effect.</p>\n<p>Bind approval to the exact arguments and a short expiry. If the model changes the target after approval, request a new confirmation.</p>\n<h3>Preserve external audit identity</h3>\n<p>Per-user OAuth lets external systems record the actual account behind a change. Include your conversation and tool-call correlation IDs in permitted metadata where the provider supports it.</p>\n<p>Keep an application audit record with the user, connector, MCP server, tool, arguments hash, approval, result status, and timestamps. Exclude tokens and sensitive response bodies.</p>\n<h2>Manage Token Freshness and Revocation</h2>\n<p>Calling the Connect provider before each request helps with refresh and rotation, but applications still need predictable behavior when a grant expires or is revoked.</p>\n<h3>Do not cache raw tokens in application memory</h3>\n<p>Let the Connect-backed provider retrieve the appropriate credential. A process-level token cache can outlive revocation, mix subjects through a bad key, or create inconsistent refresh behavior across instances.</p>\n<p>If performance requires caching, use only a library-supported mechanism with expiry and exact subject, connector, environment, and scope keys. The default should be no application-owned token cache.</p>\n<h3>Reconnect after revocation</h3>\n<p>When a provider rejects a previously valid grant, stop the tool request and re-enter the consent flow at the route boundary. Tell the user that the connection expired or was revoked.</p>\n<p>Do not ask the model to reinterpret a 401. The model cannot refresh a user's authorization and should not be given token details.</p>\n<h3>Disconnect cleanly</h3>\n<p>Provide a product control to disconnect an integration. Revoke the provider grant when supported, remove or disable the Connect relationship, clear application connection metadata, and stop presenting those tools to later chats.</p>\n<p>Audit the disconnect without retaining the credential. A user should be able to see which integrations remain connected.</p>\n<h2>Protect the Browser Boundary</h2>\n<p>The example runs on the server. Keep it there.</p>\n<h3>Never expose connector credentials to client code</h3>\n<p>The browser sends messages and receives chat events. It should not receive access tokens, refresh tokens, <code>GH_TOKEN</code>-style environment values, or internal Connect credentials.</p>\n<p>The consent URL is the exception because the browser must navigate to it. Validate that it comes from the expected challenge extractor and avoid logging its full query string.</p>\n<h3>Defend against CSRF and login confusion</h3>\n<p>Bind the chat POST and OAuth callback to the authenticated session. Use same-site cookies where appropriate, CSRF protection for state-changing endpoints, and a one-time OAuth state value.</p>\n<p>If the user signs out or changes accounts during consent, invalidate the continuation and ask them to start again. Never attach the returned grant to whichever session happens to be active.</p>\n<h3>Limit message body size</h3>\n<p>Authentication happens before model execution, but parsing an unbounded request can still consume memory. Enforce content type, body size, message count, and allowed message structure before initializing external connections.</p>\n<p>This also reduces the chance that tool or system fields supplied by an untrusted client bypass the intended server configuration.</p>\n<h2>Test the Full Consent Lifecycle</h2>\n<p>Unit tests around <code>getConsentChallenge</code> are useful, but browser and integration tests catch redirect and session problems.</p>\n<h3>First-time user</h3>\n<p>Start with no stored grant. POST a chat request, assert that no model call begins, receive the consent response, complete authorization, return to the application, and retry the original intent through a safe continuation.</p>\n<p>Verify that the external provider shows the correct scopes and that the resulting tool call uses the signed-in user's account.</p>\n<h3>Returning user</h3>\n<p>With a valid grant, create the MCP client and stream a response without redirect. Confirm that credentials never appear in client events, application logs, model messages, or tool arguments.</p>\n<p>Exercise several MCP requests so token retrieval and connection reuse behave as expected.</p>\n<h3>Denial, expiry, and revocation</h3>\n<p>Test a user who denies consent, a token that expires between calls, a revoked grant, a provider 401, and a Connect outage. Each case should produce a bounded, user-readable outcome.</p>\n<p>Assert that the model does not receive consent URLs or raw authentication errors as tool content.</p>\n<h3>Cross-user isolation</h3>\n<p>Create grants for two users and request the same MCP tool concurrently. Verify that each external call uses the matching account and that changing a body field cannot select the other subject.</p>\n<p>This is the most important multitenant test. A working happy path says nothing about subject isolation.</p>\n<h2>Observe Without Logging Secrets</h2>\n<p>Production debugging needs enough structure to locate failures without turning logs into a token store.</p>\n<h3>Useful fields</h3>\n<p>Record request ID, user subject hash, tenant ID, connector name, MCP hostname, lifecycle stage, tool name, status category, retry count, and duration. Hash or map the user identifier according to your logging policy.</p>\n<p>Track consent-required rate, consent completion rate, Connect latency, MCP initialization failures, 401 frequency, tool latency, and repeated-call loops.</p>\n<h3>Fields to exclude</h3>\n<p>Do not log authorization headers, access tokens, refresh tokens, consent query strings, cookies, full OAuth callback URLs, or unrestricted MCP response bodies.</p>\n<p>Prompt and tool arguments can also contain customer data. Apply field-level redaction and retention limits rather than assuming application logs are harmless.</p>\n<h3>Trace route, client, and tool stages</h3>\n<p>Use one correlation ID from the chat POST through Connect retrieval, MCP initialization, model stream, tool call, and response. Separate spans make latency and failure ownership visible.</p>\n<p>When a tool fails, operators should be able to tell whether the cause was application authentication, missing consent, token retrieval, transport, provider authorization, tool validation, or the external service.</p>\n<h2>A Production Route Pattern</h2>\n<p>Factor identity, connector configuration, and error classification into testable functions:</p>\n<pre><code class=\"language-ts\">const MCP_SERVERS = {\n  linear: {\n    url: 'https://mcp.linear.app/mcp',\n    connector: 'oauth/linear',\n  },\n} as const\n\nasync function createUserMCPClient(\n  name: keyof typeof MCP_SERVERS,\n  userId: string,\n) {\n  const server = MCP_SERVERS[name]\n\n  return createMCPClient({\n    transport: connectMCPTransport(\n      { type: 'http', url: server.url },\n      server.connector,\n      { subject: { type: 'user', id: userId } },\n    ),\n  })\n}\n</code></pre>\n<p>The route authenticates once, authorizes the requested integration, creates the client, handles a consent challenge, and only then starts <code>chat</code>. A <code>finally</code> block should close any client or transport resources exposed by the library version you use.</p>\n<p>For the broader MCP security model, see <a href=\"/blog/implementing-model-context-protocol-for-secure-agent\">Implementing Model Context Protocol for Secure Agents</a>. <a href=\"/blog/vercel-ai-sdk-vs-tanstack-ai-2026\">Vercel AI SDK vs TanStack AI</a> covers framework selection, while <a href=\"/blog/build-agentic-native-apis-beyond-static-endpoints\">Building Agentic-Native APIs</a> covers API boundaries beyond MCP.</p>\n<h2>Security Checklist</h2>\n<p>Before enabling an OAuth-protected MCP tool:</p>\n<ol>\n<li>Authenticate the application user before selecting a Connect subject.</li>\n<li>Derive the subject from the server session, never the request body.</li>\n<li>Keep connector IDs and MCP URLs in server-owned configuration.</li>\n<li>Allowlist exact HTTPS MCP origins and expected connector pairings.</li>\n<li>Create required MCP clients before model execution.</li>\n<li>Extract consent challenges at the route boundary.</li>\n<li>Use a safe redirect or structured client navigation flow.</li>\n<li>Store only an opaque, user-bound continuation state.</li>\n<li>Keep tokens out of prompts, logs, tool arguments, and browser bundles.</li>\n<li>Request minimum provider scopes and separate environments.</li>\n<li>Filter tools and enforce application authorization for every call.</li>\n<li>Require argument-bound approval for external mutations.</li>\n<li>Cap retries for 401s, outages, and repeated tool calls.</li>\n<li>Test denial, expiry, revocation, and cross-user isolation.</li>\n<li>Provide a visible disconnect and revocation path.</li>\n</ol>\n<p>The decisive architectural move is simple: authorization belongs before inference. Once the route hands TanStack AI an authenticated MCP client, the model can focus on tool selection. When the route cannot create that client, the user sees a normal consent flow instead of an agent trying to reason its way through an OAuth error.</p>\n<h2>Official Sources</h2>\n<ul>\n<li><a href=\"https://vercel.com/changelog/vercel-connect-tanstack-ai\">Vercel changelog: Connect now supports TanStack AI</a></li>\n<li><a href=\"https://vercel.com/connect/oauth\">Vercel Connect OAuth documentation</a></li>\n<li><a href=\"https://vercel.com/connect\">Vercel Connect</a></li>\n<li><a href=\"https://tanstack.com/ai/latest/docs/tools/mcp\">TanStack AI MCP tools documentation</a></li>\n<li><a href=\"https://tanstack.com/ai/latest\">TanStack AI documentation</a></li>\n<li><a href=\"https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization\">Model Context Protocol authorization specification</a></li>\n<li><a href=\"https://datatracker.ietf.org/doc/draft-ietf-oauth-v2-1/\">OAuth 2.1 draft</a></li>\n<li><a href=\"https://www.rfc-editor.org/rfc/rfc9700\">OAuth 2.0 security best current practice</a></li>\n<li><a href=\"https://vercel.com/docs/mcp\">Vercel Model Context Protocol documentation</a></li>\n<li><a href=\"https://vercel.com/docs/agent-resources/vercel-mcp\">Vercel MCP server documentation</a></li>\n</ul>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
