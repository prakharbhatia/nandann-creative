import type { BlogPost } from '../blogPosts';

const post: BlogPost = {
  slug: "bun-141",
  title: "Bun 1.4.1",
  description: "Bun 1.4.1 brings HTTP/2, offline installs, build improvements, and 202 fixes. A practical upgrade playbook for teams that need compatibility evidence, not release-note optimism.",
  date: "2026-09-04",
  readTime: "25 min",
  category: "Engineering",
  tags: ["Bun","JavaScript","Node.js","DevOps"],
  faqs: [
      {
          "question": "What is new in Bun 1.4.1?",
          "answer": "The official release lists 202 fixed issues, HTTP/2 support in Bun.serve, streamed Response writes to disk, self-contained workspace node_modules, offline install modes, WebSocket pause and resume, crypto.argon2, build improvements, and many compatibility fixes."
      },
      {
          "question": "Is Bun 1.4.1 a safe drop-in replacement for Node.js?",
          "answer": "No runtime upgrade is automatically a drop-in replacement. Test the actual application, its native addons, package-manager behavior, test runner, build output, deployment target, and rollback before changing production traffic."
      },
      {
          "question": "How should we upgrade Bun in CI?",
          "answer": "Pin the tested Bun version in setup and cache keys, install with the lockfile enforced, run typecheck, tests, build, and a representative integration suite, then promote the same version through environments."
      },
      {
          "question": "What do offline install modes do?",
          "answer": "bun install --offline avoids network use and only installs from available cache data. --prefer-offline prefers cache data while permitting a network fallback when required. Verify both against your registry and cache policy."
      },
      {
          "question": "Do workspace node_modules changes affect monorepos?",
          "answer": "They can. Bun 1.4.1 adds self-contained node_modules for workspace packages, so monorepos should inspect package resolution, deployment packaging, cache keys, and any tooling that assumes a hoisted layout."
      },
      {
          "question": "Should we switch a production HTTP server to HTTP/2 immediately?",
          "answer": "Only after validating your proxy, TLS termination, client mix, observability, and backpressure behavior. HTTP/2 is a transport capability, not a free performance setting."
      },
      {
          "question": "What is the rollback plan?",
          "answer": "Keep the previously working Bun version pinned and available in CI and the deployment image. Roll back the runtime version first, preserve logs and failing lockfiles or artifacts, and avoid rewriting the lockfile during incident response."
      }
  ],
  coverImage: "/images/bun-141.webp",
  contentHtml: "<picture>\n  <source media=\"(min-width: 1px)\" srcset=\"/images/bun-141.webp 1x\" type=\"image/webp\" />\n  <img src=\"/images/bun-141.webp\" alt=\"Bun 1.4.1\" style=\"width:100%; border-radius:12px; margin-bottom: 2rem;\" loading=\"eager\" width=\"1200\" height=\"630\" />\n</picture>\n\n<p>Bun releases often tempt teams into a one-line migration plan: update the setup action, run <code>bun upgrade</code>, enjoy the faster command. Bun 1.4.1 makes that instinct especially understandable. The release is substantial: 202 fixed issues, HTTP/2 in <code>Bun.serve</code>, streamed <code>Response</code> writes to disk, offline install modes, self-contained workspace <code>node_modules</code>, WebSocket flow control, Argon2 support, faster buffer operations, build-system improvements, and a long list of compatibility fixes.</p>\n<p>Those are useful reasons to upgrade. They are not a reason to skip the evidence. The runtime, package manager, test runner, bundler, compiler, and Node compatibility layer all participate in the same toolchain. A change that is invisible in a toy app can alter lockfile behavior, native addon loading, source-map output, production artifact size, cache effectiveness, signal handling, or a framework's assumptions about module resolution.</p>\n<p>This is the upgrade posture worth keeping: treat Bun 1.4.1 as a focused engineering change, not a tooling chore. Establish the current behavior, pin the candidate, test the boundaries that matter to your application, measure the claims you care about, and keep the prior version ready to restore. The release then becomes a chance to improve delivery discipline, not a bet on release-note confidence.</p>\n<h2>The Release in One Operational Sentence</h2>\n<h3>A broad release with many surfaces</h3>\n<p>According to Bun's official announcement, 1.4.1 fixes 202 issues and addresses 236 reported reactions. The headline additions cross several layers:</p>\n<ul>\n<li>runtime: HTTP/2 in <code>Bun.serve</code>, streamed writes, WebSocket <code>pause()</code> and <code>resume()</code>, <code>crypto.argon2</code>, and Node compatibility work;</li>\n<li>package management: self-contained workspace <code>node_modules</code>, <code>bun install --offline</code>, and <code>--prefer-offline</code>;</li>\n<li>building: dynamic-import tree shaking, chunking controls, better CommonJS-to-ESM conversion, and compile improvements;</li>\n<li>operations: faster startup and compiled executables, plus fixes across Windows, test execution, shell, SQLite, and the CLI.</li>\n</ul>\n<p>That breadth is precisely why one green unit-test run is not a complete migration signal. A typical application uses only a fraction of the runtime API but almost all of the delivery chain: install, resolution, test, build, containerization, startup, and traffic.</p>\n<h3>Start with the release's facts, not its adjectives</h3>\n<p>The announcement claims up to 9x faster <code>Buffer</code> reads and writes and 2x faster <code>AsyncLocalStorage</code> in relevant cases. Treat these as potential gains to measure in your workload, not universal expectations. A service dominated by database waits will not become nine times faster because its buffer copies improve. A CLI with many small imports may care much more about startup behavior than a long-lived server does.</p>\n<p>The upgrade question is not, \"Is 1.4.1 faster?\" It is, \"Does 1.4.1 preserve the behavior we rely on, and does it improve a bottleneck we can actually observe?\"</p>\n<h2>Inventory the Places Bun Is Already Involved</h2>\n<h3>Runtime, package manager, or both?</h3>\n<p>Many repositories say they use Bun when they only use it to install dependencies or run local scripts. Others run production handlers with Bun while using another package manager. A few rely on its test runner and bundler too. These have different risk profiles.</p>\n<p>Make the inventory explicit before the version changes:</p>\n<table>\n<thead>\n<tr><th>Surface</th><th>Questions to answer</th></tr>\n</thead>\n<tbody>\n<tr><td>Local development</td><td>Is Bun installed through a version manager, an action, a container, or a bootstrap script?</td></tr>\n<tr><td>CI</td><td>Is the version pinned? Are cache keys tied to the lockfile and runtime version?</td></tr>\n<tr><td>Package install</td><td>Do you use workspaces, private registries, patches, lifecycle scripts, or native packages?</td></tr>\n<tr><td>Test</td><td>Does <code>bun test</code> run all tests, only unit tests, or a framework adapter?</td></tr>\n<tr><td>Build</td><td>Is <code>bun build</code> producing browser chunks, a server bundle, or a compiled executable?</td></tr>\n<tr><td>Production</td><td>Is Bun the runtime, only a build-time dependency, or present in a release image?</td></tr>\n</tbody>\n</table>\n<p>Do not rely on tribal memory for this list. Search the repository for <code>bun</code>, <code>setup-bun</code>, <code>Bun.</code>, <code>bun.lock</code>, Docker images, package scripts, and CI actions. The first useful migration artifact is a map of what could change.</p>\n<h3>Capture a before-state</h3>\n<p>Record the existing Bun version, Node version if one is also present, operating-system images, lockfile checksum, package-manager cache configuration, build size, and a small set of request or CLI benchmarks. Capture a clean test run and at least one representative integration path.</p>\n<p>This is not paperwork for its own sake. It gives a failure an anchor. If an upgrade changes module resolution only in a Linux production image, you want to compare it to a known baseline rather than guess whether the issue was already present.</p>\n<pre><code class=\"language-bash\">bun --version\nsha256sum bun.lock 2&gt;/dev/null || shasum -a 256 bun.lock\nbun install --frozen-lockfile\nbun test\nbun run typecheck\nbun run build\n</code></pre>\n<p>Use the scripts your project actually defines. The point is a repeatable gate, not these exact names.</p>\n<h2>Pin the Candidate Before You Evaluate It</h2>\n<h3>Avoid floating runtime versions</h3>\n<p>\"Latest\" is a moving target. It makes a failing CI run impossible to reason about and makes rollback unnecessarily slow. Pin Bun 1.4.1 in the installation method used by developers and CI. If containers provide the runtime, pin the image tag or immutable digest. If <code>oven-sh/setup-bun</code> provisions it, configure that action with the same tested version.</p>\n<pre><code class=\"language-yaml\">- uses: oven-sh/setup-bun@v2\n  with:\n    bun-version: \"1.4.1\"\n\n- run: bun install --frozen-lockfile\n- run: bun test\n- run: bun run build\n</code></pre>\n<p>The action version and the Bun version are separate decisions. Pin both deliberately under your dependency update policy.</p>\n<h3>Keep the old version available</h3>\n<p>Before changing an environment, make sure the previous runtime is still downloadable from your approved source or present in the build cache. A rollback that depends on a network fetch during an incident is not a rollback plan.</p>\n<p>For a release image, keep the previous image reference. For CI, retain the prior runtime pin in the change record. For a developer bootstrap script, make the old version a documented parameter rather than forcing everyone to rediscover it from git history.</p>\n<h2>Package Installation Is a Production Surface</h2>\n<h3>Test the lockfile as an input and an output</h3>\n<p>The safest first installation is one that refuses to improvise dependency resolution. Run it from a clean workspace using the lockfile enforcement your repository expects. Review whether it changes <code>bun.lock</code>, package metadata, or installed layout. A runtime upgrade should not quietly become a mass dependency update.</p>\n<pre><code class=\"language-bash\">git clean -fdx  # use only in a disposable clone or CI workspace\nbun install --frozen-lockfile\ngit diff --exit-code -- bun.lock package.json\n</code></pre>\n<p>In a working directory, do not run destructive cleanup casually. CI or a disposable clone is the right place for an install reproducibility test.</p>\n<h3>Offline behavior needs a real cache drill</h3>\n<p><code>bun install --offline</code> is designed to avoid the network and use available cached data only. <code>--prefer-offline</code> prefers the cache but can reach the registry when something is missing. These are valuable capabilities for flaky connectivity, hermetic builds, and controlled environments, but only if the cache contains the dependencies you expect.</p>\n<p>Run two tests in an isolated CI job. First, populate the cache with a normal install. Then block registry access and run the offline install. Record which cache directories and credentials are required. Finally, run <code>--prefer-offline</code> with a deliberately missing package to confirm its fallback follows your network policy.</p>\n<p>Do not confuse an offline flag with supply-chain security. You still need lockfile review, registry controls, artifact provenance, and a policy for lifecycle scripts.</p>\n<h2>Monorepos Deserve Their Own Gate</h2>\n<h3>Self-contained workspace node_modules change assumptions</h3>\n<p>Bun 1.4.1 adds self-contained <code>node_modules</code> for workspace packages. That may be exactly what a monorepo needs. It can also reveal assumptions in tools that relied on a particular hoisted structure.</p>\n<p>Run each workspace from its own directory. Test package exports, CLI resolution, TypeScript project references, code generation, test discovery, and deployment packaging. Build the exact package that will ship rather than only the repository root.</p>\n<pre><code class=\"language-bash\">bun install --frozen-lockfile\nbun --cwd packages/api test\nbun --cwd packages/web run build\nbun --cwd packages/worker run integration:test\n</code></pre>\n<p>Replace these paths with your repository's structure. The purpose is to prove local package boundaries, not merely root-level success.</p>\n<h3>Private registries and native packages are stress points</h3>\n<p>If an install depends on scoped registry authentication, check it in the same environment model as CI. If it installs native addons, build or fetch them on every platform you ship. The Bun 1.4 series includes compatibility changes around Node and native modules, so this is not a place for a macOS-only developer check.</p>\n<p>Use a platform matrix where production has multiple architectures. At minimum, test the production operating system and architecture, then a developer platform if the project supports one. Keep the failure log and the precise package version; native compatibility failures are usually package-specific and need exact evidence when escalated upstream.</p>\n<h2>HTTP/2 in Bun.serve: Capability, Not Default Policy</h2>\n<h3>Validate the whole request path</h3>\n<p>Bun 1.4.1 adds HTTP/2 support in <code>Bun.serve</code>. Whether to use it depends on where TLS terminates and what sits in front of the process. A CDN or reverse proxy may already negotiate HTTP/2 with clients while using HTTP/1.1 upstream. Turning on a server capability without understanding that topology may add complexity with no user-facing benefit.</p>\n<p>Test protocol negotiation, headers, streaming behavior, timeouts, connection limits, error responses, tracing, and load balancer health checks. Confirm that metrics distinguish connection-level from request-level behavior. Test one slow client and a burst of multiplexed requests. Protocol changes often surface backpressure bugs rather than raw throughput issues.</p>\n<h3>A simple server is not the full test</h3>\n<pre><code class=\"language-ts\">const server = Bun.serve({\n  port: 3000,\n  fetch(request) {\n    return new Response(`ok: ${request.url}`)\n  },\n})\n\nconsole.log(`listening on ${server.url}`)\n</code></pre>\n<p>This establishes basic handler behavior. It does not validate proxy configuration, certificate management, streaming uploads, observability, or client compatibility. Keep the integration test close to the deployed topology.</p>\n<h2>Streaming Writes and WebSocket Flow Control</h2>\n<h3>Preserve backpressure when writing responses</h3>\n<p>The release lets <code>Bun.write(path, response)</code> stream a <code>Response</code> body to disk. That can reduce memory pressure for large responses, exports, or asset transforms. The engineering question is still ownership and backpressure: who handles a partial file, what happens on cancellation, where does temporary output live, and how is disk capacity observed?</p>\n<p>Test success, interrupted download, full disk, permission error, and restart recovery. If the output is customer-visible, write to a temporary path and atomically move it into place only after completion. Do not replace a known-good artifact with a partially written response.</p>\n<h3>Pause and resume are flow-control tools</h3>\n<p>WebSocket <code>pause()</code> and <code>resume()</code> can help applications control inbound work. They should be driven by a bounded queue or a clear pressure signal, not by an unbounded attempt to protect every callback. Test slow consumers, disconnects while paused, reconnect churn, and shutdown. The best flow-control change is one whose queue depth and recovery state are visible in metrics.</p>\n<h2>Security and Compatibility Gates</h2>\n<h3>Argon2 deserves parameter review</h3>\n<p>The new Node-compatible <code>crypto.argon2</code> and <code>crypto.argon2Sync</code> APIs are welcome when an application needs modern password hashing. Do not treat their presence as a mandate to change credential formats in the same runtime upgrade. Password migrations need a separate design: parameter selection, stored hash format, compatibility with existing users, timing behavior, and a gradual rehash policy after successful login.</p>\n<p>If you do adopt it, use the asynchronous API on request paths unless you have measured the synchronous cost. Bound concurrent work. Monitor latency and memory. Verify hashes against documented test vectors and your security review requirements.</p>\n<h3>Compatibility means testing the dependencies you own</h3>\n<p>The release includes Node.js compatibility improvements and smarter CommonJS-to-ESM default import conversion. That is valuable, but it is exactly the type of change that can reveal an application that relies on accidental module semantics. Exercise the framework startup, test environment, code generators, dynamic imports, and the packages that use both <code>require</code> and <code>import</code>.</p>\n<p>Avoid a broad source rewrite during the upgrade. If one import changes behavior, isolate it, add a test, and decide whether the correct fix is a package update, a narrower import form, or a compatibility configuration. Keep the runtime change reviewable.</p>\n<h2>Build Output Must Be Compared, Not Assumed</h2>\n<h3>Dynamic imports and splitting can move work</h3>\n<p>1.4.1 introduces tree shaking through dynamic <code>import()</code>, smaller or fewer chunks with splitting controls, <code>--min-chunk-size</code>, module preloading for code-split browser builds, and revised chunk-boundary behavior in relevant targets. Those changes can improve payloads. They can also move code between chunks in ways that affect preload behavior, caching, error reporting, and application startup.</p>\n<p>Build a production-shaped bundle under both versions. Compare artifact manifests, total bytes, chunk count, source maps, and the request waterfall in a browser or synthetic environment. Test a cold navigation, a route loaded through dynamic import, and an error path that exercises a lazy chunk.</p>\n<pre><code class=\"language-bash\">bun build ./src/client.ts \\\n  --outdir=dist \\\n  --splitting \\\n  --target=browser \\\n  --sourcemap=linked\n</code></pre>\n<p>Do not optimize from the output directory alone. A smaller bundle that creates an extra round trip on the critical route can still be a slower user experience.</p>\n<h3>Compiled executables require platform evidence</h3>\n<p>The release calls out smaller and faster-starting compiled executables, including <code>--compile --bytecode</code> support for cross-compilation. Benchmark cold startup and confirm the artifact runs on the exact target environment. Test configuration files, assets, dynamic imports, native dependencies, signals, exit codes, and write permissions. A fast binary is only useful if it remains diagnosable and deployable.</p>\n<h2>Build a Promotion Pipeline Instead of a Big-Bang Switch</h2>\n<h3>Stage 1: hermetic CI</h3>\n<p>Pin 1.4.1, install from a clean workspace, run static checks, unit tests, integration tests, and build. Repeat on every production platform. Make lockfile changes fail the job unless the change explicitly includes them.</p>\n<h3>Stage 2: a narrow runtime canary</h3>\n<p>Deploy the same built artifact to a small traffic slice or non-critical worker class. Compare startup time, errors, latency, memory, CPU, dependency calls, and log volume to the baseline. If your process serves WebSockets or streaming downloads, include those traffic types in the canary.</p>\n<h3>Stage 3: progressive promotion</h3>\n<p>Promote only after the canary has covered a meaningful traffic period and scheduled jobs. Keep the rollback pin ready. During the first full deployment, avoid unrelated framework, dependency, or lockfile changes. One variable makes diagnosis faster.</p>\n<h2>A Rollback Plan You Can Actually Execute</h2>\n<h3>Decide the trigger before deployment</h3>\n<p>Write down which signals cause a rollback: a startup failure rate, sustained 5xx change, incompatible native package, lockfile install failure, bundle load error, or a significant regression against agreed service objectives. Give the on-call engineer a versioned command or deployment reference, not a prose promise that rollback is easy.</p>\n<h3>Preserve the evidence</h3>\n<p>When rolling back, retain the Bun version, operating-system image, package manager logs, lockfile, build manifest, failing stack trace, and minimal reproduction. Do not regenerate the lockfile in the middle of response. The best upstream bug reports state the previous working version, the new failing version, the platform, expected behavior, and a small repo or command sequence.</p>\n<h2>Observe the Upgrade While It Is Boring</h2>\n<p>The first hour after a runtime rollout is usually quiet, which is exactly when teams are tempted to call it complete. Keep a small comparison view for the candidate and baseline: process start failures, request errors, latency percentiles, memory, CPU, event-loop or queue pressure, dependency errors, install duration, and build output. For browser bundles, include client-side chunk-load failures and route-level performance. For a CLI, include exit-code distribution and startup timing.</p>\n<p>This is not a request for a new observability platform. It is a request to decide in advance what evidence would prove that the upgrade behaved differently. A clear comparison period also protects against confirmation bias. Faster local <code>bun install</code> is pleasant, but it should not hide an error on the production-only code path.</p>\n<h2>Split Runtime Adoption From Feature Adoption</h2>\n<p>It is reasonable to upgrade to 1.4.1 without immediately enabling HTTP/2, moving password hashes to Argon2, changing WebSocket pressure handling, or retuning bundle splitting. Those are separate product and operations decisions. First establish that the runtime version is compatible. Then evaluate each new capability with its own design, security review, benchmark, and rollback plan.</p>\n<p>This sequencing produces smaller diffs and faster diagnosis. If a deployment changes the runtime, transport protocol, build strategy, and authentication format at once, no test result can tell you which change was responsible. Adopt the new runtime first. Harvest its features deliberately afterward.</p>\n<h2>Make CI Prove Reproducibility</h2>\n<p>A high-value Bun job starts from a fresh checkout and has no undeclared dependency on a developer's global cache. It prints the runtime version, restores only the caches it expects, installs with the lockfile constrained, and runs the same scripts a release would run. Put the version and cache key in the build log so an artifact is always traceable back to its toolchain.</p>\n<p>Run one job without a warm package cache periodically. A cache hides registry, integrity, lifecycle, and resolution behavior that a new runner will encounter. Then run the normal cached job so delivery speed is still measured. If offline installation is part of the plan, add a deliberately isolated job after cache population rather than assuming a normal install has exercised the offline path.</p>\n<p>For an application that supports Node as well as Bun, retain a small Node compatibility lane during the migration. That lane is not an argument against Bun. It is a way to distinguish an application regression from an implementation-specific behavior change, and it gives the team a fallback while the runtime rollout earns confidence.</p>\n<h2>What Success Looks Like</h2>\n<p>Success is not merely a green <code>bun --version</code> command. It is a pinned 1.4.1 toolchain that installs a clean checkout reproducibly, builds the expected artifacts, passes meaningful tests on shipping platforms, and runs a canary without a new error or resource pattern. It has an explicit old-version rollback and a short evidence trail that another engineer can follow.</p>\n<p>That is a modest definition, and it scales. Once it exists, the next Bun release is a predictable maintenance change rather than a fresh migration project.</p>\n<h2>The Upgrade Checklist</h2>\n<ol>\n<li>Inventory where Bun acts as runtime, installer, test runner, builder, or compiler.</li>\n<li>Record current versions, lockfile hash, artifact sizes, and key baseline metrics.</li>\n<li>Pin Bun 1.4.1 in CI, images, and developer setup.</li>\n<li>Install in a clean workspace with the project lockfile enforced.</li>\n<li>Run a cache and offline-install drill if reproducible installs matter.</li>\n<li>Test every workspace, private registry path, and native package on shipping platforms.</li>\n<li>Compare build manifests and exercise dynamically imported routes.</li>\n<li>Test HTTP/2, streams, WebSockets, and cryptography only where your application uses them.</li>\n<li>Canary the same artifact that passed CI.</li>\n<li>Retain a documented pin and command for rollback.</li>\n</ol>\n<h2>The Bottom Line</h2>\n<p>Bun 1.4.1 is a worthwhile release because it improves real seams: serving, file output, installation, workspaces, dependency compatibility, testing, building, and compiled deployment. Its 202 fixes are also a reminder that those seams contain a great deal of behavior.</p>\n<p>Upgrade with a pinned version, a baseline, platform-specific tests, production-shaped integration checks, and an explicit rollback. Do that and the release's improvements become a controlled gain. Skip it and a promising runtime update can become a difficult incident with too many variables.</p>\n<h2>Sources and further reading</h2>\n<ol>\n<li><a href=\"https://bun.com/blog/bun-v1.4.1\">Bun v1.4.1 release notes</a></li>\n<li><a href=\"https://bun.com/blog/bun-v1.4\">Bun v1.4 release notes</a></li>\n<li><a href=\"https://bun.com/docs/feedback\">Bun upgrade documentation</a></li>\n<li><a href=\"https://github.com/oven-sh/bun/issues/28792\">Bun 1.4 breaking changes issue</a></li>\n<li><a href=\"https://bun.com/docs/installation\">Bun installation documentation</a></li>\n<li><a href=\"https://bun.com/docs/install/lockfile\">Bun lockfile documentation</a></li>\n<li><a href=\"https://bun.com/docs/runtime/nodejs-compat\">Bun Node.js compatibility documentation</a></li>\n<li><a href=\"https://bun.com/docs/pm/cli/install\">Bun install CLI documentation</a></li>\n<li><a href=\"https://bun.com/docs/test\">Bun test documentation</a></li>\n<li><a href=\"https://github.com/oven-sh/setup-bun\">oven-sh/setup-bun action</a></li>\n</ol>\n<hr />\n<div style=\"background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2); border-radius: 12px; padding: 2rem; margin-top: 3rem;\">\n  <h3 style=\"color: #60a5fa; margin-top: 0;\">Let's build something together</h3>\n  <p>We build fast, modern websites and applications using Next.js, React, WordPress, Rust, and more. If you have a project in mind or just want to talk through an idea, we'd love to hear from you.</p>\n  <p><a href=\"/contact?ref=blog\" style=\"display: inline-block; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; padding: 0.75rem 1.5rem; border-radius: 8px; text-decoration: none; font-weight: 600; margin-top: 0.5rem;\">Start a Project &rarr;</a></p>\n</div>\n",
};

export default post;
